# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# uv
.venv/

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp

# Eä-managed.
# AGENTS.md is committed (managed by render layer; user-region preserved on re-render).
# CLAUDE.md + .claude/ stay gitignored until plugin-install pass is self-applied.
CLAUDE.md
.claude/
.opencode/

# .ea/ — state.json + config.yaml are committed; locks + local scratch + manifest are not.
.ea/locks/
.ea/local/
.ea/indexes/

# Secrets
.env
*.pem
*.key
secrets/
