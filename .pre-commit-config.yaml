repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.12
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
        args: [--maxkb=1024]
      - id: check-merge-conflict
      - id: debug-statements

  - repo: https://github.com/Yelp/detect-secrets
    # Baseline-hash: 537238d809fdc33a (.secrets.baseline; recompute and bump
    # this comment when the baseline is edited so the change is re-acknowledged).
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        args: [--baseline, .secrets.baseline]
        exclude: 'uv\.lock|\.gitignore'

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v2.0.0
    hooks:
      - id: mypy
        stages: [manual]
        additional_dependencies:
          - pydantic>=2.13
          - typer>=0.25

  - repo: local
    hooks:
      - id: commit-prefix-lint
        name: commit-prefix-lint
        entry: python tools/commit_prefix_lint.py
        language: system
        stages: [commit-msg]
      - id: eawf-doc-verify-strict
        name: eawf-doc-verify-strict
        entry: uv run eawf doc verify --strict
        language: system
        pass_filenames: false
        stages: [pre-push]
