[project]
name = "eawf"
version = "0.1.0.dev0"
description = "Eä Workflow — agent-driven development framework (v0.1 in development)"
readme = "README.md"
requires-python = ">=3.14"
license = { text = "MIT" }
authors = [
    { name = "Elementarno9", email = "edwardpro9@gmail.com" },
]
dependencies = [
    "jsonschema>=4.26.0",
    "orjson>=3.11.9",
    "platformdirs>=4.9.6",
    "portalocker>=3.2.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.1",
    "rich>=15.0.0",
    "textual>=8.2.5",
    "typer>=0.25.1",
]

[project.scripts]
eawf = "eawf.cli.app:main"
ea = "eawf.cli.app:main"

[tool.uv]
package = true

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-cov>=5.0",
    "pytest-xdist>=3.6",
    "hypothesis>=6.112",
    "ruff>=0.6.9",
    "mypy>=1.13",
    "pre-commit>=3.8",
    "detect-secrets>=1.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers --strict-config"
markers = [
    "unit: fast in-process unit tests",
    "integration: integration tests crossing module boundaries",
    "property: Hypothesis-driven property tests",
    "golden: golden-output regression tests",
    "slow: long-running tests (excluded from default run)",
]

[tool.mypy]
strict = true
python_version = "3.14"
packages = ["eawf"]

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "C4", "SIM", "RUF"]
ignore = []

[tool.coverage.run]
source = ["src/eawf"]
branch = true

[tool.coverage.report]
fail_under = 0

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/eawf"]
