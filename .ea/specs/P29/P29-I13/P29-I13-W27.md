<!-- eawf-template: spec-wave -->

# Add the carousel tour modal with dismissible cards

**Scope:** P29-I13-W27

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the carousel tour modal advances its dismissible cards, asserted when the tour Pilot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "Pilot asserts the tour carousel advances across its cards and sets ui.tour_completed on dismissal"
    gate_ids: [G-01]
  - id: CR-02
    text: "the advertised tour key transitions to its action via tui_pilot"
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "the advertised tour next and dismiss keys resolve to live bindings that advance and close the tour modal"
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_tour_modal.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: affordance_parity
    args:
      mode: home
    policy: block
    cadence: every-wave
```
