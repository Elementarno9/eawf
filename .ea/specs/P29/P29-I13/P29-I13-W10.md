<!-- eawf-template: spec-wave -->

# Add the verifier-role drill modal

**Scope:** P29-I13-W10

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the verifier-role drill modal renders the per-criterion oracle tier, asserted when the modal snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "snapshot asserts the verifier drill modal shows the oracle tier and producer for each scored criterion"
    gate_ids: [G-01]
  - id: CR-02
    text: "the advertised verifier key transitions to its action via tui_pilot"
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "the advertised verifier key resolves to a live binding that opens the verifier-role drill modal"
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_verifier_drill.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: affordance_parity
    args:
      mode: trust
    policy: block
    cadence: every-wave
```
