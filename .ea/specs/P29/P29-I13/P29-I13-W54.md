<!-- eawf-template: spec-wave -->

# Stabilize snapshot daemon-banner + perf ceilings under CI load

**Scope:** P29-I13-W54

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the snapshot normalizer strips the env-dependent daemon-degraded banner, asserted when the pilot-harness test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "normalize_snapshot drops the daemon-socket-unavailable banner line (and its runtime socket path) so captures stay byte-stable regardless of daemon availability"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_pilot_harness.py", "-q"]
      scope: touched
      timeout_class: standard
    policy: block
    cadence: every-wave
```
