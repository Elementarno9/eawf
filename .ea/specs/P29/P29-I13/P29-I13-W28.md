<!-- eawf-template: spec-wave -->

# Add active-mode highlight to the help screen

**Scope:** P29-I13-W28

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the help screen renders the active mode with the highlight style, asserted when the help snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "snapshot asserts the help screen shows the active mode row with the highlight style applied"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_help_active_mode.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
