<!-- eawf-template: spec-wave -->

# Add ctx and rate-window progress bars to the statusline

**Scope:** P29-I13-W39

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the statusline renders the ctx and rate-window progress bars, asserted when the bars snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "the statusline renders a context-usage bar and a rate-window bar each as a block-eighths progress glyph"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/statusline/test_statusline_bars.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
