<!-- eawf-template: spec-wave -->

# Add a determinate ProgressBar wrapper widget

**Scope:** P29-I13-W21

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the determinate ProgressBar wrapper renders the bound fill ratio, asserted when the widget Pilot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "Pilot asserts the ProgressBar widget shows the block-eighths fill matching the bound completion ratio"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_progress_widget.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
