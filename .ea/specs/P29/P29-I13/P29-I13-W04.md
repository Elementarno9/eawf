<!-- eawf-template: spec-wave -->

# Render the evidence ledger over typed criteria

**Scope:** P29-I13-W04

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the evidence ledger renders one row per typed criterion, asserted when the ledger snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "build_evidence_ledger emits one row per CloseReadiness criterion with criterion gate status and produced_by columns"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_evidence_ledger.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
