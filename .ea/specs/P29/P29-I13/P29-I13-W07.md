<!-- eawf-template: spec-wave -->

# Add scrub-gated evidence export

**Scope:** P29-I13-W07

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the evidence export emits a scrub-clean manifest, asserted when the export test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: security
    measurable_signal: "the evidence export refuses a payload carrying unscrubbed host paths and emits a scrub-clean manifest"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/tui/test_evidence_export.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
