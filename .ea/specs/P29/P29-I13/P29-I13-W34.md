<!-- eawf-template: spec-wave -->

# Add the PR-count column to the workspace dashboard

**Scope:** P29-I13-W34

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the workspace dashboard renders the PR-count column, asserted when the column snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "snapshot asserts the workspace dashboard shows a PR-count column with the open-PR total per repo row"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_workspace_pr_count.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
