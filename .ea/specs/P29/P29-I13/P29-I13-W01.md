<!-- eawf-template: spec-wave -->

# Migrate state on TUI load and show a staleness banner

**Scope:** P29-I13-W01

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the TUI staleness banner is rendered on load, asserted when the state-binding snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "snapshot asserts the staleness banner shows when the bound state schema_version trails the live daemon schema"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_state_binding.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
