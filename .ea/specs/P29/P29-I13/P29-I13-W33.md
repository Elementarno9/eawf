<!-- eawf-template: spec-wave -->

# Add cross-repo attention chips to the workspace table

**Scope:** P29-I13-W33

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the workspace table renders cross-repo attention chips, asserted when the chips snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "snapshot asserts each repo row renders an attention chip when its blocker or stale-band threshold trips"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_workspace_chips.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
