<!-- eawf-template: spec-wave -->

# Sweep the affordance matrix across all legal scopes

**Scope:** P29-I13-W12

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the affordance matrix sweep reports zero unresolved advertised keys, asserted when the matrix sweep test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "the affordance sweep over NAV_SCOPES and MODE_REGISTRY reports zero unresolved advertised keys outside DEFERRED_KEYS"
    gate_ids: [G-01]
  - id: CR-02
    text: "the advertised home-scope key transitions to its action via tui_pilot"
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "each advertised home-scope key resolves to a live binding with no dead advertised key"
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_affordance_matrix.py", "-q"]
      scope: touched
      timeout_class: slow
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: affordance_parity
    args:
      mode: home
    policy: block
    cadence: every-wave
```
