<!-- eawf-template: spec-wave -->

# Add the portfolio-totals reducer and summary row

**Scope:** P29-I13-W32

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the portfolio-totals reducer returns a totals row, asserted when the reducer test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the portfolio reducer sums per-repo wave counts and EU into the totals row rendered under the workspace table"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/tui/test_portfolio_totals.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
