<!-- eawf-template: spec-wave -->

# Populate the unresolved tab from open questions

**Scope:** P29-I13-W16

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the research board unresolved tab renders one row per open question, asserted when the unresolved snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "snapshot asserts the unresolved tab lists one row per OpenQuestion with its status and round number"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_research_board_unresolved.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
