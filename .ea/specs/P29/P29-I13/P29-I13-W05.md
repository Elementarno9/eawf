<!-- eawf-template: spec-wave -->

# Join evidence records to criterion rows

**Scope:** P29-I13-W05

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the evidence-to-criterion join returns matched rows, asserted when the join test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the join maps each EvidenceRecord to its criterion id and groups unmatched evidence under an orphan section"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/tui/test_evidence_join.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
