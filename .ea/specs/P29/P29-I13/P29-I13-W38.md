<!-- eawf-template: spec-wave -->

# Add the rows-aware multi-line statusline renderer

**Scope:** P29-I13-W38

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the rows-aware statusline renders the configured row count, asserted when the rows snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "the statusline renderer emits the configured row count and file matches the committed multi-line golden"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/statusline/test_statusline_rows.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
