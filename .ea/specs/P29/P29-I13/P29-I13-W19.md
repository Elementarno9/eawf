<!-- eawf-template: spec-wave -->

# Test the config-modal writable-layers lock for new keys

**Scope:** P29-I13-W19

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the config modal renders the new keys read-only on locked layers, asserted when the lock snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "snapshot asserts the config modal shows the adapter-enable and budget keys read-only on every layer the writable-lock forbids"
    gate_ids: [G-01]
  - id: CR-02
    text: "the advertised config key transitions to its action via tui_pilot"
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "the advertised config key resolves to a live binding that opens the config modal"
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_config_modal_lock.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: affordance_parity
    args:
      mode: config
    policy: block
    cadence: every-wave
```
