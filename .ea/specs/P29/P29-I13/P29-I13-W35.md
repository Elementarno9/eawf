<!-- eawf-template: spec-wave -->

# Align the offline workspace render with the totals row

**Scope:** P29-I13-W35

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the offline workspace render emits the totals-row layout, asserted when the offline render test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the offline workspace render emits the same totals-row layout as the live render when no daemon is reachable"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/render/test_workspace_offline.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
