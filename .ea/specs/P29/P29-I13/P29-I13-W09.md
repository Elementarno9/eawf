<!-- eawf-template: spec-wave -->

# Render the escape-ledger readout in the trust mode

**Scope:** P29-I13-W09

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the trust-mode escape-ledger readout is rendered, asserted when the escape-ledger snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "snapshot asserts the escape-ledger readout lists each escaped criterion with its waiver reason"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_trust_escape_ledger.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
