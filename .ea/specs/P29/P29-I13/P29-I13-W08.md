<!-- eawf-template: spec-wave -->

# Add the oracle-determinism section to the trust mode

**Scope:** P29-I13-W08

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the trust-mode oracle-determinism section is rendered, asserted when the determinism snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "snapshot asserts the trust mode shows the oracle-determinism ratio computed over closed criteria"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_trust_determinism.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
