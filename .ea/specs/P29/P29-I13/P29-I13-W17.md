<!-- eawf-template: spec-wave -->

# Populate options and conflicts tabs from claims

**Scope:** P29-I13-W17

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the research board options and conflicts tabs render claim rows, asserted when the claims snapshot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "snapshot asserts the options and conflicts tabs render claims grouped by supporting and contradicting evidence"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_research_board_claims.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
