<!-- eawf-template: spec-wave -->

# Propagate the render-mode flip and verify ascii fallback

**Scope:** P29-I13-W25

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the render-mode flip swaps every bar to the ascii fallback, asserted when the flip Pilot test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: compatibility
    measurable_signal: "Pilot asserts the unicode-to-ascii render-mode flip swaps every block-eighths bar to an ascii fallback glyph"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_render_mode_flip.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
