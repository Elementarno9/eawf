<!-- eawf-template: spec-wave -->

# Add glyph and color mode resolution to the statusline

**Scope:** P29-I13-W37

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the statusline resolves glyph and color modes, asserted when the resolution test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: compatibility
    measurable_signal: "the statusline resolves glyph-mode and color-mode from config and downgrades to ascii on a no-color terminal"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/render/test_statusline_modes.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
