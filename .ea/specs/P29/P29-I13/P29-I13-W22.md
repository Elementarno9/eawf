<!-- eawf-template: spec-wave -->

# Add the indeterminate braille spinner helper

**Scope:** P29-I13-W22

```eawf-wave-body
criteria:
  - id: CR-01
    text: "the braille spinner helper returns the next frame, asserted when the spinner test exits zero under pytest"
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "the spinner helper returns the next braille frame in the eight-dot cycle on each advance call"
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/render/test_spinner.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
