<!-- eawf-template: spec-wave -->

# RS-31 reskin the brief_viewer markdown chassis to green + sigil

**Scope:** P30-I02-W31

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden of the BriefViewerScreen markdown modal asserts the two-tone green wordmark header, the green accent, and sigil-prefixed claim/status rows render in the reskinned language across the whole modal chassis, not just the single ClaimStatus sigil site the earlier research-board pass touched."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_brief_viewer_reskin.py asserts the two-tone wordmark header, green accent, and sigil-prefixed claim/status rows on the BriefViewerScreen golden; pytest exit zero"
    response:
      observe: exits
      object: "zero from the brief-viewer reskin golden suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_brief_viewer_reskin.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
