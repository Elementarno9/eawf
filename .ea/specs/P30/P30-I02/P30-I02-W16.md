<!-- eawf-template: spec-wave -->

# RS-16 reskin feed to lifecycle sigil + fixed timestamp column

**Scope:** P30-I02-W16

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the feed renders a lifecycle sigil + a fixed-width timestamp column and a two-cell sigil column per event line, and that the no-events path renders the waiting-for-events sentinel."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_modes_feed asserts each event line renders a lifecycle sigil + a fixed-width HH:MM:SS timestamp column + a two-cell sigil column, and the no-events path renders the waiting-for-events sentinel; pytest exit zero"
    response:
      observe: exits
      object: "zero from the feed lifecycle-sigil + fixed-timestamp + empty-sentinel suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_modes_feed.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
