<!-- eawf-template: spec-wave -->

# RS-14 reskin doctor to grouped checks + per-check sigil

**Scope:** P30-I02-W14

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the doctor groups install/state/drift checks, renders the ok/warn/fail sigil per check (warn distinct from pending), and the header reads N checks, M warn."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_modes_doctor asserts install/state/drift grouping, the ok/warn/fail sigil per check (warn renders the attention triangle distinct from the pending ring), and the header reads N checks, M warn; pytest exit zero"
    response:
      observe: exits
      object: "zero from the doctor grouped-checks + per-check-sigil suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_modes_doctor.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
