<!-- eawf-template: spec-wave -->

# RS-22 reskin confirm/multichoice/init/PR overlays

**Scope:** P30-I02-W22

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the multichoice filled/empty toggle glyphs, the confirm + init-wizard sigil/key-hint vocab and the narrow-sigil PR grid render with no glyph stranding; affordance_parity confirms the toggle/confirm keys resolve to live Bindings and fire."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_overlays_reskin_w22 + test_overlays_multichoice_checklist assert the check_on/check_off toggle glyphs, confirm + init-wizard sigil/key-hint vocab, narrow-sigil PR grid with no stranding, and affordance_parity that toggle/confirm/nav keys resolve to live Bindings and fire; pytest exit zero"
    response:
      observe: exits
      object: "zero from the confirm/multichoice/init/PR overlay reskin + affordance suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_overlays_reskin_w22.py", "tests/tui/test_overlays_multichoice_checklist.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
