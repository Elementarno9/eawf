<!-- eawf-template: spec-wave -->

# RS-11 reskin autopilot to ready/blocked split + dispatch arrow

**Scope:** P30-I02-W11

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the autopilot renders the ready/blocked split with blocked rows naming their dep, the dispatch chrome arrow, and the multi-select look; affordance_parity confirms the dispatch action key resolves to a live Binding and fires."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_autopilot_split asserts the ready/blocked split (blocked rows name their dep), the dispatch chrome arrow + multi-select look via snapshot golden, and affordance_parity that d->dispatch_selected resolves to a live Binding and fires; pytest exit zero"
    response:
      observe: exits
      object: "zero from the autopilot ready/blocked split snapshot + affordance-parity suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_autopilot_split.py", "tests/tui/test_modes_autopilot.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
