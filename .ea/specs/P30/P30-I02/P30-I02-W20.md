<!-- eawf-template: spec-wave -->

# RS-20 reskin config modal + edit field overlay

**Scope:** P30-I02-W20

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the config modal renders the overridden marker via sigils, typed bool/choice/float chips and the layer indicator, and the scalar edit-field overlay renders its inline validation chip + caret in the rotated palette."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_config_modal_reskin_w20 asserts the overridden marker is the claimed sigil (not asterisk), typed bool/choice/float value chips, the overview-sigil layer line, and the edit-field inline validation chip + green caret in the rotated palette; pytest exit zero"
    response:
      observe: exits
      object: "zero from the config-modal + edit-field reskin snapshot suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_config_modal_reskin_w20.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
