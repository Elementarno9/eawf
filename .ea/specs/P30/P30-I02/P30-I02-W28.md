<!-- eawf-template: spec-wave -->

# RS-28 ratify the extended sigil alphabet and totality gate

**Scope:** P30-I02-W28

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Every extended status resolves via status_sigil to a ratified glyph (ABANDONED/ARCHIVED + BLOCKED/WAIVED to the 6th withheld sigil, PASS_WITH_FOLLOWUPS to closed+badge, MINOR/STALE to warn, MAJOR to failed), never '?' and never a bare .value, warn mapping to the triangle not the pending ring; a sigil-totality gate over every status enum + the FSM terminals fires on a deliberately-unmapped value; the brief_viewer ClaimStatus bullet leads with a sigil."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_widgets_sigils + test_sigil_totality_gate prove every status enum + FSM terminal resolves via status_sigil to a ratified glyph (warn=triangle, no ? or bare .value) and the gate FIRES on an unmapped value; pytest exit zero"
    response:
      observe: exits
      object: "zero from the extended-alphabet resolver + firing sigil-totality gate suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_widgets_sigils.py", "tests/unit/test_sigil_totality_gate.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
