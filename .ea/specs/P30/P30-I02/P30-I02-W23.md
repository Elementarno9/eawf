<!-- eawf-template: spec-wave -->

# RS-23 reskin metrics/drills/help/events/tour

**Scope:** P30-I02-W23

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the metrics overlay pins the frozen literal honest-negative lights up after EU capture (middle-dot), and the calibration table + drill teaching cards + keymap reference + sigil event log + first-run tour render the migrated sigils/green language."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_metrics_overlay pins the frozen honest-negative middle-dot literal; test_overlays_events asserts the lifecycle-sigil event log; the calibration table + 3 drills + keymap + tour render the migrated sigils/green language; pytest exit zero"
    response:
      observe: exits
      object: "zero from the metrics frozen-literal + events sigil-log + drills/help/tour reskin suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_metrics_overlay.py", "tests/tui/test_overlays_events.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
