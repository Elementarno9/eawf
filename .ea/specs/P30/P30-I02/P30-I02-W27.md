<!-- eawf-template: spec-wave -->

# RS-27 tui_flow audit kind + 7 operator-journey flow gates

**Scope:** P30-I02-W27

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The tui_flow kind unit tests assert a passing flow and a missing-terminal-state fail-path that names the divergent observable field, each of the 7 operator-journey flow specs runs green against the reskinned surfaces, a malformed flow spec degrades-not-raises, and the behaviour_probe keypress-transcript driver the kind wraps holds its own contract."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_tui_flow.py asserts pass-flow, divergent-field fail-path naming, all 7 flow specs green, and malformed-spec degrade; test_behaviour_probe.py asserts the driver contract; pytest exit zero"
    response:
      observe: exits
      object: "zero from the tui_flow kind + behaviour-probe suites"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_tui_flow.py", "tests/snapshots/tui/test_behaviour_probe.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
