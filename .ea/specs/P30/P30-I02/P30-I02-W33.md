<!-- eawf-template: spec-wave -->

# RS-33 reskin the placeholder coming-soon mode to the honest-empty voice

**Scope:** P30-I02-W33

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden of the placeholder mode asserts the calm honest-empty voice (green accent, sigil, the byte-for-byte coming-soon copy) renders in the reskinned language; the pre-reskin shell fails the golden."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_placeholder_reskin asserts the placeholder mode renders the calm honest-empty voice (green accent + pending sigil + the byte-for-byte coming-soon copy) and a discrimination test proves the pre-reskin shell fails the reskin golden; pytest exit zero"
    response:
      observe: exits
      object: "zero from the placeholder honest-empty reskin + discrimination suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_placeholder_reskin.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
