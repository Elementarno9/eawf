<!-- eawf-template: spec-wave -->

# RS-05 unify the bar renderer + retire dead Braille

**Scope:** P30-I02-W05

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Re-pointed bar-swap and render-mode-flip tests assert the unicode/ascii flip swaps every block glyph and a completion bar renders full block cells (unicode) and the ascii ratio; the deletion proof asserts importing the removed BRAILLE_* names and render_bar_braille raises ImportError and the retired Braille test block is gone."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "eu_bar + bar_swap + render_mode_flip assert the unicode/ascii flip swaps every block glyph and the completion bar renders full block cells + the ascii ratio; the deletion proof asserts the removed BRAILLE_* names raise ImportError; pytest exit zero"
    response:
      observe: exits
      object: "zero from the eu_bar unit + bar-swap + render-mode-flip suites incl the Braille deletion proof"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_widgets_eu_bar.py", "tests/snapshots/tui/test_bar_swap.py", "tests/snapshots/tui/test_render_mode_flip.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
