<!-- eawf-template: spec-wave -->

# RS-26 per-pane error boundary + generic crash frame

**Scope:** P30-I02-W26

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A Pilot test injects a render exception into one pane and asserts the boundary frame renders the fail sigil plus the literal crash copy, the neighbouring panes still render, and r/l/Esc resolve to live Bindings and fire (retry / view-log / dismiss); a healthy pane renders no boundary frame and one pane exception does not escalate to App panic."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_pane_error_boundary.py asserts the boundary frame (fail sigil + crash copy), neighbour-pane survival, live r/l/Esc bindings firing, no boundary frame on a healthy pane, and no App panic on a single-pane exception; pytest exit zero"
    response:
      observe: exits
      object: "zero from the per-pane error-boundary Pilot suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_pane_error_boundary.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
