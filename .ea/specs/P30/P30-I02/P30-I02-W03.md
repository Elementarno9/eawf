<!-- eawf-template: spec-wave -->

# RS-03 reskin header to two-tone Eae + idle runtime cell

**Scope:** P30-I02-W03

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the header renders the two-tone Eae wordmark with the accent on the ae and an idle runtime cell rendering the harmony chrome glyph + idle (no runtime label), with the breadcrumb unchanged."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_header_wordmark_two_tone asserts the umlaut cell resolves #16b384 while the E cell does not (two-tone via render_wordmark_markup), the idle runtime cell renders the harmony glyph + idle with no runtime label, and the breadcrumb is byte-equal to the builder; pytest exit zero"
    response:
      observe: exits
      object: "zero from the header two-tone wordmark + idle-cell + breadcrumb-unchanged suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_header_wordmark_two_tone.py", "tests/tui/test_widgets_header.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
