<!-- eawf-template: spec-wave -->

# RS-04 reskin footer + heartbeat to green/sigil tokens

**Scope:** P30-I02-W04

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the footer active-mode token resolves the rotated accent, the needs_user badge resolves warn, and the heartbeat dot resolves accent/err through the rotated vars."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_footer_heartbeat_green_accent_capture captures the footer active-mode token at #16b384, the needs_user badge at warn, and the heartbeat dot at accent/err via per-cell truecolor; reverting to teal #56b6c2 reds the gate; pytest exit zero"
    response:
      observe: exits
      object: "zero from the footer + heartbeat rotated-accent capture suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_footer_heartbeat_green_accent_capture.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
