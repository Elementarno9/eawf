<!-- eawf-template: spec-wave -->

# RS-21 reskin plan-preview + reference + audit overlays

**Scope:** P30-I02-W21

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the plan-preview frontier marker, reference cards and audit-running/audit-failed overlays render the migrated sigils + block progress in the rotated palette."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_overlays_reskin_w21 + test_overlays_audit_running assert the plan-preview dispatch frontier marker, reference-card chassis, audit-running block-progress + ticking running/closed/fail sigils, and audit-failed failing-check + evidence render the migrated sigils; pytest exit zero"
    response:
      observe: exits
      object: "zero from the plan-preview + reference + audit overlay reskin suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_overlays_reskin_w21.py", "tests/tui/test_overlays_audit_running.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
