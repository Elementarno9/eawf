<!-- eawf-template: spec-wave -->

# Route verdict sigils through status_sigil + pin two-tone CLI banner

**Scope:** P30-I02-W34

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The Evidence-mode verdict column resolves through the extended status_sigil home: a blocked verdict wears the warn-tinted withheld mark (not a pending ring), pass-with-followups trails its badge, and the unknown-verdict fallback stays the muted pending ring; the stale five-member Sigil comment reads six-member; the CLI bare-invocation banner assert pins the exact two-tone E-accent-umlaut byte run."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_evidence_rollup.py asserts blocked = status_sigil(BLOCKED) render with warn tint, followups badge trails, unknown falls back to pending ring; test_cli_dispatch.py pins startswith E+accent_sgr+umlaut; pytest exit zero"
    response:
      observe: exits
      object: "zero from the evidence-rollup + cli-dispatch suites"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_evidence_rollup.py", "tests/tui/test_modes_evidence.py", "tests/integration/test_cli_dispatch.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
