<!-- eawf-template: spec-wave -->

# RS-17 reskin agent watch to running sigil + cancel look

**Scope:** P30-I02-W17

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts agent watch renders the running sigil + the cancel look and the no-active-session honest-empty sentinel; affordance_parity confirms the cancel key resolves to a live Binding and fires."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_agent_watch_reskin asserts the running-sigil header + failed-x cancel look + the no-active-session honest-empty sentinel, and affordance_parity that the cancel key (k) resolves to a live Binding and fires; pytest exit zero"
    response:
      observe: exits
      object: "zero from the agent-watch running-sigil + cancel-look + affordance-parity suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_agent_watch_reskin.py", "tests/tui/test_modes_agent_watch.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
