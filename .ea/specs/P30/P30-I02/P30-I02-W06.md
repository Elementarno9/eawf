<!-- eawf-template: spec-wave -->

# RS-06 reskin roadmap tree to sigil prefix + inline bar

**Scope:** P30-I02-W06

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot/Pilot test mounts a fixture roadmap with pending/claimed/running/closed/failed waves and asserts each row renders its lifecycle sigil and a closed iter renders a full block bar with the n/m ratio; the closed-iter-full-bar regression asserts a 55/55 iter renders ten full cells and a fully-empty roadmap renders EMPTY_STATE, not a fabricated bar."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_widgets_roadmap_tree mounts pending/claimed/running/closed/failed waves and asserts each row's lifecycle sigil; a 55/55 closed iter renders ten full block cells + the n/m ratio (no remainder) and a fully-empty roadmap renders EMPTY_STATE not a fabricated bar; pytest exit zero"
    response:
      observe: exits
      object: "zero from the roadmap-tree sigil + completion-bar + empty-state suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_widgets_roadmap_tree.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
