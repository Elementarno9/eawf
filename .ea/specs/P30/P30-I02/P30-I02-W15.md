<!-- eawf-template: spec-wave -->

# RS-15 reskin evidence to wave-keyed table + verdict sigil

**Scope:** P30-I02-W15

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the evidence mode renders a wave-keyed table with report, verdict sigil and EU columns, and that the no-reports path renders the honest-empty sentinel."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_evidence_rollup asserts the wave-keyed report/verdict-sigil/EU table (verdict mapped to lifecycle shapes via the helper) and the no-reports path frames the honest-empty 'no agent reports yet' sentinel with no fabricated rows; pytest exit zero"
    response:
      observe: exits
      object: "zero from the evidence wave-keyed table + honest-empty suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_evidence_rollup.py", "tests/tui/test_modes_evidence.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
