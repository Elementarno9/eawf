<!-- eawf-template: spec-wave -->

# RS-18 reskin command palette to grouped verbs + stage sigil

**Scope:** P30-I02-W18

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the command palette groups Recent/Lifecycle/All with lifecycle verbs carrying the stage sigil and pins the frozen literal no verb matches Esc to cancel (em-dash) byte-for-byte."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_command_palette_sections asserts the Recent/Lifecycle/All grouping with lifecycle verbs wearing the stage sigil and pins the frozen em-dash no-match literal byte-for-byte; pytest exit zero"
    response:
      observe: exits
      object: "zero from the command-palette grouped-verbs + frozen-literal suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_command_palette_sections.py", "tests/tui/test_palette_command.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
