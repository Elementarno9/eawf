<!-- eawf-template: spec-wave -->

# RS-30 narrow-terminal + resize acceptance for HOME surfaces

**Scope:** P30-I02-W30

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Narrow-width snapshot goldens over the roadmap tree, status pane, and autopilot pane render without truncating a sigil, wave id, or n/m count; an on_resize pass reflows rather than clips; and every HOME affordance the wide layout exposes stays reachable at the narrow width, proven by an affordance-parity check across the wide and narrow renders of each HOME pane."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_home_narrow.py asserts narrow goldens with no truncated sigil / wave id / n-of-m count, on_resize reflow over clipping, and wide-vs-narrow affordance parity per HOME pane; pytest exit zero"
    response:
      observe: exits
      object: "zero from the narrow-terminal HOME acceptance suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_home_narrow.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
