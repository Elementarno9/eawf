<!-- eawf-template: spec-wave -->

# RS-32 reskin the offline daemon-down brand frame to match the header

**Scope:** P30-I02-W32

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The offline daemon-down brand frame renders the two-tone green wordmark identical to the reskinned in-app header, proven by committed text goldens plus a token-parity assert that the offline frame and brand.py emit the same accent SGR tokens; the old teal frame fails the parity assert."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_offline_brand.py asserts offline-frame / brand.py accent-token parity, absence of the old teal SGR, and byte-stable text goldens for the status + dashboard frames; pytest exit zero"
    response:
      observe: exits
      object: "zero from the offline brand-frame parity + golden suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_offline_brand.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
