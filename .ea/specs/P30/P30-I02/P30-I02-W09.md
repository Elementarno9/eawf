<!-- eawf-template: spec-wave -->

# RS-09 tint git pane + backlog priority, sigil-prefixed

**Scope:** P30-I02-W09

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the backlog rows render priority-tinted with a leading sigil and the git pane renders the added/removed/changed tint through the rotated vars."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_widgets_backlog_table asserts priority-tinted rows (P0 err -> P2 muted) with a leading sigil; test_widgets_git_pane asserts the added/removed/changed counts tint through the rotated vars via per-cell truecolor; pytest exit zero"
    response:
      observe: exits
      object: "zero from the backlog-table + git-pane tint capture suites"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_widgets_git_pane.py", "tests/tui/test_widgets_backlog_table.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
