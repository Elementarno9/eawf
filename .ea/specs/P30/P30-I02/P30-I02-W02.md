<!-- eawf-template: spec-wave -->

# RS-02 add widgets/sigils.py lifecycle + chrome helper

**Scope:** P30-I02-W02

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A unit test asserts glyph(CLOSED, mode=unicode) is the closed sigil and the ascii column for mode=ascii across all five states + the chrome roles, that tint(CLOSED) returns the Wong closed green via status_colour, that a non-TTY harness resolves the ascii column, and a regression test proves the ascii alphabet shares no character with the bar glyphs (deconfliction invariant)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_widgets_sigils asserts glyph over 5 lifecycle states + 8 chrome roles in both columns, tint(CLOSED)==#009e73 via status_colour, a non-TTY harness resolves ascii, and the ascii sigil alphabet shares no char with the eu_bar glyphs (deconfliction); pytest exit zero"
    response:
      observe: exits
      object: "zero from the sigils helper unit suite (glyph/chrome/tint + deconfliction invariant)"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_widgets_sigils.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
