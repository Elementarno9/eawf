<!-- eawf-template: spec-wave -->

# RS-29 commit the canonical Ea Seal SVG brand asset

**Scope:** P30-I02-W29

```eawf-wave-body
criteria:
  - id: CR-01
    text: "assets/ea-seal.svg exists, parses as valid SVG, and carries the evenodd-knockout disc path (the Seal mark); render/brand.py exposes a SEAL_ASSET_PATH so a consumer resolves the asset without a hardcoded literal."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_ea_seal_asset asserts SEAL_ASSET_PATH resolves to a real ea-seal.svg, the file parses as valid SVG via ElementTree with root tag svg, and a path element carries fill-rule=evenodd (the knockout disc); green under pytest exit zero"
    response:
      observe: exits
      object: "zero from the Ea Seal asset unit test"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_ea_seal_asset.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
