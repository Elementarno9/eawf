<!-- eawf-template: spec-wave -->

# RS-01 rotate accent green across theme + status_tint + brand

**Scope:** P30-I02-W01

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Per-theme snapshot goldens (dark/cb/light) assert the header brand and a closed-wave row render the green accent and that /theme swaps as a pure var rebind with no recolour gap; a brand.py unit test asserts the rendered wordmark carries green on the ae and the ASCII channel stays plain Ea; the Wong/IBM status-* tints are byte-identical to the prior baseline."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_theme_green_accent_capture pins the per-theme green accent on the header brand + green status-closed row + /theme pure-rebind; test_brand_glyph_fallback asserts #16b384 on the umlaut and ascii_wordmark plain Ea; test_status_tint_baseline pins tints byte-identical; pytest exit zero"
    response:
      observe: exits
      object: "zero from the per-theme green-accent capture, brand wordmark, status-tint baseline, and palette-theme suites"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The colourblind theme's status-* band hexes are proven pairwise hue-distinct, not just byte-stable: an SVG export of the status-tint legend pins an svg_pixel_diff golden and a unit asserts every EA_CB band hex is pairwise-distinct under a CVD (deuteranopia/protanopia) simulation, so a future palette edit that collapses two bands reds the gate."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_cvd_distinctness asserts every EA_CB status-band pair stays above the collapse epsilon under Vienot-1999 deuteranopia + protanopia; test_status_legend_golden pins the legend SVG via the svg_pixel_diff resvg byte-equality golden; pytest exit zero"
    response:
      observe: exits
      object: "zero from the CVD pairwise-distinctness unit and the status-tint legend svg_pixel_diff golden"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_theme_green_accent_capture.py", "tests/unit/test_brand_glyph_fallback.py", "tests/tui/test_status_tint_baseline.py", "tests/tui/test_palette_theme.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_cvd_distinctness.py", "tests/snapshots/svg/test_status_legend_golden.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
