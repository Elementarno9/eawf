<!-- eawf-template: spec-wave -->

# RS-19 reskin needs_user inbox to pause cards + calm empty

**Scope:** P30-I02-W19

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A Pilot test seeds two open pauses and asserts a card per pause carries the attention sigil + the wave name, the empty inbox renders the literal calm empty-inbox copy (not a bordered void), and affordance_parity confirms the open/dismiss/close keys fire; an empty inbox issues no fabricated pause row."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_overlays_needs_user_reskin_w19 seeds two pauses and asserts a card per pause with the attention sigil + wave name, the empty inbox renders the literal calm copy with no fabricated row, and affordance_parity that open/dismiss/close keys fire; pytest exit zero"
    response:
      observe: exits
      object: "zero from the needs_user inbox pause-card + calm-empty + affordance suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_overlays_needs_user_reskin_w19.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
