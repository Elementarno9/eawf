<!-- eawf-template: spec-wave -->

# RS-25 reskin the global degraded / stale-schema banner

**Scope:** P30-I02-W25

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A non-normalised Pilot test mounts the app with the daemon unreachable and asserts the degraded banner is mounted, leads with the fail sigil and the calm reconnecting copy, and toggles visibility instead of unmounting; the stale-schema banner message leads with the warn sigil and names both schema versions; a healthy app mounts neither banner."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "the banner-scoped tests in test_app_scaffold.py and test_state_binding.py (degraded fail-sigil calm copy, mounted-toggle, socket diagnostics, stale-schema warn sigil naming both versions, healthy mounts neither) pass; pytest exit zero"
    response:
      observe: exits
      object: "zero from the banner-scoped scaffold + state-binding suites"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_app_scaffold.py", "tests/snapshots/tui/test_state_binding.py", "-k", "banner and not first_paint_renders_brand and not stale_schema_banner_shows_when_bound_trails_live", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
