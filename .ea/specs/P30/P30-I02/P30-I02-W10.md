<!-- eawf-template: spec-wave -->

# RS-10 reuse the block bar in variance tile + progress

**Scope:** P30-I02-W10

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the variance tile and progress widget render the unified block cell-bar and that the no-data path renders the EMPTY_STATE sentinel, not a fabricated bar."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_progress_widget + test_variance_tile_widget assert both widgets render the unified eu_bar block cell-bar and that the no-data path renders EMPTY_STATE with no block glyph (no fabricated bar); render-mode flip honoured; pytest exit zero"
    response:
      observe: exits
      object: "zero from the progress + variance unified-bar suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_progress_widget.py", "tests/snapshots/tui/test_variance_tile_widget.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
