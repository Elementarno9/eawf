<!-- eawf-template: spec-wave -->

# RS-13 reskin trust to sigil scorecard + honest-negative

**Scope:** P30-I02-W13

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the trust scorecard renders sigil rows where the mock shows them, keeps the number-based metric rows with dashes + sample counts, places the overridden marker only on the advisory-authority row, and pins the literal starved / no-data copy rather than a fabricated trust number."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_trust_jury_authority asserts the scorecard renders sigil rows, number-based metric rows with dashes + sample counts, the overridden marker only on the authority row, and pins the literal starved/no-data copy with no fabricated trust number; pytest exit zero"
    response:
      observe: exits
      object: "zero from the trust JURY AUTHORITY scorecard snapshot suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_trust_jury_authority.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
