<!-- eawf-template: spec-wave -->

# RS-08 reskin attention feed to idle vs needs-you hero

**Scope:** P30-I02-W08

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A Pilot test with zero acute items asserts the calm idle render pinning the literal nothing needs you plus the next-action sub-line; a test with one HIGH pause and one open incident asserts the warn band names each with its sigil and shows Enter to review, the footer badge agrees, Enter over an acute row fires its action, and an idle feed never renders a warn band."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_attention_feed_hero: zero-acute pins the calm nothing-needs-you literal + next-action sub-line and no warn band; one HIGH pause + one incident name each with its sigil + Enter to review, the footer badge agrees, and Enter fires the row action; pytest exit zero"
    response:
      observe: exits
      object: "zero from the two-state attention-feed hero Pilot suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_attention_feed_hero.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
