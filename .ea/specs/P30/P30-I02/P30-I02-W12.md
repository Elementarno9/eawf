<!-- eawf-template: spec-wave -->

# RS-12 reskin research board to lifecycle layout + sigil claims

**Scope:** P30-I02-W12

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the research board renders the lifecycle layout with a sigil per claim (hardcoded glyphs migrated to the sigils helper) and pins the literal empty-state headline plus the press-n sub-line copy from the mock."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_research_board_reskin asserts the lifecycle layout with a sigil per claim (glyphs migrated to the sigils helper, no .value word, no ? fallthrough) and pins the literal empty-state headline + press-n sub-line copy from the mock; pytest exit zero"
    response:
      observe: exits
      object: "zero from the research-board lifecycle-layout + sigil-claim + empty-state suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/snapshots/tui/test_research_board_reskin.py", "tests/tui/test_modes_research_board.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
