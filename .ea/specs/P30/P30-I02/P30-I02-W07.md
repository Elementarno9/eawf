<!-- eawf-template: spec-wave -->

# RS-07 reskin status pane to collapse all-absent + sigil rows

**Scope:** P30-I02-W07

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A snapshot golden asserts the all-absent state collapses to the single dim awaiting-first-wave line, that any present metric expands to per-metric rows with a selectively-absent metric showing its own dash, and that live rows render a leading sigil."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_status_pane_reskin + test_widgets_status_pane assert the all-absent collapse to one dim awaiting-first-wave line, expansion to per-metric rows with a selectively-absent metric keeping its own dash, and a leading sigil on live rows; pytest exit zero"
    response:
      observe: exits
      object: "zero from the status-pane collapse/expand + sigil-row suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_widgets_status_pane.py", "tests/snapshots/tui/test_status_pane_reskin.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
