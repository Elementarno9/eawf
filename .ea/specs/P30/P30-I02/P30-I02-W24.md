<!-- eawf-template: spec-wave -->

# RS-24 restructure detail overlay into the 5-tab chassis

**Scope:** P30-I02-W24

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A Pilot test opens the detail modal on a fixture wave and asserts the five tab ids are present with their chrome-glyph labels, o/c/g/v/r jump to each tab and Tab cycles; a wave with no gates renders no gates tab and the runtime tab on a no-runtime wave shows the empty sentinel, not a fabricated 0.00/0.00."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test_overlays_detail asserts the five tab ids (overview/criteria/gates/evidence/runtime) with chrome-glyph labels, o/c/g/v/r jumps + Tab cycle, no-gates wave renders no gates tab, and the runtime tab shows EMPTY_STATE not a fabricated 0.00/0.00; pytest exit zero"
    response:
      observe: exits
      object: "zero from the detail 5-tab chassis Pilot suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_overlays_detail.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
