<!-- eawf-template: spec-wave -->

# AUTH-1 require IntentBrief at add-wave authoring

**Scope:** P30-I04-W01

```eawf-wave-body
criteria:
  - id: CR-01
    text: "plan_wave raises LifecycleError with an intent-naming message when called with intent=None, and a populated IntentBrief lands a wave with wave.intent not None; the add_wave daemon op rejects a mutation with a missing or explicit-None intent param at the apply boundary (LifecycleError before plan_wave is reached) rather than passing None through."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_wave_dag.py asserts the None-intent raise message and the populated-intent landing; test_intent_brief.py asserts the add_wave op rejects missing and explicit-None intent params and passes a populated intent dict through; pytest exit zero"
    response:
      observe: exits
      object: "zero from the plan_wave guard + daemon add_wave op suites"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_wave_dag.py", "tests/unit/test_intent_brief.py", "tests/workflow/lifecycle/", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
