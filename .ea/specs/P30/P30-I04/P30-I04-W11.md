<!-- eawf-template: spec-wave -->

# Repair synced-wave intent drift

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I04/P30-I04-W11

**Status:** DRAFT

**Scope:** P30-I04-W11

```eawf-wave-body
criteria:
  - id: CR-01
    text: "pytest tests/unit/state/test_wave_intent_backfill.py tests/integration/test_cli_backfill_wave_intents.py exits zero and proves the guarded wave-intent backfill dry-run and apply paths."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the backfill test pair asserts dry-run rows, apply mutation, explicit wave targeting, and no broad closed-wave mutation"
    response:
      observe: exits
      object: "zero from the wave-intent backfill unit and CLI integration tests"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "pytest tests/integration/test_spec_sync.py::test_sync_rejects_wave_with_no_intent exits zero and proves the current spec.sync missing-intent regression still rejects."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "test_sync_rejects_wave_with_no_intent asserts EAWF022 no intent and no state write"
    response:
      observe: exits
      object: "zero from the missing-intent spec.sync regression"
      locus: pytest
    gate_ids: [G-02]
  - id: CR-03
    text: "pytest tests/integration/test_cli_backfill_wave_intents.py::test_wave_intents_check_exits_zero_after_apply exits zero and proves the wave-intent check passes after repair is applied."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: operability
    measurable_signal: "test_wave_intents_check_exits_zero_after_apply asserts --check exits zero after --apply fills the target wave intent"
    response:
      observe: exits
      object: "zero from the wave-intent backfill check regression"
      locus: pytest
    gate_ids: [G-03]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/state/test_wave_intent_backfill.py", "tests/integration/test_cli_backfill_wave_intents.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/integration/test_spec_sync.py::test_sync_rejects_wave_with_no_intent", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-03
    criterion_id: CR-03
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/integration/test_cli_backfill_wave_intents.py::test_wave_intents_check_exits_zero_after_apply", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
