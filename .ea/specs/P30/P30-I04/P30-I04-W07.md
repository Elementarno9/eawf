<!-- eawf-template: spec-wave -->

# AUTH-7 capture operator-picked mockup as approved ASCII golden

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I04/P30-I04-W07

**Status:** DRAFT

**Scope:** P30-I04-W07

```eawf-wave-body
criteria:
  - id: CR-01
    text: "pytest tests/unit/test_mockup_golden_capture.py exits zero and asserts the /mockup pick handler writes normalize_snapshot content plus mockup_golden_path."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_mockup_golden_capture.py asserts normalized golden bytes and stamped mockup_golden_path; pytest exit zero"
    response:
      observe: exits
      object: "zero from the mockup golden capture unit suite"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "pytest tests/unit/test_spec_validators.py exits zero and asserts a UI wave with neither mockup golden nor mockup_waiver_reason is rejected."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "test_spec_validators.py asserts UI wave rejection without mockup_golden_path or mockup_waiver_reason; pytest exit zero"
    response:
      observe: exits
      object: "zero from the UI mockup-reference validator unit suite"
      locus: pytest
    gate_ids: [G-02]
  - id: CR-03
    text: "affordance_parity validates the HOME mode advertised keys so the UI-scope wave has a structural T2 gate before jury fallback."
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "affordance_parity HOME mode probe finds no dead advertised key before close"
    response:
      observe: validates
      object: "HOME mode advertised key bindings"
      locus: tui_pilot
      gate_ref: affordance_parity
    gate_ids: [G-03]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_mockup_golden_capture.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_spec_validators.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-03
    criterion_id: CR-03
    kind: affordance_parity
    args: {mode: home}
    policy: block
    cadence: every-wave
```
