<!-- eawf-template: spec-wave -->

# AUTH-10 idle-contract meta-gate proof

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I04/P30-I04-W10

**Status:** DRAFT

**Scope:** P30-I04-W10

```eawf-wave-body
criteria:
  - id: CR-01
    text: "pytest tests/unit/test_idle_contract_gate.py::test_cli_returns_zero_on_pass exits zero and proves the idle-contract gate main path passes with the I03 required-intent, UI affordance-parity, and mockup_golden_diff probes wired."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_cli_returns_zero_on_pass asserts tools/idle_contract_gate.py main returns zero on the current tree"
    response:
      observe: exits
      object: "zero from the shipped idle-contract gate CLI-wrapper regression"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "pytest tests/unit/test_idle_contract_gate.py::test_main_returns_nonzero_when_required_intent_guard_idle exits zero and proves monkeypatching the required-intent guard to a no-op makes the gate fail while naming the idled contract."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "test_main_returns_nonzero_when_required_intent_guard_idle asserts main returns non-zero and stderr includes required-intent guard is idle"
    response:
      observe: exits
      object: "zero from the required-intent idle-regression test"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_idle_contract_gate.py::test_cli_returns_zero_on_pass", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_idle_contract_gate.py::test_main_returns_nonzero_when_required_intent_guard_idle", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
