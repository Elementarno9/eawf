<!-- eawf-template: spec-wave -->

# Add an evidence-report open affordance to Evidence mode

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I22/P30-I22-W10

**Status:** DRAFT

**Scope:** P30-I22-W10

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Enter on a highlighted agent-report row in Evidence mode opens a report-detail modal rendering the report verdict, summary, evidence_refs, followups, and attempt provenance; the action is a safe no-op (no crash) when the table is empty or no row is highlighted."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "a Pilot case drives Enter on a seeded report row and asserts the modal mounts with verdict and summary text, plus an empty-table no-op case, under pytest exit zero"
    response:
      observe: triggers_action
      object: "the report-detail modal push from Enter on the highlighted evidence-mode report row"
      locus: tui_pilot
    gate_ids: [G-01]
  - id: CR-02
    text: "The Evidence-mode footer hint strip advertises the new affordance (Enter open) alongside the retained p peek hint, and the p criterion-drill binding keeps working unchanged."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "footer-hint assertions include Enter open and p peek, and the existing drill test still passes, under pytest exit zero"
    response:
      observe: renders_token
      object: "the Enter open hint token in the evidence-mode footer strip"
      locus: tui_snapshot
    gate_ids: [G-01, G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_modes_evidence.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: affordance_parity
    args:
      mode: evidence
    policy: block
    cadence: every-wave
```
