<!-- eawf-template: spec-wave -->

# Render compact UTC timestamps across TUI detail surfaces

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I22/P30-I22-W09

**Status:** DRAFT

**Scope:** P30-I22-W09

```eawf-wave-body
criteria:
  - id: CR-01
    text: "One shared compact-UTC datetime formatter renders YYYY-MM-DD HH:MM:SS (converted to UTC, no microseconds, no +00:00 offset) and is wired at both offenders: _fmt_dt in surfaces/tui/screens/overlays/detail.py and _attempt_dt in workflow/agent_report/rollup.py; None still renders the unset sentinel."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "formatter tests assert the compact form for an aware UTC datetime, a non-UTC aware datetime, and None, and detail/rollup cases show no microseconds or offset, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the overlays-detail and attempt-rollup suites asserting the compact UTC form at both render sites"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "A sweep of surfaces/tui render paths finds no remaining full-isoformat datetime render (microseconds or offset visible to the operator); each remaining isoformat call site is either converted or recorded in the report as non-render (persistence/serialization)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the wave report enumerates every surfaces/tui isoformat call site with converted or non-render disposition and the targeted suites pass"
    response:
      observe: exits
      object: "zero from the touched TUI suites after the sweep dispositions land"
      locus: pytest
    gate_ids: [G-01, G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_overlays_detail.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: affordance_parity
    args:
      mode: home
    policy: block
    cadence: every-wave
```
