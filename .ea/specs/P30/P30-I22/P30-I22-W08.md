<!-- eawf-template: spec-wave -->

# Restyle evidence-tab attempt tables and humanize modal token counts

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I22/P30-I22-W08

**Status:** DRAFT

**Scope:** P30-I22-W08

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The wave-modal evidence tab renders every token figure through the W05 units humanizer: the attempt summary line and the timeline tokens column show humanized counts (e.g. 352.1k), never raw ints, wired in detail_attempts.py at _attempt_summary and _attempt_timeline_table."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "evidence-tab test cases assert the humanized token form appears and the raw 6-digit int form does not, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the overlays-detail evidence-tab suite asserting humanized token counts in the attempt summary and timeline column"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The attempt timeline renders with the cost-tab table treatment (styled aligned columns that fit the modal width without mid-row wrapping), and the remaining detail tabs are swept for other raw-table or raw-token renders with each hit converted or recorded as none-found in the report."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "the evidence-tab timeline snapshot/unit case asserts the restyled table shape at modal width with no wrapped row, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the overlays-detail suite covering the restyled attempt-timeline table shape"
      locus: pytest
    gate_ids: [G-01, G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_overlays_detail.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: affordance_parity
    args:
      mode: home
    policy: block
    cadence: every-wave
```
