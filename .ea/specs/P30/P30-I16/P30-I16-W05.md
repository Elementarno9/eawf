<!-- eawf-template: spec-wave -->

# needs_user a/h/x affordances and agent-watch x-cancel alias

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W05

**Status:** DRAFT

**Scope:** P30-I16-W05

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The needs_user inbox binds a (approve), h (hold), x (dismiss) per card and the actions resolve through the existing pause resolution path; agent_watch binds x as a cancel alias to the existing confirm-gated kill."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "Pilot flow test drives a/h/x on an inbox card asserting the resolved PauseResolution row after each action, and x on a running lane; key-hint footer goldens show the new chords"
    response:
      observe: triggers_action
      object: "Pilot flow test drives a/h/x on an inbox card asserting the resolved PauseResolution row after each action, and x on a running lane; key-hint footer goldens show the new chords"
      locus: tui_pilot
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_p30_i16_w05.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: agent_watch
    policy: block
    cadence: every-wave
```
