<!-- eawf-template: spec-wave -->

# Bypass-door bundle and daemonless close-with-waiver

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W18

**Status:** DRAFT

**Scope:** P30-I16-W18

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A gate-bearing wave close under EAWF_DAEMONLESS without the waiver flag is rejected; with the flag it succeeds and appends a waiver event naming the wave and reason; every wave-close event carries the close-mechanism field."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: security
    measurable_signal: "integration test drives both paths; event.jsonl rows carry close_mechanism and the waiver event"
    response:
      observe: exits
      object: "integration test drives both paths; event.jsonl rows carry close_mechanism and the waiver event"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The band resolver no longer narrows enforce for fleet high-risk waves after band merge (ruling recorded as a Decision), and the spec-jury token arm matches on word boundaries so a substring token cannot arm it."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: security
    measurable_signal: "readiness resolver test covers the band-merge case; word boundary negative test passes at spec_jury arm predicate"
    response:
      observe: exits
      object: "readiness resolver test covers the band-merge case; word boundary negative test passes at spec_jury arm predicate"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i16_w18.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i16_w18.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
