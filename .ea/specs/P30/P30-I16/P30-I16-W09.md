<!-- eawf-template: spec-wave -->

# INIT-2 implement the stepped live-executing init wizard

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W09

**Status:** DRAFT

**Scope:** P30-I16-W09

```eawf-wave-body
criteria:
  - id: CR-01
    text: "From a fresh directory the wizard executes a real repo init end-to-end inside the TUI (no shell round-trip): identity step validates the project code live, profile chips toggle, preview shows the file tree and exact CLI-equivalent line, execute streams substep rows, the success card lists created artifacts and next-action chips."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "Pilot flow test inits a tmp repo through the wizard and asserts .ea/state.json exists after the done card; journey-state goldens match the ratified pin-strip"
    response:
      observe: triggers_action
      object: "Pilot flow test inits a tmp repo through the wizard and asserts .ea/state.json exists after the done card; journey-state goldens match the ratified pin-strip"
      locus: tui_pilot
    gate_ids: [G-01]
  - id: CR-02
    text: "Workspace bootstrap journey creates a workspace state and links multi-selected registry repos in one pass with per-repo validate sigils; failure on any step renders the honest error card with stderr tail and a retry affordance, never a crash."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "Pilot test bootstraps a 2-repo workspace; error-path test injects a failing step and asserts the error card + retry"
    response:
      observe: triggers_action
      object: "Pilot test bootstraps a 2-repo workspace; error-path test injects a failing step and asserts the error card + retry"
      locus: tui_pilot
    gate_ids: [G-02]
  - id: CR-03
    text: "The J1 first-run journey is pinned: on a fresh user scope with init_needed the wizard auto-opens, renders the seal/wordmark hero with the three entry paths, and never auto-mutates; the execute step offers Esc cancel-confirm; the done card runs the doctor mini-probe; every journey ships its 80-column narrow-variant golden against the ratified pin-strip."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "J1 Pilot flow + goldens; never-auto-mutates negative test; cancel-confirm Pilot step; narrow-variant goldens for all four journeys"
    response:
      observe: triggers_action
      object: "J1 Pilot flow + goldens; never-auto-mutates negative test; cancel-confirm Pilot step; narrow-variant goldens for all four journeys"
      locus: tui_pilot
    gate_ids: [G-03]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_p30_i16_w09.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_p30_i16_w09.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-03
    criterion_id: CR-03
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_p30_i16_w09.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-04
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: home
    policy: block
    cadence: every-wave
```
