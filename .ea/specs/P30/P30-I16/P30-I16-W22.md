<!-- eawf-template: spec-wave -->

# Doctor green-path and operational hygiene

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W22

**Status:** DRAFT

**Scope:** P30-I16-W22

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Plain eawf doctor (no -w) on this repo reports overall ok: the anchor resolves pwd-upward, plugin-owned manifest regions are satisfied by the plugin cache without local renders, and the 12 historical git-state drifts are acknowledged via the new verb and stop warning."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "regression test pins the plugin-mode manifest resolution and the pwd-upward anchor; the repo run showing overall ok is recorded as attested evidence at close"
    response:
      observe: exits
      object: "regression test pins the plugin-mode manifest resolution and the pwd-upward anchor; the repo run showing overall ok is recorded as attested evidence at close"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The release workflow gains an install-smoke job (uv tool install from the built wheel + eawf --help + eawf init in a tmp dir) that gates publish; eawf -w probing no longer writes instrument-probe.json into arbitrary anchor directories."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "workflow YAML carries the smoke job before publish; write-path tests assert none of the three stray classes lands outside the repo .ea"
    response:
      observe: exits
      object: "workflow YAML carries the smoke job before publish; write-path tests assert none of the three stray classes lands outside the repo .ea"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i16_w22.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i16_w22.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
