<!-- eawf-template: spec-wave -->

# SEAL-1 default-install seal deps and doctor capability row

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W12

**Status:** DRAFT

**Scope:** P30-I16-W12

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A default install (no extras) imports textual_image and seal_capable() returns True when resvg is on PATH; an EAWF_SEAL_DISABLE kill switch forces seal_capable() False and the snapshot-test harness sets it autouse so CI goldens never take the image path; eawf doctor renders a seal-capability row naming which precondition is missing when not capable."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: compatibility
    measurable_signal: "local install-smoke script (re-run by the W22 CI job) imports textual_image; autouse fixture sets EAWF_SEAL_DISABLE in tests and goldens are unchanged on the W12 diff; doctor golden shows the capability row in capable and degraded fixtures"
    response:
      observe: file_matches
      object: "local install-smoke script (re-run by the W22 CI job) imports textual_image; autouse fixture sets EAWF_SEAL_DISABLE in tests and goldens are unchanged on the W12 diff; doctor golden shows the…"
      locus: golden
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i16_w12.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: doctor
    policy: block
    cadence: every-wave
```
