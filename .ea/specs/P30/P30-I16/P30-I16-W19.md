<!-- eawf-template: spec-wave -->

# EUCAP harness and model attribution

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W19

**Status:** DRAFT

**Scope:** P30-I16-W19

```eawf-wave-body
criteria:
  - id: CR-01
    text: "ActualSummary and RuntimeCounters carry harness and model fields; the capture path writes them on the next recorded actual; the schema bump migrates existing rows with null attribution rather than failing validation."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "capture integration test asserts attribution on a new actual; migration test loads a pre-bump state.json clean"
    response:
      observe: exits
      object: "capture integration test asserts attribution on a new actual; migration test loads a pre-bump state.json clean"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i16_w19.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
