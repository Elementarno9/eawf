<!-- eawf-template: spec-wave -->

# SEAL-2 fix black seal render; spread seal to header and heroes

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W13

**Status:** DRAFT

**Scope:** P30-I16-W13

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The rasterized seal PNG carries the theme accent color, never black: the rasterize path substitutes currentColor with the resolved accent hex before invoking resvg; the cache keys on (px, accent hex) and overwrites stale PNGs so a pre-existing black PNG cannot survive the fix; the tmp path is per-user; a unit test asserts the output PNG's non-transparent pixels match the accent within tolerance."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "test samples the cached PNG pixels: zero pure-black opaque pixels and dominant hue equals the accent hex; cache-key test: changing the accent produces a new PNG"
    response:
      observe: exits
      object: "test samples the cached PNG pixels: zero pure-black opaque pixels and dominant hue equals the accent hex; cache-key test: changing the accent produces a new PNG"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The hero seal renders horizontally centered above the empty text; the steady empty-tick update preserves with_sigil=False so no glyph is re-added beside the image (the I15-W20 regression); the observed board lag is root-caused and the fix noted in the wave close body; rebuild latency with the seal mounted stays within 1.1x the measured no-seal baseline per tick."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: performance_efficiency
    measurable_signal: "Pilot test asserts no brand glyph inside the empty Static while the image is mounted across two refresh ticks; instrumented rebuild timing at or under 1.1x baseline; operator screenshot review confirms centered placement on Ghostty"
    response:
      observe: triggers_action
      object: "Pilot test asserts no brand glyph inside the empty Static while the image is mounted across two refresh ticks; instrumented rebuild timing at or under 1.1x baseline; operator screenshot review…"
      locus: tui_pilot
    gate_ids: [G-02]
  - id: CR-03
    text: "When capable, the header renders the 1-cell seal image in place of the brand glyph and the home/offline surfaces render their seal or glyph marks: the offline frame GAINS the header brand glyph (UX-19, golden changes accordingly); on a non-graphics terminal every site falls back to the glyph and only surfaces already at parity stay byte-identical under the test kill switch."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: attested
    quality_dimension: interaction_capability
    measurable_signal: "operator screenshot review on Ghostty shows header + hero seals; offline golden gains the brand glyph; kill-switch goldens for already-parity surfaces byte-identical"
    response:
      observe: validates
      object: "operator screenshot review on Ghostty shows header + hero seals; offline golden gains the brand glyph; kill-switch goldens for already-parity surfaces byte-identical"
      locus: human
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_p30_i16_w13.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_p30_i16_w13.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-03
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: research_board
    policy: block
    cadence: every-wave
```
