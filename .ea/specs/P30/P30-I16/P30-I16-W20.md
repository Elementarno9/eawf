<!-- eawf-template: spec-wave -->

# Build-Report pre-registration

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W20

**Status:** DRAFT

**Scope:** P30-I16-W20

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A typed EvalReport schema validates the build-report metric set and the recount tool reproduces each metric from repo data alone, exiting nonzero on any unreproducible figure."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "uv run tools/recount_build_report.py exits 0 and emits the validated report JSON; a corrupted-figure fixture exits nonzero"
    response:
      observe: exits
      object: "uv run tools/recount_build_report.py exits 0 and emits the validated report JSON; a corrupted-figure fixture exits nonzero"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i16_w20.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
