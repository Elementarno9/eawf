<!-- eawf-template: spec-wave -->

# VIS-1 image-diff verification gate over mockup vs TUI renders

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W14

**Status:** DRAFT

**Scope:** P30-I16-W14

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The mockup_golden_diff kind runs end-to-end on a fixture pair and fails on a known layout divergence (square-vs-round border, one-vs-two column) while passing on a faithful pair; the rubric weights layout shape above token fidelity; reference mockup frames are committed as scrub-gated pre-rendered PNGs under tests fixtures (the gitignored brand corpus is not CI-readable) and the gate spec pins the CSS-to-Textual mapping table so an un-renderable claim must cite it."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "negative test with a square-border fixture fails the gate; faithful fixture passes; committed reference PNGs exist; the W15 sweep runs the kind over at least one real Textual SVG export against a reference frame"
    response:
      observe: exits
      object: "negative test with a square-border fixture fails the gate; faithful fixture passes; committed reference PNGs exist; the W15 sweep runs the kind over at least one real Textual SVG export against a…"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i16_w14.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
