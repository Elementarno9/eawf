<!-- eawf-template: spec-wave -->

# Route residual detail-card status rows through status_sigil

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W01

**Status:** DRAFT

**Scope:** P30-I16-W01

```eawf-wave-body
criteria:
  - id: CR-01
    text: "No detail-card render path emits a bare StrEnum .value status word: every status cell in detail.py routes through status_sigil, including iter, phase, backlog, and incident cards."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "a test asserts the shared status-sigil helper is the only status formatter in detail.py (no .value interpolation in any status renderer); regenerated detail goldens show sigil-prefixed statuses on all four card kinds"
    response:
      observe: file_matches
      object: "a test asserts the shared status-sigil helper is the only status formatter in detail.py (no .value interpolation in any status renderer); regenerated detail goldens show sigil-prefixed statuses on…"
      locus: golden
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_p30_i16_w01.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: home
    policy: block
    cadence: every-wave
```
