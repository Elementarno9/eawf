<!-- eawf-template: spec-wave -->

# A3-core journal-hole fix and journal_chain gate kind

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W17

**Status:** DRAFT

**Scope:** P30-I16-W17

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The demonstrated V1 hole closes: a tampered WAL record is detected by digest compare and the replay refuses it; the poisoned-record falsifier test passes un-skipped; the journal_chain kind validates an intact chain and fails a broken one."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "test_replay_wal_pending_record_is_poisoned passes without xfail; journal_chain negative fixture fails the gate"
    response:
      observe: exits
      object: "test_replay_wal_pending_record_is_poisoned passes without xfail; journal_chain negative fixture fails the gate"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_p30_i16_w17.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
