<!-- eawf-template: spec-wave -->

# Pin U6 sandbox golden, Enter row affordance, chrome-pin source assert

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W03

**Status:** DRAFT

**Scope:** P30-I16-W03

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A sandbox_events snapshot golden exists pinning the frozen literals; Enter opens the selected row; the chrome-role test asserts against the source _CHROME table in widgets/sigils.py so a new role fails the pin until ratified."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "tests/snapshots/tui/golden/sandbox_events*.txt exists and passes; test_widgets_sigils imports _CHROME from source; a synthetic extra role makes the pin fail in a negative test"
    response:
      observe: file_matches
      object: "tests/snapshots/tui/golden/sandbox_events*.txt exists and passes; test_widgets_sigils imports _CHROME from source; a synthetic extra role makes the pin fail in a negative test"
      locus: golden
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_p30_i16_w03.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: sandbox_events
    policy: block
    cadence: every-wave
```
