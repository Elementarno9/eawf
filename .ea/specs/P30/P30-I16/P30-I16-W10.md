<!-- eawf-template: spec-wave -->

# Batch strict-spec XS items

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W10

**Status:** DRAFT

**Scope:** P30-I16-W10

```eawf-wave-body
criteria:
  - id: CR-01
    text: "One canonical bar width is recorded and used by both the roadmap completion bar and the EU bar; the doctor header uses the middle-dot separator; the selection tint applies to focused rows; tab markers resolve through sigils.py chrome roles; the lane grid carries the sandbox column."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: interaction_capability
    measurable_signal: "regenerated goldens for roadmap, doctor, agent-watch lane grid; grep shows no local glyph literals in detail.py tab markers"
    response:
      observe: file_matches
      object: "regenerated goldens for roadmap, doctor, agent-watch lane grid; grep shows no local glyph literals in detail.py tab markers"
      locus: golden
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_p30_i16_w10.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: doctor
    policy: block
    cadence: every-wave
```
