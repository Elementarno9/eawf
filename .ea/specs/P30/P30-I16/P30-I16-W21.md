<!-- eawf-template: spec-wave -->

# UserDecisionKind enum, XS trio, juror FindingSet persistence

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I16/P30-I16-W21

**Status:** DRAFT

**Scope:** P30-I16-W21

```eawf-wave-body
criteria:
  - id: CR-01
    text: "UserDecisionKind and PauseResolution land in a single schema bump covering both pause and fleet decision shapes; the OracleTier criterion/evidence maps agree on every kind; raw juror FindingSets persist to the store on every jury fire; the P30-close re-measure (era split + evidence coverage by close mechanism) is emitted as an artifact."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "one migration in the diff; tier-map property test passes; a jury fire writes findingset rows; CHANGELOG carries 0.3.0; re-measure artifact present under the local research corpus"
    response:
      observe: exits
      object: "one migration in the diff; tier-map property test passes; a jury fire writes findingset rows; CHANGELOG carries 0.3.0; re-measure artifact present under the local research corpus"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i16_w21.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
