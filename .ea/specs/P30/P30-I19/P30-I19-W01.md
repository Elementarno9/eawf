<!-- eawf-template: spec-wave -->

# WIN-P1 import and atomic floor

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I19/P30-I19-W01

**Status:** DRAFT

**Scope:** P30-I19-W01

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Every POSIX-only os attribute behind the daemon import graph is guarded; the full POSIX suite passes unchanged (byte-identical proof); a win32-marked test asserts the guarded paths import and atomic-write on Windows semantics."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: compatibility
    measurable_signal: "full POSIX suite green on the diff; grep shows all nine fsync sites guarded by hasattr O_DIRECTORY"
    response:
      observe: exits
      object: "full POSIX suite green on the diff; grep shows all nine fsync sites guarded by hasattr O_DIRECTORY"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i19_w01.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: home
    policy: block
    cadence: every-wave
```
