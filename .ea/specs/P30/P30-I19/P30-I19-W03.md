<!-- eawf-template: spec-wave -->

# WIN-P3 pipe streaming with CancelIoEx teardown and idle reap

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I19/P30-I19-W03

**Status:** DRAFT

**Scope:** P30-I19-W03

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A TUI subscription receives a live event.push after a mutation THROUGH the binding's real scope_id (never scope_id=None in the assert — the intended filter semantics are recorded); disconnect() returns promptly by cancelling the blocked read via CancelIoEx; active_subscriptions drops after the client leaves; the listener keeps serving RPCs while a subscription streams."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "win32 tests: streaming round-trip, teardown returns under budget, reap drops the count, concurrent RPC during stream"
    response:
      observe: exits
      object: "win32 tests: streaming round-trip, teardown returns under budget, reap drops the count, concurrent RPC during stream"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_p30_i19_w03.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: home
    policy: block
    cadence: every-wave
```
