<!-- eawf-template: spec-wave -->

# Windows-only test set with per-test pipe isolation

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I19/P30-I19-W06

**Status:** DRAFT

**Scope:** P30-I19-W06

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The win32 test set covers the five PoC verification scenarios with isolated pipe names per test; the cold-start test pins the 20-second win32 readiness budget; the set is skipped cleanly on POSIX."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "pytest -m win32 (or skipif inverse) passes on a Windows runner; POSIX run shows them skipped"
    response:
      observe: exits
      object: "pytest -m win32 (or skipif inverse) passes on a Windows runner; POSIX run shows them skipped"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i19_w06.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
