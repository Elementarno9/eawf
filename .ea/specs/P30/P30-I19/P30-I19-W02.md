<!-- eawf-template: spec-wave -->

# WIN-P2 named-pipe RPC with large-frame reassembly

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I19/P30-I19-W02

**Status:** DRAFT

**Scope:** P30-I19-W02

```eawf-wave-body
criteria:
  - id: CR-01
    text: "daemon ping, status, and state.mutate round-trip over the per-user named pipe on Windows; a frame larger than the pipe buffer reassembles via the MORE_DATA loop in both pipe_client_call and PipeReader rather than truncating; the readiness probe never consumes a pipe instance."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: compatibility
    measurable_signal: "win32 tests cover RPC round-trip and a >64KB frame; a wrong-SID client is rejected (negative test); probe test asserts WaitNamedPipe (non-consuming) usage"
    response:
      observe: exits
      object: "win32 tests cover RPC round-trip and a >64KB frame; a wrong-SID client is rejected (negative test); probe test asserts WaitNamedPipe (non-consuming) usage"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_p30_i19_w02.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
