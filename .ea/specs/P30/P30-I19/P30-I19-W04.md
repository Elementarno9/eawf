<!-- eawf-template: spec-wave -->

# WIN-P4 policy, daemon run verb, console UTF-8

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I19/P30-I19-W04

**Status:** DRAFT

**Scope:** P30-I19-W04

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Mutations route through the daemon by default on Windows (composing with the I16-W18 waiver semantics); phase open stays in-process by design with a regression asserting it is never daemon-routed (gotcha 10); eawf daemon run starts foreground on win32 and serves a ping; eawf --help renders clean on a cp1251 console without UnicodeEncodeError."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: compatibility
    measurable_signal: "win32 test sets a cp1251 console encoding and asserts no UnicodeEncodeError; proxy-default test on win32"
    response:
      observe: exits
      object: "win32 test sets a cp1251 console encoding and asserts no UnicodeEncodeError; proxy-default test on win32"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i19_w04.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
