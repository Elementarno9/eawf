<!-- eawf-template: spec-wave -->

# Windows packaging, typing, and lint floor

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I19/P30-I19-W05

**Status:** DRAFT

**Scope:** P30-I19-W05

```eawf-wave-body
criteria:
  - id: CR-01
    text: "pip install eawf[windows] resolves pywin32 (install-resolve smoke on the built wheel); CancelIoEx.argtypes is set once at module load; POSIX mypy and ruff pass with the win32 modules import-guarded; committed files are LF-normalized."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: compatibility
    measurable_signal: "uv run mypy src/ and ruff check green on POSIX; extras table in pyproject carries the pin; git ls-files --eol shows lf on the touched set"
    response:
      observe: exits
      object: "uv run mypy src/ and ruff check green on POSIX; extras table in pyproject carries the pin; git ls-files --eol shows lf on the touched set"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_p30_i19_w05.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
