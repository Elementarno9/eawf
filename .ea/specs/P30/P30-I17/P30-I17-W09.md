<!-- eawf-template: spec-wave -->

# needs_user detection, fleet jury authority, non-lane kill

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I17/P30-I17-W09

**Status:** DRAFT

**Scope:** P30-I17-W09

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A live lane that pauses needs_user produces the NEEDS_USER fork; DriveParams resolves jury block authority through the same resolver as the close gate; killing a single-wave dispatched session (no fleet run) succeeds; placeholder docstrings are corrected."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "live needs_user test forks; authority resolver shared (one definition, two callers); non-lane kill test passes"
    response:
      observe: exits
      object: "live needs_user test forks; authority resolver shared (one definition, two callers); non-lane kill test passes"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_p30_i17_w09.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
