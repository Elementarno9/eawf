<!-- eawf-template: spec-wave -->

# Background-run fleet.drive off the daemon event loop

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I17/P30-I17-W01

**Status:** DRAFT

**Scope:** P30-I17-W01

```eawf-wave-body
criteria:
  - id: CR-01
    text: "fleet.drive returns a run handle in under one second while the drain continues in the background; a concurrent daemon.ping answers mid-run; the loop persists every transition through the canonical writer exactly as the synchronous form did; bus publishes from the drive thread marshal through loop.call_soon_threadsafe; a second fleet.drive while a run is DRAINING is rejected; daemon shutdown signals the drive thread and joins it cleanly."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: performance_efficiency
    measurable_signal: "two-client test arms an actively persisting drain and asserts ping latency under budget mid-run; double-arm rejection test; clean-shutdown test joins the drive thread; existing drive-loop tests stay green"
    response:
      observe: exits
      object: "two-client test arms an actively persisting drain and asserts ping latency under budget mid-run; double-arm rejection test; clean-shutdown test joins the drive thread; existing drive-loop tests stay…"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_p30_i17_w01.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
