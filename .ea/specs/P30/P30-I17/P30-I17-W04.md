<!-- eawf-template: spec-wave -->

# Dynamic per-round frontier recompute

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I17/P30-I17-W04

**Status:** DRAFT

**Scope:** P30-I17-W04

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The loop recomputes the ready frontier each round: a three -layer dep chain armed at layer one drains to empty in one run, with newly-unblocked waves claimed as their deps close."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "dep-chain integration test drains 3 layers in one armed run"
    response:
      observe: exits
      object: "dep-chain integration test drains 3 layers in one armed run"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_p30_i17_w04.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
