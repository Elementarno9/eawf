<!-- eawf-template: spec-wave -->

# Wire the bounded spawn and repair ladders into the loop

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I17/P30-I17-W03

**Status:** DRAFT

**Scope:** P30-I17-W03

```eawf-wave-body
criteria:
  - id: CR-01
    text: "_fill_lanes routes through spawn_lane_or_fork and the failure path through repair_lane_or_fork: an injected spawn error forks instead of aborting the run, a failing check re-dispatches up the bounded ladder, and the cockpit repair counter reflects real attempts."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "grep shows production call-sites for both ladders; fault injection test forks on spawn error; repair test climbs the ladder then forks at exhaustion"
    response:
      observe: exits
      object: "grep shows production call-sites for both ladders; fault injection test forks on spawn error; repair test climbs the ladder then forks at exhaustion"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_p30_i17_w03.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
