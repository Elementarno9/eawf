<!-- eawf-template: spec-wave -->

# FA4 stdout producer for the live output tail

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I17/P30-I17-W08

**Status:** DRAFT

**Scope:** P30-I17-W08

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The dispatch runner captures spawned-child stdout/stderr and fans it to the App buffer; the agent-watch tail renders live lines from a real spawned session with bounded memory (ring-buffer cap)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "Pilot test sees injected child output in the tail; buffer cap property test holds"
    response:
      observe: triggers_action
      object: "Pilot test sees injected child output in the tail; buffer cap property test holds"
      locus: tui_pilot
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_p30_i17_w08.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: agent_watch
    policy: block
    cadence: every-wave
```
