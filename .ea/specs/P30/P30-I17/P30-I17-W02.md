<!-- eawf-template: spec-wave -->

# Transmit the full arm spec into DriveParams

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I17/P30-I17-W02

**Status:** DRAFT

**Scope:** P30-I17-W02

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Every arm-form field reaches DriveParams: a run armed with a budget cap HALTs at the cap with the DL-4 teeth, hard_halt escalates per spec, and the scope option changes the claimed frontier."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "integration test arms a capped run and asserts HALT at cap and the hard_halt escalation; params snapshot in the persisted fleet_run matches the form"
    response:
      observe: exits
      object: "integration test arms a capped run and asserts HALT at cap and the hard_halt escalation; params snapshot in the persisted fleet_run matches the form"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/tui/test_p30_i17_w02.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: autopilot
    policy: block
    cadence: every-wave
```
