<!-- eawf-template: spec-wave -->

# SKILL-2 Bind per-skill body validation at the engine emit path

**Scope:** P30-I03-W02

```eawf-wave-body
criteria:
  - id: CR-01
    text: "run_skill validates a registered dict body via model_validate before building the OutputEnvelope: a conforming dict emits a normal envelope, a drifted dict (extra-forbid key) raises pydantic.ValidationError on the success/emit path naming the offending key, and a raw markdown string body emits unchanged (the str branch is not gated); a skill name with no registered model is ungated."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_skill_engine_body_validation.py asserts the conforming-dict emit, the drifted-dict ValidationError naming the extra key, the str-body bypass, and the unregistered-skill bypass; the engine + CLI skill suites stay green; pytest exit zero"
    response:
      observe: exits
      object: "zero from the engine body-validation + skill suites"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/workflow/test_skill_engine_body_validation.py", "tests/unit/test_skills_engine.py", "tests/unit/test_cli_skill.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
