<!-- eawf-template: spec-wave -->

# SKILL-4 Add lean/explain output-style presets over clarity dimensions

**Scope:** P30-I03-W04

```eawf-wave-body
criteria:
  - id: CR-01
    text: "OutputStyle resolves to lean by default; explain renders a directive with why/jargon clauses absent from lean; an unknown config output.style value raises ValidationError at load; OutputStylePreset is extra-forbid and frozen; and the lean directive asserts readable sentences (no all-caps fragment markers), pinning the not-caveman-caps clause."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_output_style.py asserts lean default, explain-only why/jargon clauses, ValidationError on an unknown style, extra-key rejection on the preset, and the no-all-caps-fragment regex over the lean directive; pytest exit zero"
    response:
      observe: exits
      object: "zero from the output-style preset suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/platform/test_output_style.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
