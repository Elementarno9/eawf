<!-- eawf-template: spec-wave -->

# Reconcile AGENTS-source IntentBrief/NarrativeBundle refs to live homes

**Scope:** P30-I03-W09

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The AGENTS.md source block in core.yaml names the live homes (IntentBrief at kernel.spec.intent, NarrativeBundle at surfaces.render.narrative) with the stale v0.4 stamp dropped; the re-rendered AGENTS.md passes the no-manual-wrap lint (the freshly synced quality/agent_driven prose unwrapped at source); and the two agents_md goldens match the reconciled render byte-for-byte."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the agents_md golden suite, the lint-hook suite, and the platform profile suites pass against the reconciled source + render; pytest exit zero"
    response:
      observe: exits
      object: "zero from the agents_md golden + lint-hook suites"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/golden/test_golden_agents_md.py", "tests/unit/test_lint_hooks.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
