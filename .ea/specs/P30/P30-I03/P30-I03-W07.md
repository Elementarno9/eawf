<!-- eawf-template: spec-wave -->

# SKILL-7 Reconcile registry.py descriptive prose to shipped P29 symbols

**Scope:** P30-I03-W07

```eawf-wave-body
criteria:
  - id: CR-01
    text: "registry.py prose names live P29 symbols: the IntentBrief reference resolves to kernel/spec/intent, v0.4 stamps are dropped from landed skills, and the verify/trust/depth surface (run_oracle, cross-vendor jury, CriterionSpec, QualityDimension, EAWF021/022, trust scorecard, EviBound) is described as it ships today, with enforcement framed advisory-by-default; the paired plugin-install golden trees match the reconciled render."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the registry/skill render suites plus the paired plugin-install golden trees pass against the reconciled prose; pytest exit zero"
    response:
      observe: exits
      object: "zero from the registry render + plugin-install golden suites"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "-k", "registry and skill", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
