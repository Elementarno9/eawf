<!-- eawf-template: spec-wave -->

# SKILL-1 Move skill body-model registry into the library

**Scope:** P30-I03-W01

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A canonical SKILL_BODY_MODELS dict and a body_model_for(name) lookup live in the library under workflow/skills/bodies; the CLI _skill_body_models re-exports the same object so skill list's fingerprint column stays byte-stable; body_model_for resolves every canonical skill name to a BaseModel subclass and raises KeyError (never returns None) on an unknown name."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_skill_body_registry.py asserts every canonical name resolves to a BaseModel subclass, KeyError names the unknown skill, and the CLI re-export is the identical object; the skill-list CLI suites stay green; pytest exit zero"
    response:
      observe: exits
      object: "zero from the body-registry + skill-list CLI suites"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/workflow/test_skill_body_registry.py", "tests/unit/test_cli_skill.py", "tests/unit/test_cli_skill_list_payload.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
