<!-- eawf-template: spec-wave -->

# SKILL-6 Idle-contract-gate row + binding-proof regression suite

**Scope:** P30-I03-W06

```eawf-wave-body
criteria:
  - id: CR-01
    text: "With the SKILL-2 binding present the idle-contract gate's binding-proof probe drives a drifted dict body through run_skill, the emit path raises pydantic.ValidationError, and the gate exits 0; a run_skill stub that skips body validation makes the probe stop raising and the gate exits non-zero (the gate that guards the gate bites when the binding is removed)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_idle_contract_binding_probe.py asserts the live-binding exit-0 path and the no-validation-stub non-zero path through the injection seam; the existing idle-contract gate suite stays green; pytest exit zero"
    response:
      observe: exits
      object: "zero from the binding-proof probe suites"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_idle_contract_binding_probe.py", "tests/unit/test_idle_contract_gate.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
