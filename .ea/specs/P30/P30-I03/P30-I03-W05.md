<!-- eawf-template: spec-wave -->

# SKILL-5 Bind validate_prose over OutputEnvelope.body at emit

**Scope:** P30-I03-W05

```eawf-wave-body
criteria:
  - id: CR-01
    text: "At emit a clean markdown body yields no prose_clarity warning while a body tripping a deterministic leg (e.g. the manual-wrap lint) adds a footer.warnings entry with code=prose_clarity; the check is advisory (status stays ok, never flips to blocked); a dict body is normalized to markdown before the prose check; and a prose-lint crash fails open (emit still succeeds)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_skill_emit_prose_check.py asserts the clean-body no-warning path, the wrapped-body prose_clarity warning with status ok, the dict-body normalize-then-check path, and the fail-open crash path; pytest exit zero"
    response:
      observe: exits
      object: "zero from the emit prose-check suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/workflow/test_skill_emit_prose_check.py", "tests/unit/test_skills_engine.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
