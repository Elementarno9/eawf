<!-- eawf-template: spec-wave -->

# SKILL-3 Make /prep dag required + cross-field validators

**Scope:** P30-I03-W03

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A planning PrepBody (no_op False, blocked False) requires a non-empty dag with reconciled waves and no dangling deps: an empty dag, a wave task not in the dag, or a dangling dep each raise ValidationError naming the offending id, while a no_op=True body and a blocked=True closed-phase stub validate clean with no dag (the conditional exemption holds)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_prep_body_validators.py pins the three failure modes (empty dag, unreconciled wave named, dangling dep named) and the three clean paths (reconciled plan, no_op stub, blocked stub); the prep skill + body suites stay green; pytest exit zero"
    response:
      observe: exits
      object: "zero from the prep body-validator suites"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/workflow/test_prep_body_validators.py", "tests/unit/test_skill_prep.py", "tests/unit/test_skill_bodies.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
