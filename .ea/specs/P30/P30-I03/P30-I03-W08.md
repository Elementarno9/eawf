<!-- eawf-template: spec-wave -->

# SKILL-8 Prose-vs-symbol drift oracle for registry.py

**Scope:** P30-I03-W08

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The skill-prose-symbol gate over the reconciled registry.py exits 0 (every backticked dotted symbol and src/eawf path resolves via import / filesystem); a fixture prose block naming the unresolvable kernel.spec.research.IntentBrief makes the gate exit non-zero naming the offending token plus the carrying skill; and a block with bare prose nouns (IntentBrief, --depth, gate) produces no findings; the gate runs as a blocking pre-commit hook sibling to idle-contract-gate."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "skill_prose_symbol_gate.py exits 0 on the live registry; test_skill_prose_symbol_gate.py asserts the fixture fail names token + skill and the bare-noun / boundary blocks yield no findings; pytest exit zero"
    response:
      observe: exits
      object: "zero from the prose-symbol gate suite"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_skill_prose_symbol_gate.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
