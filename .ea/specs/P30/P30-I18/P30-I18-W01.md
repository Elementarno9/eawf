<!-- eawf-template: spec-wave -->

# Production round_runner binding

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I18/P30-I18-W01

**Status:** DRAFT

**Scope:** P30-I18-W01

```eawf-wave-body
criteria:
  - id: CR-01
    text: "drive_campaign executes one live round against a toy question: every StagedDispatch becomes a spawned researcher session, each agent_end researcher body parses into findings rows, and a failed parse surfaces a typed error rather than a silent empty round."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "live integration test runs one round and asserts persisted round results; parse-failure negative test raises typed error"
    response:
      observe: exits
      object: "live integration test runs one round and asserts persisted round results; parse-failure negative test raises typed error"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_p30_i18_w01.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
