<!-- eawf-template: spec-wave -->

# Split render_agents_md into tier0 Zone-1 and reference Zone-2

**Scope:** P30-I01-W07

```eawf-wave-body
criteria:
  - id: CR-01
    text: "render_agents_md partitions output keyed on RenderBlock.tier: tier0 block ids appear in the Zone-1 region and reference block ids appear in Zone-2; a profile with zero tier0 blocks emits no empty Zone-1 region."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the tier-partition and zero-tier0 cases in test_render_agents_md assert tier0 ids land in Zone-1 and a zero-tier0 profile emits no empty Zone-1 region under pytest exit zero"
    response:
      observe: exits
      object: "zero from the render_agents_md test suite asserting tier0 ids partition into Zone-1 and a zero-tier0 profile emits no empty Zone-1 region"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The affordance_parity invariant holds: every composed tier0 block id is present in the rendered Zone-1 region (no dropped affordance) and no tier0 block id leaks into the Zone-2 reference region."
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the affordance-parity and no-tier0-leak cases in test_render_agents_md assert every tier0 id is present in Zone-1 and no tier0 id leaks into the Zone-2 reference region under pytest exit zero"
    response:
      observe: exits
      object: "zero from the render_agents_md test suite asserting affordance parity holds and no tier0 id leaks into the Zone-2 reference region"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_render_agents_md.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_render_agents_md.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
