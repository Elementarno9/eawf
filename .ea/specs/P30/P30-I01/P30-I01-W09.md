<!-- eawf-template: spec-wave -->

# Bind a source-brief coverage gate over --from-briefs units

**Scope:** P30-I01-W09

```eawf-wave-body
criteria:
  - id: CR-01
    text: "check_source_brief_coverage extracts units from each referenced source-brief document and flags any unit not mapped to a wave criterion / planned_step / backlog id / explicit deferral: a brief enumerating an uncovered deliverable yields one CoverageFinding naming the dropped unit; the same unit marked deferred yields zero findings."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "test_check_source_brief_coverage_flags_uncovered_unit yields one CoverageFinding for the dropped unit while test_check_source_brief_coverage_deferred_unit_is_not_a_finding and the all-units-covered case yield zero, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the source-brief coverage unit suite asserting an uncovered deliverable yields one finding and a deferred unit yields none"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The spec.py:1025 empty-planned_steps no-op no longer hides drift for required-intent waves and apply is refused on an uncovered source unit: a required-intent wave with empty planned_steps is a finding, and an uncovered unit at apply refuses the apply."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the propose-coverage unit suite asserts a required-intent wave with empty planned_steps still surfaces the source-brief gap rather than short-circuiting, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the propose-coverage and source-brief unit suites asserting a required-intent empty-planned-steps wave still surfaces the uncovered source unit"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/platform/lint/test_eawf021_022.py", "tests/unit/propose/test_generator_coverage.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/platform/lint/test_eawf021_022.py", "tests/unit/propose/test_generator_coverage.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
