<!-- eawf-template: spec-wave -->

# Surface daemonless state and fix feed event delivery

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I21/P30-I21-W25

**Status:** DRAFT

**Scope:** P30-I21-W25

```eawf-wave-body
criteria:
- id: CR-01
  text: The EVENT-kind daemon subscription no longer pins scope_id to the state URN (state_binding.py _subscribe_params), so wave- and iter-scoped fleet events reach the feed ring buffer; pinned by a test in tests/tui/test_daemonless_ux_w25.py that pushes a wave-scoped envelope through the subscription path and asserts it lands in the app live-event buffer.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: uv run pytest tests/tui/test_daemonless_ux_w25.py -q exits zero including the wave-scoped-event delivery test
  response:
    observe: exits
    object: zero from the named regression suite over the subscription scope
    locus: cli_exit
  gate_ids:
  - G-01
- id: CR-02
  text: With no reachable daemon the feed seeds from the event.jsonl tail (store_path StoreKind.EVENT, mirroring the /events overlay) and renders a connection indicator that distinguishes daemon-disconnected from connected-but-no-events; the misleading unconditional "live feed waiting for events" empty state is gone; pinned by Pilot tests in the same suite.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the named suite includes a daemonless mount test asserting file-tail rows render plus a disconnected badge
  response:
    observe: exits
    object: zero from the daemonless feed fallback tests
    locus: cli_exit
  gate_ids:
  - G-02
- id: CR-03
  text: Escape on a top-level mode screen no longer quits the app silently (binding removed or routed through an explicit confirm); the autopilot pane derives its ready frontier from state.json when the daemon is absent so claim-ready waves are not reported as "no ready waves", and the duplicated empty-state message renders once; all pinned by tests in the same suite.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: interaction_capability
  measurable_signal: the named suite asserts escape does not exit the app from Home and that a state with claim-ready pending waves renders a non-empty autopilot frontier without a daemon
  response:
    observe: exits
    object: zero from the escape-quit and ready-frontier tests
    locus: cli_exit
  gate_ids:
  - G-03
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/tui/test_daemonless_ux_w25.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/tui/test_daemonless_ux_w25.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-03
  criterion_id: CR-03
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/tui/test_daemonless_ux_w25.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-04
  criterion_id: CR-01
  kind: affordance_parity
  args:
    mode: feed
  policy: block
  cadence: every-wave
```
