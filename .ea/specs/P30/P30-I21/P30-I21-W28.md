<!-- eawf-template: spec-wave -->

# Add interaction-liveness gate over advertised TUI keys

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I21/P30-I21-W28

**Status:** DRAFT

**Scope:** P30-I21-W28

```eawf-wave-body
criteria:
- id: CR-01
  text: A pilot_harness helper asserts that pressing an advertised footer key on a mode screen produces a visible frame delta or a toast (extending mutating_action_keys_resolve from handler-exists to visibly-responds); exposed for reuse by mode suites; pinned by unit tests in tests/tui/test_interaction_liveness_w28.py.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: uv run pytest tests/tui/test_interaction_liveness_w28.py -q exits zero including the helper unit tests
  response:
    observe: exits
    object: zero from the named regression suite over the liveness helper
    locus: cli_exit
  gate_ids:
  - G-01
- id: CR-02
  text: The suite sweeps all nine modes against the footer-advertised key set and fails on a silently dead key, so regressions of the W27/W26 dead-key class are caught in CI; keys whose action is legitimately unavailable in the fixture state are declared with a reason in the test, not skipped silently.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the named suite enumerates the nine mode screens and asserts liveness (or a declared exemption) per advertised key
  response:
    observe: exits
    object: zero from the nine-mode liveness sweep
    locus: cli_exit
  gate_ids:
  - G-01
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/tui/test_interaction_liveness_w28.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-01
  kind: affordance_parity
  args:
    mode: home
  policy: block
  cadence: every-wave
```
