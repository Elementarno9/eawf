<!-- eawf-template: spec-wave -->

# Fix evidence pane labels, columns, and dead keys

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I21/P30-I21-W27

**Status:** DRAFT

**Scope:** P30-I21-W27

```eawf-wave-body
criteria:
- id: CR-01
  text: Researcher report rows label by campaign topic (joined via the research_campaign store on base_id == campaign_id) instead of the raw campaign UUID, and role moves to its own table column with the label column no longer carrying the fused role suffix; pinned by tests in tests/tui/test_evidence_pane_w27.py.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: interaction_capability
  measurable_signal: uv run pytest tests/tui/test_evidence_pane_w27.py -q exits zero including the topic-label and role-column tests
  response:
    observe: exits
    object: zero from the named regression suite over the evidence row build
    locus: cli_exit
  gate_ids:
  - G-01
- id: CR-02
  text: The Followups section is visually separated from the reports table (margin or rule) and the jury tier ladder renders inline on at most two rows while preserving the scored-tier marker; pinned by render tests in the same suite.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: interaction_capability
  measurable_signal: the named suite asserts the separator presence and the tier ladder line count
  response:
    observe: exits
    object: zero from the separator and tier-ladder layout tests
    locus: cli_exit
  gate_ids:
  - G-01
- id: CR-03
  text: The reports table receives focus on mount so arrow keys move the selection and both p (peek) and Enter (open) produce a visible response under Pilot tests; a report whose wave status is terminal-failed renders a cross-mark against the self-claimed verdict cell so a failed wave cannot read as an unqualified pass; pinned in the same suite.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the named suite asserts selection movement on down-arrow, frame delta on p and Enter, and a failed-wave verdict cross-mark
  response:
    observe: exits
    object: zero from the focus, peek/enter, and verdict cross-mark tests
    locus: cli_exit
  gate_ids:
  - G-01
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/tui/test_evidence_pane_w27.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-01
  kind: affordance_parity
  args:
    mode: evidence
  policy: block
  cadence: every-wave
```
