<!-- eawf-template: spec-wave -->

# Re-close P30 with validated v0.6.0 release annotation

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I21/P30-I21-W22

**Status:** DRAFT

**Scope:** P30-I21-W22

```eawf-wave-body
criteria:
- id: CR-01
  text: 'The phase-close commit subject is exactly the runbook draft [P30] state: close iter + phase (audit=A-P30-I22-ship) (release=v0.6.0) with the release annotation as its own paren group; the W32 lint dry-run accepts it and hard-rejects the fused (audit=..., release=...) shape.'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the W32 release-annotation suite (commit-prefix lint dry-run of the exact drafted subject) exits zero under pytest
  response:
    observe: exits
    object: zero from the release-annotation suite in tests/unit/test_commit_prefix_lint.py proving the drafted subject parses and the fused shape rejects
    locus: pytest
  gate_ids:
  - G-01
- id: CR-02
  text: CHANGELOG.md carries the released 0.6.0 section heading with at least one bullet and the migration notes for the 1.13 and 1.14 schema bumps; the structural gate reads the committed file, not prose claims.
  kind: structural
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: maintainability
  measurable_signal: the reclose staging suite asserts the 0.6.0 CHANGELOG heading, a bullet under it, and the 1.13/1.14 migration notes, under pytest exit zero
  response:
    observe: exits
    object: zero from tests/lint/test_reclose_staging.py changelog assertions over the committed CHANGELOG.md
    locus: pytest
  gate_ids:
  - G-02
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/unit/test_commit_prefix_lint.py
    - -q
    - -k
    - release
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/lint/test_reclose_staging.py
    - -q
    - -k
    - changelog
    scope: touched
  policy: block
  cadence: every-wave
```
