<!-- eawf-template: spec-wave -->

# Fix watch-mode trust and contrast defect cluster

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I21/P30-I21-W24

**Status:** DRAFT

**Scope:** P30-I21-W24

```eawf-wave-body
criteria:
- id: CR-01
  text: Selected watch lane and picker rows render without per-span muted colours and the .-selected CSS adds color $text (attention_feed.py:202-212 pattern), so the row is readable on the SELECTION_TINT band; render_lane_row(selected=True) and render_picker_row(selected=True) both covered by unit tests in tests/tui/test_watch_correctness_w24.py.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: interaction_capability
  measurable_signal: uv run pytest tests/tui/test_watch_correctness_w24.py -q exits zero with tests asserting the plain-render selected variants
  response:
    observe: exits
    object: zero from the named regression suite over the selected-row render paths
    locus: cli_exit
  gate_ids:
  - G-01
- id: CR-02
  text: pick_watch_target excludes executor sessions whose wave status is terminal (failed/closed/abandoned) from the active pool, and render_watch_header derives its status label from the wave when the wave is terminal, so a failed wave is never labelled active; pinned by tests in the same suite.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the named suite includes a test constructing a FAILED wave with a stale ACTIVE session and asserting it is not picked (or is labelled from wave status)
  response:
    observe: exits
    object: zero from the pick_watch_target terminal-wave exclusion tests
    locus: cli_exit
  gate_ids:
  - G-01
- id: CR-03
  text: The watch OutputTail replay for a wave whose status is terminal-not-closed is framed with the real wave verdict (a banner or prefix naming the wave terminal status) instead of echoing the agent self-claimed pass unqualified; Esc from watch returns to the previous mode or the session picker (gate relaxed to >=1 row) and no longer falls through to a hardcoded feed switch; both pinned by tests in the same suite.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the named suite includes replay-framing and Esc-routing tests that fail on the pre-fix code paths
  response:
    observe: exits
    object: zero from the replay-framing and leave_zoom routing tests
    locus: cli_exit
  gate_ids:
  - G-01
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/tui/test_watch_correctness_w24.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-01
  kind: affordance_parity
  args:
    mode: watch
  policy: block
  cadence: every-wave
```
