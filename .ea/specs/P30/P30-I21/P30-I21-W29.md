<!-- eawf-template: spec-wave -->

# Stamp headless spawn tokens onto wave runtime for close capture

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I21/P30-I21-W29

**Status:** DRAFT

**Scope:** P30-I21-W29

```eawf-wave-body
criteria:
- id: CR-01
  text: The headless spawn path stamps the matched runtime snapshot onto wave.runtime_latest so per-class tokens land even when a claim-time baseline exists, and the runtime.capture merge helper never overwrites a populated token field with None; a unit test suite closes a headless wave and asserts wave.tokens_consumed exceeds zero.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: uv run pytest tests/runtime/test_headless_token_capture_w29.py -q exits zero
  response:
    observe: exits
    object: zero from the headless-token-capture regression suite
    locus: cli_exit
  gate_ids:
  - G-01
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/runtime/test_headless_token_capture_w29.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
