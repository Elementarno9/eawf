<!-- eawf-template: spec-wave -->

# Fix research board grouping, status, and scoping

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I21/P30-I21-W26

**Status:** DRAFT

**Scope:** P30-I21-W26

```eawf-wave-body
criteria:
- id: CR-01
  text: The scope-wide questions grouping in build_tree_nodes is hoisted to depth zero (a scope-root sibling of campaigns) so question leaves no longer render as children of the last campaign; pinned by a tree-shape test in tests/tui/test_research_board_ia_w26.py using a two-campaign fixture with questions.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: uv run pytest tests/tui/test_research_board_ia_w26.py -q exits zero including the questions-parenting tree test
  response:
    observe: exits
    object: zero from the named regression suite over the topic-tree build
    locus: cli_exit
  gate_ids:
  - G-01
- id: CR-02
  text: CampaignRow carries the campaign's own status and each campaign's round node is classified individually (CONVERGED renders as a terminal label, not "round running"); wrapped tree lines keep their node indent; pinned by tests in the same suite with one converged and one running campaign.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the named suite asserts a converged campaign's node label differs from a running one and wrapped continuation lines start at the node indent column
  response:
    observe: exits
    object: zero from the per-campaign status and wrap-indent tests
    locus: cli_exit
  gate_ids:
  - G-02
- id: CR-03
  text: The claims pane scopes to the selected tree node (or explicitly headers the selection it is showing) instead of one global claim soup; questions whose row reads answered leave the Unresolved section; Enter on a tree node produces a visible peek response (overlay or detail render) under a Pilot test; all pinned in the same suite.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: interaction_capability
  measurable_signal: the named suite asserts claims-pane content changes with tree selection, answered questions are absent from Unresolved, and Enter changes the rendered frame
  response:
    observe: exits
    object: zero from the claims-scoping, unresolved-split, and enter-peek tests
    locus: cli_exit
  gate_ids:
  - G-03
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/tui/test_research_board_ia_w26.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/tui/test_research_board_ia_w26.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-03
  criterion_id: CR-03
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/tui/test_research_board_ia_w26.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-04
  criterion_id: CR-01
  kind: affordance_parity
  args:
    mode: research_board
  policy: block
  cadence: every-wave
```
