<!-- eawf-template: spec-wave -->

# EUCAP-3 Register SESSION_END capture hook -> daemon RPC

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I05/P30-I05-W03

**Status:** DRAFT

**Scope:** P30-I05-W03

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Dispatching a SESSION_END HookEvent carrying a cost block through a HookRunner invokes runtime.capture with the parsed counters; a SESSION_END with no cost block returns a non-blocking HookResult and makes no RPC call."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "test_session_end_capture_hook_invokes_runtime_capture_rpc asserts the runtime.capture RPC payload and test_session_end_capture_hook_missing_cost_makes_no_rpc_call asserts the no-cost non-blocking no-RPC path"
    response:
      observe: exits
      object: "zero from the SESSION_END capture hook cost-block and no-cost pytest cases"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "When the daemon is down the hook returns a non-blocking HookResult carrying the raised error rather than crashing the CC session (statusline-grade degradation)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "test_session_end_capture_hook_daemon_error_is_non_blocking asserts a daemon exception is rendered into a non-blocking HookResult output"
    response:
      observe: exits
      object: "zero from the daemon-error SESSION_END capture hook pytest case"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_runtime_capture_hook.py::test_session_end_capture_hook_invokes_runtime_capture_rpc", "tests/unit/test_runtime_capture_hook.py::test_session_end_capture_hook_missing_cost_makes_no_rpc_call", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_runtime_capture_hook.py::test_session_end_capture_hook_daemon_error_is_non_blocking", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
