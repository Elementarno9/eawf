<!-- eawf-template: spec-wave -->

# EUCAP-4 Add runtime.capture daemon RPC keyed to active wave

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I05/P30-I05-W04

**Status:** DRAFT

**Scope:** P30-I05-W04

```eawf-wave-body
criteria:
  - id: CR-01
    text: "With exactly one active wave, runtime.capture updates that wave's runtime_latest counters; with two active waves it records to both and logs the active_count ambiguity."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_runtime_capture_updates_one_active_wave asserts one active wave receives runtime_latest counters and test_runtime_capture_updates_two_active_waves_and_logs_ambiguity asserts two active waves are both updated with active_count logging"
    response:
      observe: exits
      object: "zero from the one-active and two-active runtime.capture pytest cases"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "When active_wave_ids is empty the RPC refuses (LifecycleError / validation_failed) rather than silently dropping the capture; the params model forbids extra keys."
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "test_runtime_capture_refuses_without_active_waves asserts validation_failed on no active waves and test_runtime_capture_params_forbid_extra_keys asserts the strict params contract"
    response:
      observe: exits
      object: "zero from the empty-active and strict-extra runtime.capture pytest cases"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/daemon/test_runtime_capture.py::test_runtime_capture_updates_one_active_wave", "tests/unit/daemon/test_runtime_capture.py::test_runtime_capture_updates_two_active_waves_and_logs_ambiguity", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/daemon/test_runtime_capture.py::test_runtime_capture_refuses_without_active_waves", "tests/unit/daemon/test_runtime_capture.py::test_runtime_capture_params_forbid_extra_keys", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
