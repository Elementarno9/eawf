<!-- eawf-template: spec-wave -->

# EUCAP-8 Close-gate refuses silent zero-runtime close

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I05/P30-I05-W08

**Status:** DRAFT

**Scope:** P30-I05-W08

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Closing a wave with no captured runtime raises LifecycleError (exit 2) and writes no CLOSED status; a wave with a real non-zero delta closes cleanly (the gate does not over-fire); under the advisory dial the same zero-runtime wave WARNS and still closes."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "test_close_refuses_zero_runtime, test_close_allows_nonzero_runtime, and test_close_advisory_phase_warns_not_refuses assert the enforcing, non-zero, and advisory close paths"
    response:
      observe: exits
      object: "zero from the zero-runtime close pytest gate"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "A profile that wires the runtime gate but never enables it reds the idle-contract meta-gate, proving the contract is bound and not idle (mirrors the B091 guard)."
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "test_runtime_gate_is_not_idle asserts the pre-commit binding is enabled and fails when always_run is disabled"
    response:
      observe: exits
      object: "zero from the idle-contract pytest gate"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_state_methods.py::test_close_refuses_zero_runtime", "tests/daemon/test_state_methods.py::test_close_allows_nonzero_runtime", "tests/daemon/test_state_methods.py::test_close_advisory_phase_warns_not_refuses", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_idle_contract_gate.py::test_runtime_gate_is_not_idle", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
