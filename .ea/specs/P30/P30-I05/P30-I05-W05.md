<!-- eawf-template: spec-wave -->

# EUCAP-5 Statusline cost-counter sidecar as fresh backstop

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I05/P30-I05-W05

**Status:** DRAFT

**Scope:** P30-I05-W05

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Writing counters to the sidecar and reading them back yields equal counters."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_runtime_counter_sidecar_round_trips_counters asserts a written RuntimeCounters value reads back equal and test_statusline_render_writes_runtime_counter_sidecar asserts statusline rendering writes the parsed counters beside the cache"
    response:
      observe: exits
      object: "zero from the sidecar round-trip and statusline-write pytest cases"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "A missing sidecar file reads as None and corrupt JSON reads as a miss, with the statusline degrading rather than crashing."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "test_runtime_counter_sidecar_missing_file_reads_none and test_runtime_counter_sidecar_corrupt_json_reads_none assert miss semantics, while test_statusline_render_writes_runtime_counter_sidecar exits zero through the statusline path"
    response:
      observe: exits
      object: "zero from the missing, corrupt, and statusline sidecar pytest cases"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_runtime_counter_sidecar.py::test_runtime_counter_sidecar_round_trips_counters", "tests/unit/test_runtime_counter_sidecar.py::test_statusline_render_writes_runtime_counter_sidecar", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_runtime_counter_sidecar.py::test_runtime_counter_sidecar_missing_file_reads_none", "tests/unit/test_runtime_counter_sidecar.py::test_runtime_counter_sidecar_corrupt_json_reads_none", "tests/unit/test_runtime_counter_sidecar.py::test_statusline_render_writes_runtime_counter_sidecar", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
