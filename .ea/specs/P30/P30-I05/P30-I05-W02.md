<!-- eawf-template: spec-wave -->

# EUCAP-2 Parse CC cost block into typed RuntimeCounters

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I05/P30-I05-W02

**Status:** DRAFT

**Scope:** P30-I05-W02

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A real-shape CC payload with a cost block parses into typed counters whose api_duration_ms and cost_usd match the input; a payload with no cost key returns None and never raises (degrade-not-crash)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "test_parse_runtime_counters_real_status_payload asserts api_duration_ms and cost_usd, and test_parse_runtime_counters_missing_cost_returns_none asserts the no-cost None path"
    response:
      observe: exits
      object: "zero from the runtime counter real-payload and no-cost pytest cases"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "RuntimeCounters rejects unknown keys (extra=forbid) and drops type-mismatched fields rather than raising, mirroring _extract_usage."
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "test_runtime_counters_rejects_unknown_keys raises ValidationError and test_parse_runtime_counters_drops_type_mismatched_fields asserts wrong-typed fields are omitted"
    response:
      observe: exits
      object: "zero from the runtime counter strict-extra and wrong-type degradation pytest cases"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_runtime_counters.py::test_parse_runtime_counters_real_status_payload", "tests/unit/test_runtime_counters.py::test_parse_runtime_counters_missing_cost_returns_none", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_runtime_counters.py::test_runtime_counters_rejects_unknown_keys", "tests/unit/test_runtime_counters.py::test_parse_runtime_counters_drops_type_mismatched_fields", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
