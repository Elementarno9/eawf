<!-- eawf-template: spec-wave -->

# EUCAP-9 Add no-runtime waiver flag + EvidenceRecord

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I05/P30-I05-W09

**Status:** DRAFT

**Scope:** P30-I05-W09

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Closing a zero-runtime wave with --no-runtime reaches CLOSED and writes a waived EvidenceRecord carrying produced_by=human; closing the same wave without the flag still refuses (the waiver is opt-in)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_close_with_no_runtime_waiver_succeeds reaches CLOSED with a waived row; test_close_no_runtime_without_flag_still_refuses raises"
    response:
      observe: exits
      object: "zero from the no-runtime daemon close pytest gate"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The waiver EvidenceRecord is operator-attributable: produced_by is human, not an agent, so the bypass is traceable to the operator."
    kind: structural
    acceptance_style: binary
    evidence_kind: attested
    quality_dimension: security
    measurable_signal: "test_no_runtime_waiver_records_human_producer asserts the EvidenceRecord produced_by field equals human"
    response:
      observe: exits
      object: "zero from the no-runtime waiver evidence pytest gate"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_state_methods.py::test_close_with_no_runtime_waiver_succeeds", "tests/daemon/test_state_methods.py::test_close_no_runtime_without_flag_still_refuses", "tests/unit/test_cli_wave_close_commit.py::test_wave_close_daemon_proxy_forwards_no_runtime_waiver", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_cli_wave_close_commit.py::test_no_runtime_waiver_records_human_producer", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
