<!-- eawf-template: spec-wave -->

# EUCAP-1 Add RuntimeBaseline field + snapshot at wave claim

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I05/P30-I05-W01

**Status:** DRAFT

**Scope:** P30-I05-W01

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Claiming a wave with a primed runtime sidecar stamps Wave.runtime_baseline with the cumulative counters and a captured_at timestamp; a re-claim with the same session preserves the baseline (mirrors the claimed_at contract)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_claim_wave_captures_runtime_baseline_when_available asserts the captured counters and timestamp, and test_claim_wave_preserves_runtime_baseline_on_idempotent_reclaim asserts a same-session re-claim leaves the baseline unchanged"
    response:
      observe: exits
      object: "zero from the runtime-baseline claim snapshot and idempotent re-claim pytest cases"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "RuntimeBaseline is a strict model (extra=forbid) and Wave.runtime_baseline defaults to None so persisted pre-1.9 rows still validate."
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "test_runtime_baseline_rejects_extra_fields proves strict extras, test_wave_runtime_baseline_defaults_to_none proves the optional field default, and the v1.8 to v1.9 migration tests prove old state rows validate after backfill"
    response:
      observe: exits
      object: "zero from the RuntimeBaseline strict/default and v1.8 to v1.9 migration pytest cases"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_wave_close_actuals.py::test_claim_wave_captures_runtime_baseline_when_available", "tests/unit/test_wave_close_actuals.py::test_claim_wave_preserves_runtime_baseline_on_idempotent_reclaim", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/state/test_runtime_baseline.py::test_runtime_baseline_rejects_extra_fields", "tests/unit/state/test_runtime_baseline.py::test_wave_runtime_baseline_defaults_to_none", "tests/unit/state/test_runtime_baseline.py::test_v1_8_to_v1_9_backfills_runtime_baseline_none", "tests/unit/state/test_runtime_baseline.py::test_v1_8_to_v1_9_output_validates_against_live_model", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
