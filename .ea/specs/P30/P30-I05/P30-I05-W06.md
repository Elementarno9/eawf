<!-- eawf-template: spec-wave -->

# EUCAP-6 Compute claim->close delta + persist actual record

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I05/P30-I05-W06

**Status:** DRAFT

**Scope:** P30-I05-W06

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Closing a wave whose baseline is 5000 ms and latest is 17000 ms persists ActualSummary.elapsed_eu == approx(12000 / (30*60000)) with agent_runtime_eu, actual_tokens, and actual_cost_usd set to the captured deltas."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "test_mutate_wave_close_uses_runtime_delta_when_captured asserts the daemon close path writes elapsed_eu, agent_runtime_eu, actual_tokens, and actual_cost_usd from runtime baseline/latest deltas"
    response:
      observe: exits
      object: "zero from the runtime-delta daemon close pytest case"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "An absent baseline yields a None delta (no phantom EU), a latest below the baseline raises per the non-negative guard, and an equal baseline/latest yields elapsed_eu == 0.0 (the exact input the W08 gate then catches)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "test_compute_runtime_delta_absent_baseline_returns_none, test_compute_runtime_delta_latest_below_baseline_raises, and test_compute_runtime_delta_equal_counters_yields_zero_eu assert the boundary and regression cases"
    response:
      observe: exits
      object: "zero from the runtime-delta helper boundary pytest cases"
      locus: pytest
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_state_methods.py::test_mutate_wave_close_uses_runtime_delta_when_captured", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_wave_close_actuals.py::test_compute_runtime_delta_absent_baseline_returns_none", "tests/unit/test_wave_close_actuals.py::test_compute_runtime_delta_latest_below_baseline_raises", "tests/unit/test_wave_close_actuals.py::test_compute_runtime_delta_equal_counters_yields_zero_eu", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
