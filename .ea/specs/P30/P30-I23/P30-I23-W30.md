<!-- eawf-template: spec-wave -->

# Build the live-render visual oracle with broadened falsifier

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W30

**Status:** DRAFT

**Scope:** P30-I23-W30

```eawf-wave-body
criteria:
- id: CR-01
  text: 'The mockup_golden_diff IMAGE branch gains a live-capture mode: a capture_mockup_golden_screen_png sibling in pilot_harness mounts the surface under Pilot, captures export_screenshot SVG, renders via pinned resvg to PNG, and the gate diffs THAT against the mockup golden (replacing the frozen tui_png at registry.py:440-452) when live mode is selected, failing when the live render diverges.'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: interaction_capability
  measurable_signal: the gate test asserts a LIVE Pilot render (not committed tui_png) is captured in live mode and the diff fails when the live render diverges from the mockup golden, under pytest exit zero
  response:
    observe: exits
    object: zero from the mockup-golden-diff live-mode suite asserting a live Pilot render is captured and diffed against the mockup golden
    locus: pytest
  gate_ids:
  - G-01
  - G-02
- id: CR-02
  text: LayoutFeatures (src/eawf/workflow/audit_dsl/kinds/mockup_image_diff.py:331-345) broadens with a contrast-on-selection feature (foreground/background luminance ratio over the selected row band); a low-contrast fixture fails while the faithful fixture passes, layout-shape weighting kept dominant.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: interaction_capability
  measurable_signal: the falsifier test asserts the contrast-on-selection feature fails a fixture whose selected-row contrast ratio is below floor while the faithful render passes, under pytest exit zero
  response:
    observe: exits
    object: zero from the falsifier suite asserting the contrast-on-selection feature fails the low-contrast fixture and passes the faithful one
    locus: pytest
  gate_ids:
  - G-03
- id: CR-03
  text: LayoutFeatures broadens with an alignment/right-edge feature (column right-alignment detection, the W10 class); a mis-aligned fixture fails the diff while the faithful fixture passes.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: interaction_capability
  measurable_signal: the falsifier test asserts the alignment feature fails the mis-aligned fixture while the faithful render passes, under pytest exit zero
  response:
    observe: exits
    object: zero from the falsifier suite asserting the alignment feature fails the mis-aligned fixture
    locus: pytest
  gate_ids:
  - G-04
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/unit/test_p30_i16_w14.py
    - tests/unit/test_p30_i20_w17.py
    - tests/unit/daemon/test_close_gate_mockup_golden_diff.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-01
  kind: affordance_parity
  args:
    mode: home
  policy: block
  cadence: every-wave
- id: G-03
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/unit/test_p30_i16_w14.py
    - tests/unit/test_p30_i20_w17.py
    - tests/unit/daemon/test_close_gate_mockup_golden_diff.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-04
  criterion_id: CR-03
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/unit/test_p30_i16_w14.py
    - tests/unit/test_p30_i20_w17.py
    - tests/unit/daemon/test_close_gate_mockup_golden_diff.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
