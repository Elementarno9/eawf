<!-- eawf-template: spec-wave -->

# Add per-mutation telemetry and a self-deadlock watchdog

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W10

**Status:** DRAFT

**Scope:** P30-I23-W10

```eawf-wave-body
criteria:
  - id: CR-01
    text: "daemon.status reports started_at + kind for an in-flight mutation (set at the in_flight increment, state.py:2879) and a duration_ms log line is emitted on completion, so a wedged close is observable before clients time out."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the test asserts daemon.status carries started_at + kind for an in-flight mutation and a duration_ms log line on completion"
    response:
      observe: emits
      object: "daemon.status exposes started_at + kind and a duration_ms line on completion"
      locus: log_capture
    gate_ids: [G-01]
  - id: CR-02
    text: "The self-deadlock watchdog aborts an artificially-wedged mutation past the hard limit (default 300s) and the daemon serves the next RPC without restart, retiring the manual pkill + rm recovery ceremony."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the test wedges a mutation past the hard limit and asserts the watchdog aborts it and the daemon serves the next RPC without restart"
    response:
      observe: triggers_action
      object: "the watchdog aborts a wedged mutation and the daemon serves the next RPC without restart"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_mutation_watchdog.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
