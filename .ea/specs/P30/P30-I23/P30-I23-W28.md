<!-- eawf-template: spec-wave -->

# Record the first inline close with captured elapsed_eu above zero

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W28

**Status:** DRAFT

**Scope:** P30-I23-W28

```eawf-wave-body
criteria:
- id: CR-01
  text: '>= 1 ActualSummary row in committed state.json carries elapsed_eu > 0 with non-null runtime provenance (the SessionEnd hook fed runtime.capture at state.py:2745 so the inline close stamped a non-zero runtime delta); the assertion holds forever after.'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the monotone repo test asserts state.json contains at least one ActualSummary row with elapsed_eu > 0 and non-null runtime provenance, under pytest exit zero
  response:
    observe: exits
    object: zero from tests/workflow/test_runtime_capture_present.py asserting an ActualSummary row with elapsed_eu > 0 exists in state.json
    locus: pytest
  gate_ids:
  - G-01
- id: CR-02
  text: The operator confirms the capture chain fired live in this repo's session (hook log line plus runtime.capture ok), not via hand-edited state; the executor report carries the log excerpt.
  kind: behavioral
  acceptance_style: binary
  evidence_kind: attested
  quality_dimension: functional_suitability
  measurable_signal: the operator attests the live SessionEnd capture fired (hook log line plus runtime.capture ok) with the log excerpt recorded in the report, not hand-edited state
  response:
    observe: judged
    object: the operator's attestation that the live capture chain fired with the runtime.capture ok log excerpt
    locus: human
    jury_reason: a live one-shot capture is operational fact only the operator can attest; forcing it deterministic would fake the oracle
  gate_ids:
  - G-02
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/workflow/test_runtime_capture_present.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/workflow/test_runtime_capture_present.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
