<!-- eawf-template: spec-wave -->

# Pass repo_root through the four interactive prompt call sites

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W40

**Status:** DRAFT

**Scope:** P30-I23-W40

```eawf-wave-body
criteria:
- id: CR-01
  text: 'repo_root= is passed at the four interactive prompt call sites: src/eawf/surfaces/cli/commands/lifecycle_wave.py:1049, src/eawf/surfaces/cli/commands/lifecycle_wave_read.py:494, src/eawf/surfaces/cli/commands/lifecycle_wave_read.py:385, and src/eawf/surfaces/cli/commands/pr_review.py:178 (anchors at HEAD 54f8d05b; re-locate at edit time).'
  kind: behavioral
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the dispatch-renderer and integration suites assert repo_root reaches all four interactive prompt renders, under pytest exit zero
  response:
    observe: exits
    object: zero from the dispatch-prompt suites asserting repo_root is threaded through the four interactive call sites
    locus: pytest
  gate_ids:
  - G-01
- id: CR-02
  text: 'wave show --dispatch-prompt renders a ## References section when a fixture spike brief matching the wave id exists under the workspace .ea/local/research/.'
  kind: behavioral
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: 'the dispatch-prompt suite asserts the ## References section appears when a matching fixture spike brief exists, under pytest exit zero'
  response:
    observe: exits
    object: zero from the dispatch-prompt suite asserting the References section renders for a matching fixture brief
    locus: pytest
  gate_ids:
  - G-02
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/unit/test_dispatch_renderer.py
    - tests/integration/
    - -q
    - -k
    - dispatch_prompt or references
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/unit/test_dispatch_renderer.py
    - tests/integration/
    - -q
    - -k
    - dispatch_prompt or references
    scope: touched
  policy: block
  cadence: every-wave
```
