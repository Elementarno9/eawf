<!-- eawf-template: spec-wave -->

# Rewrite dispatch workflow step 5 to report-then-parent-closes

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W38

**Status:** DRAFT

**Scope:** P30-I23-W38

```eawf-wave-body
criteria:
  - id: CR-01
    text: "_render_workflow at src/eawf/workflow/agents/specs/models.py emits the unified 'Emit the typed executor report' step 5 plus a stop-step 6 on BOTH the interactive and headless shapes; the interactive self-close branch (models.py:405-411) is removed and NO 'eawf wave close' instruction survives in the section; the never-narrow schema-wave clause is appended to step 3 (full-tree run at models.py:420); the render() docstring (models.py:535-561) is updated to match."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "test_subagent_spec_model and test_dispatch_renderer assert step 5 reports and step 6 stops on both shapes with no 'eawf wave close' text, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the subagent-spec and dispatch-renderer suites asserting the unified report-then-stop workflow with no self-close instruction"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "_render_out_of_scope (models.py, :427-436 replaced) emits the 'Run NO eawf command inside this worktree' clause with the STOP-and-report escape hatch; the prior 'uv run eawf state ...' directive (:433-434) is removed."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the dispatch-renderer suite asserts the out-of-scope section bans all eawf commands and drops the uv run eawf state directive, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the dispatch-renderer suite asserting the no-eawf-in-worktree out-of-scope clause replaces the state-mutation directive"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-03
    text: "The step-5 text names the token-tally handoff owner on both paths: the parent supplies --tokens-consumed on the interactive path, and the daemon converts the runtime delta at fleet.py:2360-2374 on the headless close-on-behalf path, so Wave.tokens_consumed has a named owner rather than going dark by prompt design."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the subagent-spec suite asserts step 5 names the parent-supplied and daemon-converted token-tally owners for both dispatch paths, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the subagent-spec suite asserting the token-tally handoff owner is named for the interactive and headless paths"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_subagent_spec_model.py", "tests/unit/test_dispatch_renderer.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
