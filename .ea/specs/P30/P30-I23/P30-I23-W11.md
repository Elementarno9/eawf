<!-- eawf-template: spec-wave -->

# Refuse daemon mutations for a mismatched state root

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W11

**Status:** DRAFT

**Scope:** P30-I23-W11

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A mutation RPC carrying a repo_root whose resolved state path differs from ctx.state_path is rejected with a typed 'wrong-state-root' error naming both paths and writes zero state, instead of the silent fallback at src/eawf/runtime/daemon/methods/state.py:184-195."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the test issues a mutation with a mismatched repo_root and asserts a wrong-state-root error with no state write"
    response:
      observe: exits
      object: "a mismatched-state-root mutation is refused with a typed error and no write"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "dispatch resume (the EP3 reproduction where a daemon bound to a smoke-fixture state mutated the fixture) is refused under the same state-root mismatch; read-only methods keep the fallback."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the test reproduces the EP3 dispatch-resume mismatch and asserts it is refused"
    response:
      observe: exits
      object: "the EP3 dispatch-resume mismatch is refused"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_state_root_guard.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
