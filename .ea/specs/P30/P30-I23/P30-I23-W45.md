<!-- eawf-template: spec-wave -->

# Add runtime options to core-loop skill engines

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W45

**Status:** DRAFT

**Scope:** P30-I23-W45

```eawf-wave-body
criteria:
  - id: CR-01
    text: "ctx.args reads land for prep auto_resume/out_of_order/ceremony/runtime, audit level/enforce, ship gauntlet/release/skip_pr_pass, and flow caps/max_repair_cycles at their engine modules (src/eawf/workflow/skills/{prep,audit,ship}.py, src/eawf/workflow/skills/flow/__init__.py), each with the section-4 default and a boundary plus error-path test (unknown value -> recorded warning or typed refusal). NO registry.py edits here."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the skill_prep/audit/ship/flow suites assert each engine reads its ctx.args option with a default and an unknown-value boundary refusal, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the core-loop skill-engine suites asserting the prep/audit/ship/flow ctx.args option reads with boundary and error-path tests"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "New config rows prep.auto_resume, audit.default_level, ship.gauntlet, and flow.max_repair_cycles exist in config_keys.py + leaf_catalog.py + defaults.py."
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: flexibility
    measurable_signal: "the skill-engine config suites validate the four new prep/audit/ship/flow config leaves load with their defaults, under pytest exit zero"
    response:
      observe: validates
      object: "the four new core-loop config leaves prep.auto_resume, audit.default_level, ship.gauntlet, flow.max_repair_cycles resolve against the layered-config schema"
      locus: schema
    gate_ids: [G-01]
  - id: CR-03
    text: "ship --release validates the emitted subject against the .github/workflows/phase-release.yaml:46 extraction regex and rejects any release= substring outside a standalone (release=v<X.Y.Z>) group; a test feeds the fused '(audit=..., release=v0.6.0)' shape and asserts refusal - the shape tools/commit_prefix_lint.py:409-421 passes unflagged today."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the skill_ship suite asserts --release refuses a fused (audit=..., release=v0.6.0) subject and accepts a standalone (release=v0.6.0) group, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the skill_ship suite asserting --release rejects a fused release= substring and accepts a standalone release group"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/", "-q", "-k", "skill_prep or skill_audit or skill_ship or skill_flow"]
      scope: touched
    policy: block
    cadence: every-wave
```
