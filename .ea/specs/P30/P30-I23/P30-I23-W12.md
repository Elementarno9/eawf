<!-- eawf-template: spec-wave -->

# Preserve enforcement for verdict-always waves in band narrowing

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W12

**Status:** DRAFT

**Scope:** P30-I23-W12

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A non-band, gateless, verdict-always wave resolves to enforce=True through resolve_wave_verify_block (src/eawf/workflow/verify/readiness.py:712-737) via a new third preservation arm keyed on verdict_requirement(wave)=='always'."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the test builds a non-band gateless verdict-always wave and asserts resolve_wave_verify_block returns enforce=True"
    response:
      observe: exits
      object: "a non-band gateless verdict-always wave resolves to enforce=True"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "A non-band mechanical (non-verdict-always) wave still narrows to enforce=False, so the low-risk narrowing is preserved and only verdict-always waves are held un-narrowed."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the test asserts a non-band mechanical wave still narrows to enforce=False"
    response:
      observe: exits
      object: "a non-band mechanical wave still narrows to enforce=False"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/workflow/verify/test_readiness.py", "-k", "narrow", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
