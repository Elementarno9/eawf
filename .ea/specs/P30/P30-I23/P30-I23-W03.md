<!-- eawf-template: spec-wave -->

# Make close-RPC timeout terminal and stamp fallback close mechanism

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W03

**Status:** DRAFT

**Scope:** P30-I23-W03

```eawf-wave-body
criteria:
  - id: CR-01
    text: "_wave_close_via_daemon (src/eawf/surfaces/cli/commands/lifecycle.py:325) re-raises a typed terminal error on TimeoutError from the close RPC with no in-process retry; the close does not silently fall through to the ungated in-process path."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the test monkeypatches the client to raise TimeoutError and asserts a non-zero exit with no state write"
    response:
      observe: exits
      object: "a close-RPC TimeoutError raises a terminal error and writes no state"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "A transport-fallback close (RuntimeError/OSError) is stamped with close_mechanism == 'daemon-fallback' in the event extras, distinct from a gate-passed daemon close; the CloseMechanism Literal (src/eawf/surfaces/cli/_mutation.py:69) carries the new value."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the fallback close event records close_mechanism 'daemon-fallback' in its extras under the test"
    response:
      observe: exits
      object: "a transport-fallback close event carries close_mechanism 'daemon-fallback'"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/cli/test_close_timeout_terminal.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
