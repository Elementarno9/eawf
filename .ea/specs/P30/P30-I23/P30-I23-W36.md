<!-- eawf-template: spec-wave -->

# Stage the v0.6.0 re-close ordering and hand off to I21-W22

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W36

**Status:** DRAFT

**Scope:** P30-I23-W36

```eawf-wave-body
criteria:
  - id: CR-01
    text: "P30-I21-W22.success_criteria carries kind != legacy rows with non-empty gate_ids (retyped via spec sync while W22 stays PENDING), and its gates include the release-annotation dry-run gated by the W32 test plus a structural criterion on the CHANGELOG section."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the staging checker asserts P30-I21-W22 criteria are non-legacy with non-empty gate_ids and include the release-annotation dry-run gate, under pytest exit zero"
    response:
      observe: exits
      object: "zero from tests/lint/test_reclose_staging.py asserting P30-I21-W22 carries typed gated criteria including the release-annotation dry-run"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The re-close runbook artifact is present with the drafted phase-close subject [P30] state: close iter + phase (audit=A-P30-I22-ship) (release=v0.6.0) matching the extraction regex (annotation as its own paren group) and the Windows-support de-scope line (per D-WINDOWS-DESCOPE), and the repo wave census is exactly one PENDING wave (P30-I21-W22)."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the staging checker asserts the runbook has the regex-matching subject and Windows de-scope line and exactly one PENDING wave (P30-I21-W22) remains repo-wide, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the staging checker asserting the runbook subject/de-scope line and the single-PENDING-wave census"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-03
    text: ">= 1 deterministic-pass evidence row dated within I22 appears in .ea/store/evidence.jsonl (this is where W34's CR-02 evidence row lands), confirming the phase's teeth metric is live before re-close."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the staging checker asserts a deterministic-pass evidence row dated within I22 exists in .ea/store/evidence.jsonl, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the staging checker asserting a deterministic-pass evidence row dated within I22 is present"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/lint/test_reclose_staging.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
