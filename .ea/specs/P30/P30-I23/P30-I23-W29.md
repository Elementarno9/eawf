<!-- eawf-template: spec-wave -->

# Canonicalize runtime labels at the dispatch_cost write site

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W29

**Status:** DRAFT

**Scope:** P30-I23-W29

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A dispatch_cost append built with input runtime label 'claude' persists the canonical label via canonical_runtime_label (src/eawf/kernel/store/kinds/events/base.py:46) wrapped at the dispatch_runner.py:700 payload construction, identical to a 'claude-code'-labeled append, so cost-by-runtime no longer double-counts one runtime as two."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the payload model test asserts a dispatch_cost append with input label 'claude' persists the same canonical label as a 'claude-code' append, under pytest exit zero"
    response:
      observe: exits
      object: "zero from tests/daemon/test_dispatch_cost_labels.py asserting the dispatch_cost append persists the canonical runtime label"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_dispatch_cost_labels.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
