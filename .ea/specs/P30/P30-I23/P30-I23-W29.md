<!-- eawf-template: spec-wave -->

# Canonicalize runtime labels at the dispatch_cost write site

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W29

**Status:** DRAFT

**Scope:** P30-I23-W29

```eawf-wave-body
criteria:
- id: CR-01
  text: "_runtime_or_none (src/eawf/observability/telemetry/sources/dispatch_cost.py) folds the adapter-id label claude-code onto the event triple claude via the un-idled runtime_triple_label helper (src/eawf/kernel/store/kinds/events/base.py), so both spellings of one runtime aggregate under one closed RuntimeName key instead of double-counting; the write site keeps the closed RuntimeTriple literal unchanged (a write-site claude-code wrap is rejected by DispatchCostPayload.runtime)."
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: "the labels suite asserts claude-code and claude fold to one RuntimeName key at the ingest seam, codex passes through, and junk coerces to None, under pytest exit zero"
  response:
    observe: exits
    object: "zero from tests/daemon/test_dispatch_cost_labels.py asserting both spellings fold to one closed key at the read seam"
    locus: pytest
  gate_ids:
  - G-01
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/daemon/test_dispatch_cost_labels.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
