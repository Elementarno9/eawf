<!-- eawf-template: spec-wave -->

# Add runtime options to research-family and review skill engines

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W46

**Status:** DRAFT

**Scope:** P30-I23-W46

```eawf-wave-body
criteria:
  - id: CR-01
    text: "research rounds/agents/budget are parsed in src/eawf/workflow/skills/research.py with --agents resolving through the existing idle research.agent_count leaf (config_keys.py:381, defaults.py:196); polish auto_apply_safe/category with new polish.auto_apply_safe and polish.deletion_policy keys; review level/criteria; agent-dispatch headless/model shaping next_valid_actions (no sandbox_profile); roadmap --dry-run and --criteria-from-brief parse. NO registry.py edits here."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the skill_research/polish/review/agent_dispatch suites assert each engine parses its runtime options and --agents consumes research.agent_count, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the research-family and review skill-engine suites asserting the parsed runtime options and the research.agent_count consumer"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "roadmap propose --dry-run renders the plan text plus coverage lint WITHOUT any state mutation - state.json is byte-identical before and after in the test."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the roadmap_dry_run suite asserts propose --dry-run renders plan and coverage lint while state.json stays byte-identical, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the roadmap_dry_run suite asserting --dry-run renders plan text with no state mutation"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/", "-q", "-k", "skill_research or skill_polish or skill_review or skill_agent_dispatch or roadmap_dry_run"]
      scope: touched
    policy: block
    cadence: every-wave
```
