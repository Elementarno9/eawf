<!-- eawf-template: spec-wave -->

# Re-render plugin trees and pin render goldens for new clauses

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W48

**Status:** DRAFT

**Scope:** P30-I23-W48

```eawf-wave-body
criteria:
  - id: CR-01
    text: "eawf plugin install claude plus the codex render regenerate the local trees, and eawf skill reconcile reports zero drift over the CLAUDE tree (missing_on_disk / extra_on_disk / flag_mismatches empty; src/eawf/surfaces/cli/commands/skill.py:791, root at :726-735); the codex mirror has no reconcile analogue and is covered by the golden gate below."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the skill-reconcile suite asserts a re-rendered CLAUDE tree reports zero drift across all three drift classes, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the skill-reconcile suite asserting the re-rendered CLAUDE tree reports no missing/extra/flag drift"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "Golden assertions confirm each new clause appears in a RENDERED SKILL.md, a RENDERED .claude/agents/executor.md, a rendered .codex/agents/executor.toml, and a rendered dispatch prompt - not only in the registry constants."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the cli-skill-render and plugin-install-codex goldens assert each new clause appears in the rendered SKILL.md, agent files, and dispatch prompt, under pytest exit zero"
    response:
      observe: renders_token
      object: "each new clause appears in the rendered SKILL.md, .claude/agents/executor.md, .codex/agents/executor.toml, and dispatch prompt goldens"
      locus: golden
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_skill_reconcile.py", "tests/unit/test_cli_skill_render.py", "tests/unit/test_plugin_install_codex.py", "tests/unit/test_prompt_rule_clauses.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
