<!-- eawf-template: spec-wave -->

# Read Claude hook stdin JSON in session_end and subagent_stop

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W27

**Status:** DRAFT

**Scope:** P30-I23-W27

```eawf-wave-body
criteria:
- id: CR-01
  text: The rendered claude hook.sh.j2 wrapper (src/eawf/platform/templates/claude/hook.sh.j2:14) reads stdin (payload from cat) and pipes the stdin JSON to eawf hook run session_end, argv-synthesis as empty-stdin fallback only; capture_runtime_on_session_end (src/eawf/runtime/hooks/runner.py:187) extracts a non-empty cost block from a recorded real Claude SessionEnd payload fixture through to the runtime.capture RPC params (runner.py:221).
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the stdin smoke test feeds a recorded real SessionEnd JSON through the wrapper contract and asserts a non-empty cost block reaches capture_runtime_on_session_end, under pytest exit zero
  response:
    observe: exits
    object: zero from tests/runtime/hooks/test_session_end_stdin.py asserting the stdin cost block reaches capture_runtime_on_session_end
    locus: pytest
  gate_ids:
  - G-01
  - G-02
- id: CR-02
  text: The subagent_stop wrapper carries the same stdin-first contract (payload from stdin, argv synthesis as empty-stdin fallback), keeping POSIX bash 3.2 compatibility.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the smoke test asserts the rendered subagent_stop wrapper pipes stdin JSON to eawf hook run with argv fallback only on empty stdin, under pytest exit zero
  response:
    observe: exits
    object: zero from the stdin suite asserting the subagent_stop wrapper carries the stdin-first contract
    locus: pytest
  gate_ids:
  - G-03
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/runtime/hooks/test_session_end_stdin.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-01
  kind: affordance_parity
  args:
    mode: home
  policy: block
  cadence: every-wave
- id: G-03
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/runtime/hooks/test_session_end_stdin.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
