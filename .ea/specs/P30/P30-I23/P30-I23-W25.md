<!-- eawf-template: spec-wave -->

# Drain P30-I20/I21 legacy criteria as the first corpus

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W25

**Status:** DRAFT

**Scope:** P30-I23-W25

```eawf-wave-body
criteria:
- id: CR-01
  text: 'Zero criterion rows with kind == legacy remain across P30-I20-* and P30-I21-W01..W21,W23 waves in state.json: each row is either converted to a typed kind or carries a named non-convertible reason (no silent legacy rows), verified by tests/lint/test_i20_i21_legacy_drain.py over committed state.'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: maintainability
  measurable_signal: the legacy-drain checker asserts no kind==legacy row survives in the I20/I21 wave range and every row is typed or reason-tagged, under pytest exit zero
  response:
    observe: exits
    object: zero from tests/lint/test_i20_i21_legacy_drain.py asserting no untyped legacy criterion rows survive in the I20/I21 wave range
    locus: pytest
  gate_ids:
  - G-01
- id: CR-02
  text: '.ea/config.yaml carries odr_blocking: true (the eawf repo opts into ODR blocking now that new-iter ODR is honest) and config validation passes against the layered-config loader.'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: maintainability
  measurable_signal: 'the drain checker asserts .ea/config.yaml has odr_blocking: true and the layered config validates clean, under pytest exit zero'
  response:
    observe: exits
    object: 'zero from the drain checker asserting odr_blocking: true is set in .ea/config.yaml and config validation passes'
    locus: pytest
  gate_ids:
  - G-02
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/lint/test_i20_i21_legacy_drain.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/lint/test_i20_i21_legacy_drain.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
