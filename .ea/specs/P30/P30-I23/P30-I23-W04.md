<!-- eawf-template: spec-wave -->

# Heartbeat held state locks and enforce a hold ceiling

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W04

**Status:** DRAFT

**Scope:** P30-I23-W04

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A state lock held longer than STALE_HEARTBEAT_SECONDS stays un-stealable because a background ticker refreshed heartbeat_at (src/eawf/runtime/lock/portalock.py:44-60), so stale.py (src/eawf/runtime/lock/stale.py:19,37-48) does not treat a live holder as stale."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the test compresses the interval via injection and asserts is_stale stays False under a live holder past the stale window"
    response:
      observe: exits
      object: "a live holder past the stale window remains un-stealable via heartbeat refresh"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "A hold_ceiling_exceeded WARNING log line carrying duration_s is emitted when a hold exceeds the configurable ceiling (default 120s); observability only, with no forced release (escalation is the W10 watchdog's job)."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the test asserts a hold_ceiling_exceeded WARNING with a duration_s field fires when the ceiling elapses"
    response:
      observe: emits
      object: "a hold_ceiling_exceeded WARNING carrying duration_s"
      locus: log_capture
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_lock_portalock.py", "tests/unit/test_lock_stale.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
