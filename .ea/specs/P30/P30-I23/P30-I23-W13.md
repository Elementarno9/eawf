<!-- eawf-template: spec-wave -->

# Scope the close floor pack to wave file_scopes

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W13

**Status:** DRAFT

**Scope:** P30-I23-W13

```eawf-wave-body
criteria:
- id: CR-01
  text: 'A floor-pack close of a wave scoped to one module honors scope: touched and does NOT execute the whole-tree pytest; the invocation carries only the scoped target set. readiness.py:1151-1154 builds floor views wiring the declared python.yaml:72-90 scope keys rather than running the whole-tree commands.'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: maintainability
  measurable_signal: the floor-pack integration suite asserts a module-scoped close never invokes the whole-tree pytest, only the scoped target set, under pytest exit zero
  response:
    observe: exits
    object: zero from test_readiness and test_floor_pack_integration proving a scoped close skips the whole-tree pytest
    locus: pytest
  gate_ids:
  - G-01
- id: CR-02
  text: 'A deliberately-broken file INSIDE the wave''s file_scopes still fails the floor pack (the falsifier survives scoping), and a policy: warn floor row stays genuinely non-blocking.'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: maintainability
  measurable_signal: a broken file inside file_scopes still fails the floor pack and a policy warn row stays non-blocking, both asserted under pytest exit zero
  response:
    observe: exits
    object: zero from the floor-pack suite proving an in-scope broken file still fails and warn rows do not block
    locus: pytest
  gate_ids:
  - G-02
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/workflow/verify/test_readiness.py
    - tests/workflow/verify/test_floor_pack_integration.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/workflow/verify/test_readiness.py
    - tests/workflow/verify/test_floor_pack_integration.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
