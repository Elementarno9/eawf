<!-- eawf-template: spec-wave -->

# Detect idle contracts via AST call sites and dynamic exercise

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W22

**Status:** DRAFT

**Scope:** P30-I23-W22

```eawf-wave-body
criteria:
- id: CR-01
  text: 'The idle-contract gate''s call-site probe is AST-based (call/import detection), so a comment or docstring mention no longer discharges NO_CALL_SITE: a symbol whose only non-test reference is a docstring is flagged NO_CALL_SITE, with convert_legacy_criterion (docstring-discharged despite zero real callers) as the fixture. Replaces the bare word-boundary regex _has_call_site at idle_contract_gate.py:2009 (MissingDischarge at :1736,1744-1745, assignment :2114-2116).'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: maintainability
  measurable_signal: the idle-contract suite asserts a docstring-only reference is flagged NO_CALL_SITE under the AST probe, with convert_legacy_criterion as the counterexample fixture, under pytest exit zero
  response:
    observe: exits
    object: zero from test_idle_contract_gate proving a docstring-only symbol no longer discharges NO_CALL_SITE
    locus: pytest
  gate_ids:
  - G-01
- id: CR-02
  text: 'A new MissingDischarge.NO_RUNTIME_OUTPUT plus a check_contract_exercised leg reads .ea/store/ for the bound contracts (I22''s three: auditor_report rows > 0 for the verdict producer, an actuals row with elapsed_eu > 0 for EU capture, gold_label rows for calibration), wired into phase-close pre-flight: the dynamic leg flags a bound contract with zero store output, passes once a row exists, and SKIPS the ballot contract while the ''jury convened >= 1'' predicate is false.'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: maintainability
  measurable_signal: the binding-probe suite asserts the dynamic leg flags a zero-output bound contract, passes once a store row exists, and skips the ballot contract while the jury-convened predicate is false, under pytest exit zero
  response:
    observe: exits
    object: zero from test_idle_contract_binding_probe proving the dynamic leg flags empty contracts and skips the ungated ballot contract
    locus: pytest
  gate_ids:
  - G-02
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/unit/test_idle_contract_gate.py
    - tests/unit/test_idle_contract_binding_probe.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/unit/test_idle_contract_gate.py
    - tests/unit/test_idle_contract_binding_probe.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
