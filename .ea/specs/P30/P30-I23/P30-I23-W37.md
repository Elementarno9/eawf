<!-- eawf-template: spec-wave -->

# Isolate researcher dispatch failure inside build_round_runner

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W37

**Status:** DRAFT

**Scope:** P30-I23-W37

```eawf-wave-body
criteria:
  - id: CR-01
    text: "build_round_runner isolates a raising spawn(dispatch): a round with one injected dispatch failure completes with the remaining domains' findings folded into RoundFindings and the failure recorded as a structured per-domain error, wired inside the _round_runner loop at src/eawf/kernel/spec/campaign_driver.py:244-247."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the regression test injects one raising dispatch and asserts the round completes with remaining domains' findings in RoundFindings and a structured per-domain error, under pytest exit zero"
    response:
      observe: exits
      object: "zero from tests/kernel/spec/test_campaign_driver.py asserting a round survives one raising dispatch with remaining findings folded in"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "A round where EVERY dispatch fails still raises ResearcherDispatchError (total failure is not silently swallowed as an empty round), so a wholly-dead round is surfaced rather than masked."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the regression test asserts a round in which every dispatch fails raises ResearcherDispatchError rather than returning an empty round, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the campaign-driver suite asserting an all-dispatch-fail round raises ResearcherDispatchError"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/kernel/spec/test_campaign_driver.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
