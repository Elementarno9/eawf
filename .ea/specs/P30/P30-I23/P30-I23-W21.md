<!-- eawf-template: spec-wave -->

# Repair the incident store-to-state fold with parity checks

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W21

**Status:** DRAFT

**Scope:** P30-I23-W21

```eawf-wave-body
criteria:
- id: CR-01
  text: 'Every distinct incident base-id in .ea/store/incident.jsonl is present as a key in state.json.incidents with matching open/closed status: a daemon backfill (open_incident/close_incident, incident.py:28/89) restores INC-P30-01/02/03/06 closed and INC-P30-08 open, reconciles INC-P30-07 citing the W02 I20 audit, and opens typed rows for the launchd auto-respawn, wrong-repo daemon bind, and suite live-state contamination classes.'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: reliability
  measurable_signal: the incident-fold suite asserts every distinct incident.jsonl base-id is a state.json.incidents key with matching status, under pytest exit zero
  response:
    observe: exits
    object: zero from test_incident_fold proving the store base-ids match the state incident map keys and statuses
    locus: pytest
  gate_ids:
  - G-01
- id: CR-02
  text: The doctor payload carries an incident-fold-parity check AND a backlog-fold-parity check asserting distinct store base-ids are a subset of the state map keys for incidents and backlog.
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: reliability
  measurable_signal: the incident-fold suite asserts the doctor payload includes incident-fold-parity and backlog-fold-parity rows, under pytest exit zero
  response:
    observe: exits
    object: zero from the fold suite proving both incident-fold-parity and backlog-fold-parity doctor rows are present
    locus: pytest
  gate_ids:
  - G-02
- id: CR-03
  text: 'A concurrency regression test pins the stale-cache clobber class: a CLI direct write followed by a daemon mutation preserves both writes (exercising the W09 under-lock re-read + before_version re-check), so a daemon close no longer wipes a prior CLI backfill.'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: reliability
  measurable_signal: the clobber regression asserts a CLI write then a daemon mutation preserves both writes with no lost rows, under pytest exit zero
  response:
    observe: exits
    object: zero from test_state_clobber_regression proving the CLI write survives a following daemon mutation
    locus: pytest
  gate_ids:
  - G-03
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/workflow/evidence/test_incident_fold.py
    - tests/daemon/test_state_clobber_regression.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/workflow/evidence/test_incident_fold.py
    - tests/daemon/test_state_clobber_regression.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-03
  criterion_id: CR-03
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/workflow/evidence/test_incident_fold.py
    - tests/daemon/test_state_clobber_regression.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
