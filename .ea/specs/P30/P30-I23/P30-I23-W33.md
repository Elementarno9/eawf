<!-- eawf-template: spec-wave -->

# Run a recorded live drive on the smoke fixture with jail smoke

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W33

**Status:** DRAFT

**Scope:** P30-I23-W33

```eawf-wave-body
criteria:
- id: CR-01
  text: 'The committed live-drive recording under .ea/artifacts/evidence/<date>-i23-live-drive/ passes tools/validate_drive_recording.py (all seven assertions: cost_usd > 0, elapsed_eu > 0, model set, canonical runtime labels, readable non-envelope tail lines, campaign terminal status, jail smoke pass markers); the fleet.drive run set explicit DriveParams caps (src/eawf/runtime/daemon/methods/fleet.py:407-414).'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the validator asserts the committed recording passes all seven drive-quality assertions (cost, EU, model, labels, tail, campaign, jail smoke), under exit zero
  response:
    observe: exits
    object: zero from tools/validate_drive_recording.py over the committed i23-live-drive recording
    locus: cli_exit
  gate_ids:
  - G-01
- id: CR-02
  text: '>= 1 deterministic-gate execution event appears in the recording''s close path, proving the W19 gate-executing clean close path fired live (>= 2 waves closed via the gate-executing path, one research campaign reached CONVERGED, exercising the W38 robustness fixes).'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the validator confirms at least one deterministic-gate execution event is present in the recording's close path, under exit zero
  response:
    observe: exits
    object: zero from the drive validator asserting a deterministic-gate execution event is in the recording close path
    locus: cli_exit
  gate_ids:
  - G-02
- id: CR-03
  text: The operator attests the drive ran against the hardened HEAD (daemon restarted post-W09/W10) with explicit eu_cap/usd_cap/waves_cap armed and zero manual pkill/lock-rm interventions; the report carries daemon uptime, the cap values, and the zero-intervention statement.
  kind: behavioral
  acceptance_style: binary
  evidence_kind: attested
  quality_dimension: functional_suitability
  measurable_signal: the operator attests the drive ran on hardened HEAD with explicit caps armed and zero manual interventions, with uptime and cap values recorded in the report
  response:
    observe: judged
    object: the operator's attestation of a capped, zero-intervention drive on the restarted hardened daemon
    locus: human
    jury_reason: a live one-shot drive against restarted HEAD is operational fact only the operator can attest; deterministic gates run over the committed recording, not the run
  gate_ids:
  - G-03
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/lint/test_i23_drive_recording.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/lint/test_i23_drive_recording.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-03
  criterion_id: CR-03
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/lint/test_i23_drive_recording.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
