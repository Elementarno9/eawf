<!-- eawf-template: spec-wave -->

# Bound juror and auditor spawns and parallelize the jury loop

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W08

**Status:** DRAFT

**Scope:** P30-I23-W08

```eawf-wave-body
criteria:
  - id: CR-01
    text: "Every adapter.spawn_session call reachable from _enforce_wave_close_gate passes an explicit finite timeout (config-resolvable, default 600s), covering both juror spawns (state.py:1065-1071) and the single-auditor producer (state.py:1312)."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the test injects a never-returning spawn stub and asserts the close errors within the configured ceiling"
    response:
      observe: exits
      object: "close-time spawns pass a finite timeout and the close errors within the ceiling"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "Three stub jurors with a per-juror sleep t convene concurrently via asyncio.gather at the cross_vendor_jury.py:771 loop, completing in wall time < 2t (jury wall time is max(juror), not sum)."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "three stub jurors each sleeping t complete in wall time under 2t proving concurrent convening"
    response:
      observe: exits
      object: "three jurors convene concurrently in wall time under 2t"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/eval/jury/test_cross_vendor_jury.py", "tests/unit/test_jury_discrimination_gate.py", "tests/daemon/test_wave_cross_vendor_jury_gate.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
