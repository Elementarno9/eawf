<!-- eawf-template: spec-wave -->

# Seed the jury gold-label cohort from known-bad waves

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W18

**Status:** DRAFT

**Scope:** P30-I23-W18

```eawf-wave-body
criteria:
- id: CR-01
  text: '.ea/store/gold_label.jsonl contains >= 20 schema-valid rows with >= 8 ''bad'' labels and every row''s reason non-empty. Labels are seeded via eawf jury label (W17): >= 8 known-bad from the reopen/reactive population (I15 W17-W20 reopen class, false-closed reactive-repair waves, INC-P30-01/03 incident-linked waves), the remainder known-good from clean audited closes such as P30-I20-W58; each reason names its evidence (reopen commit, incident id, or audit row).'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the gold-label-seed suite asserts >= 20 schema-valid rows, >= 8 with a 'bad' label, and every reason non-empty, under pytest exit zero
  response:
    observe: exits
    object: zero from test_gold_label_seed proving the committed store holds >= 20 rows, >= 8 bad, all reasons populated
    locus: pytest
  gate_ids:
  - G-01
- id: CR-02
  text: eawf metrics jury-validation reports a non-empty cohort (no longer honest-empty) while block authority remains ADVISORY pending P31 calibration; the seed does not flip any authority floor.
  kind: behavioral
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the seed suite asserts the jury-validation reader returns a non-empty cohort while block authority stays ADVISORY, under pytest exit zero
  response:
    observe: exits
    object: zero from the seed suite proving the cohort reads non-empty with block authority unchanged at ADVISORY
    locus: pytest
  gate_ids:
  - G-02
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/eval/test_gold_label_seed.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/eval/test_gold_label_seed.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
