<!-- eawf-template: spec-wave -->

# Execute deterministic gates on the fleet clean-close path

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W19

**Status:** DRAFT

**Scope:** P30-I23-W19

```eawf-wave-body
criteria:
  - id: CR-01
    text: "On the fleet clean path, before _close_wave_on_disk (fleet.py:2327, invoked at :2574 from _finish_lane at :2471), the wave's deterministic gates run via the W06 pre-flight library: a lane whose wave carries a FAILING command_exit_zero gate does NOT close on a clean self-report and instead routes to the existing repair/fork ladder. verify_close_readiness (dispatch_close.py:92) no longer resolves closed-on-report on a failing gate."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the fleet-close suite asserts a lane with a failing command_exit_zero gate never flips status on a clean self-report and enters the repair path, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the fleet-and-close selection proving a failing-gate lane routes to repair instead of closing"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "A lane whose wave carries a PASSING gate closes AND mints deterministic-pass evidence rows bound to the wave; MECH gateless waves keep today's status-flip behavior (their teeth arrive at plan time via W26). The gate execution reuses the W06 shared runner, not a re-implementation inside fleet.py."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the fleet-close suite asserts a passing-gate lane closes and mints deterministic-pass evidence rows bound to the wave, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the fleet-and-close selection proving a passing-gate lane closes and binds deterministic-pass evidence"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon", "-k", "fleet and close", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
