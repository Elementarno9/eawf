<!-- eawf-template: spec-wave -->

# Wire profile role-tier blocks into live dispatch prompts

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W41

**Status:** DRAFT

**Scope:** P30-I23-W41

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A resolve_role_blocks helper whose output (ComposedProfile.role_tier_blocks, src/eawf/platform/profiles/models.py:726) is passed as role_blocks= at all six production render sites: daemon src/eawf/runtime/daemon/methods/agent.py:1191, src/eawf/runtime/daemon/methods/fleet.py:810, plus the four W40 CLI sites; the zero-production-caller status of renderer.py:503 is discharged by real call sites, not a docstring."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the role-tier suite asserts resolve_role_blocks output reaches all six production render sites as role_blocks, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the role-tier suite asserting resolve_role_blocks is wired at all six production dispatch render sites"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "render_wave_prompt (src/eawf/workflow/dispatch/renderer.py:655) accepts and forwards role_blocks to build_subagent_spec, which today at :693 drops it."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the role-tier suite asserts render_wave_prompt accepts and forwards role_blocks into build_subagent_spec, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the role-tier suite asserting render_wave_prompt accepts and forwards role_blocks"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-03
    text: "The new dispatch.role_tier_token_cap config leaf (config_keys.py + leaf_catalog.py + defaults.py, default 2400) is read by the resolver, with DEFAULT_ROLE_TIER_TOKEN_CAP (src/eawf/platform/render_block.py:27) as code fallback; a boundary falsifier proves consumption: a ~2000-token block injects when cap=2400 and the same block raises RoleTierBudgetError when the leaf is lowered to 1200; over-cap stays raise-never-truncate (renderer.py:353-357)."
    kind: structural
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: flexibility
    measurable_signal: "the role-tier suite asserts a block under the configured cap injects and the same block over a lowered cap raises RoleTierBudgetError, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the role-tier suite asserting the role_tier_token_cap leaf is consumed via a pass/raise boundary falsifier"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_dispatch_role_tier.py", "tests/unit/test_role_contract_projection.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
