<!-- eawf-template: spec-wave -->

# Run the deterministic oracle tier off the daemon event loop

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W05

**Status:** DRAFT

**Scope:** P30-I23-W05

```eawf-wave-body
criteria:
  - id: CR-01
    text: "run_oracle wraps the run_checks invocation (src/eawf/workflow/verify/oracle.py:220) in await asyncio.to_thread, with no remaining synchronous subprocess call inside the async body, so the daemon event loop is not starved by deterministic gates."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the test drives run_oracle with a sleeping gate and asserts a concurrent task on the same loop makes progress"
    response:
      observe: exits
      object: "run_checks runs via asyncio.to_thread and the loop stays responsive under a slow gate"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/verify/test_run_oracle.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
