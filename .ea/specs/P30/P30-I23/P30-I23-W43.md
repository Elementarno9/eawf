<!-- eawf-template: spec-wave -->

# Add DoR/DoD contract blocks to the five agent registry bodies

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W43

**Status:** DRAFT

**Scope:** P30-I23-W43

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The spec section-5 blocks land in _EXECUTOR_BODY (agents.py:358), _OPERATOR_BODY (:502, 8 items including the schema-bump daemon-stop rule), _AUDITOR_BODY (:396), _PLANNER_BODY (:308), _RESEARCHER_BODY (:267) - the executor DoR carrying the grandfathered-legacy carve-out line and NO evidence_refs-required DoD bullet (that bullet is W49's) - and are propagated into a rendered dispatch prompt's ## Role contract > ### System prompt via ROLE_REGISTRY (roles.py:216)."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the render-claude-agents and prompt-rule-clauses suites assert the five DoR/DoD blocks appear in their bodies and in a rendered role contract, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the render-claude-agents and prompt-rule-clauses suites asserting the five section-5 blocks land and render in the role contract"
      locus: pytest
    gate_ids: [G-01, G-02]
  - id: CR-02
    text: "A token-accounting test in tests/unit/test_prompt_rule_clauses.py measures each enlarged AgentSpec body via the canonical count_tokens and asserts it against a pinned per-body budget derived from the spec section-6 table, so registry-body growth is deliberate and reviewed (the role-tier cap cannot police this: _enforce_role_tier_budget measures only injected profile blocks, renderer.py:397-402)."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the prompt-rule-clauses suite measures each enlarged AgentSpec body via count_tokens and asserts a pinned per-body budget, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the prompt-rule-clauses suite asserting each enlarged agent body stays within its pinned count_tokens budget"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_render_claude_agents.py", "tests/unit/test_prompt_rule_clauses.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: home
    policy: block
    cadence: every-wave
```
