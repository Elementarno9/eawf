<!-- eawf-template: spec-wave -->

# Persist juror ballots and ship the eawf jury label writer

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W17

**Status:** DRAFT

**Scope:** P30-I23-W17

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A convened stub jury persists one ballot row per juror to a new typed juror-ballot StoreKind, written by the convener (observability/eval/cross_vendor_jury.py) whenever jurors run, so calibration data stops being lost."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the jury-stores suite convenes a stub jury and asserts one ballot row per juror lands in the new ballot store, under pytest exit zero"
    response:
      observe: exits
      object: "zero from test_jury_stores proving a convened stub jury writes one ballot row per juror to the ballot store"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "eawf jury label <wave-id> --good|--bad --reason <text> (new surfaces/cli/commands/jury.py) appends a schema-valid GoldLabel row through a daemon mutation (rule 4) and the reader at observability/eval/jury_validation.py:83,188-203 loads it; the cohort builder consumes both the ballot and gold-label stores."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the jury-label suite asserts the CLI appends a schema-valid GoldLabel through the daemon and the reader reloads the row, under pytest exit zero"
    response:
      observe: exits
      object: "zero from test_jury_label proving the daemon-mutation append yields a reader-loadable GoldLabel row"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/eval/test_jury_stores.py", "tests/cli/test_jury_label.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
