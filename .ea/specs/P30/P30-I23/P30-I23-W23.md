<!-- eawf-template: spec-wave -->

# Add an ODR blocking flag behind profile config

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W23

**Status:** DRAFT

**Scope:** P30-I23-W23

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A new odr_blocking: bool = False on VerifyBlock (platform/profiles/models.py; profile yaml bump so every shipped yaml that sets verify keys carries the field under extra=forbid) makes close_iter raise LifecycleError when the iter's ODR is below odr_floor AND the flag is set, and warn-only when false, at the existing advisory seam _emit_iter_odr_advisory (iter_.py:192), with a mirrored advisory at phase close. Default OFF; the eawf repo flips it in W25 after the drain."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the iter-odr-gate suite asserts close_iter raises LifecycleError on a below-floor ODR when odr_blocking=true and only warns when false, under pytest exit zero"
    response:
      observe: exits
      object: "zero from test_iter_odr_gate proving close_iter blocks below-floor ODR only when odr_blocking is set"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/workflow/lifecycle/test_iter_odr_gate.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
