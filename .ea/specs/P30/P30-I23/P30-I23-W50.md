<!-- eawf-template: spec-wave -->

# Close-gate jury authority survives ballotless labelled cohort

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W50

**Status:** DRAFT

**Scope:** P30-I23-W50

```eawf-wave-body
criteria:
  - id: CR-01
    text: "_resolve_jury_block_authority (src/eawf/runtime/daemon/methods/state.py) resolves BlockAuthority.ADVISORY when the validation cohort is non-empty but the recorded-ballot substrate is empty, instead of propagating the labelled-wave-has-no-ballots ValueError into every enforcing close; validate_jury keeps its phantom-jury hard error for the calibration path where ballots are expected."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the authority-resolve suite seeds a settled auditor verdict into the silver cohort with no recorded ballots and asserts the resolver returns ADVISORY without raising, under pytest exit zero"
    response:
      observe: exits
      object: "zero from tests/daemon/test_jury_block_authority_resolve.py asserting a ballotless non-empty cohort resolves ADVISORY"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_jury_block_authority_resolve.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
