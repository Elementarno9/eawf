<!-- eawf-template: spec-wave -->

# Fix security-keyword word boundaries and auditor report ids

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W16

**Status:** DRAFT

**Scope:** P30-I23-W16

```eawf-wave-body
criteria:
  - id: CR-01
    text: "_is_security_scoped uses whole-word boundary regex per the spec_jury.py:300 pattern instead of the bare-substring match at verdict.py:230-231: a wave titled with 'AUTH-3' but no security-relevant word does NOT classify security-scoped, while 'harden the auth flow' still does."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: security
    measurable_signal: "the verdict suite asserts an 'AUTH-3' title without a security word is not security-scoped while 'harden the auth flow' is, under pytest exit zero"
    response:
      observe: exits
      object: "zero from test_verdict proving the word-boundary scan drops the AUTH-N false positive and keeps the real match"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "Produced auditor report ids match ^AR-auditor-(?!auditor-) so the role prefix appears exactly once, including when a caller supplies a role-prefixed base_id: the mint at agent_report.py:266 normalizes or rejects a role-prefixed base_id and append_agent_report (store.py:92-125) carries an id-shape assertion."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: security
    measurable_signal: "the agent-report-store suite asserts minted ids match ^AR-auditor-(?!auditor-) even when the caller passes a role-prefixed base_id, under pytest exit zero"
    response:
      observe: matches_pattern
      object: "an auditor report id carrying the role token exactly once for both plain and role-prefixed base_id inputs"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/workflow/dispatch/test_verdict.py", "tests/unit/test_agent_report_store.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
