<!-- eawf-template: spec-wave -->

# Extract the shared wave-close pre-flight verifier library

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W06

**Status:** DRAFT

**Scope:** P30-I23-W06

```eawf-wave-body
criteria:
  - id: CR-01
    text: "run_close_preflight is defined in src/eawf/workflow/verify/preflight.py and called from the daemon close pipeline in src/eawf/runtime/daemon/methods/state.py (the region at :2893-2928), bundling the gate-ref check, deterministic oracle tier, and floor-pack readiness compute as a pure read."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the suite imports run_close_preflight from preflight.py and asserts the daemon close pipeline calls it"
    response:
      observe: exits
      object: "run_close_preflight defined in preflight.py and called from the daemon close pipeline"
      locus: pytest
    gate_ids: [G-01, G-02]
  - id: CR-02
    text: "Close-gate behavior is unchanged across the existing close-gate suites: refusal on a failing gate, advisory pass-through, and waiver honoring all hold after the extraction (byte-identical enforcement)."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the close-gate and workflow-verify suites stay green proving refusal, advisory pass-through, and waiver honoring are unchanged"
    response:
      observe: exits
      object: "unchanged refusal, advisory pass-through, and waiver behavior post-extraction"
      locus: pytest
    gate_ids: [G-01, G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/daemon", "-k", "close_gate", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/workflow/verify", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
