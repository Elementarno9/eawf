<!-- eawf-template: spec-wave -->

# Secure the phase branch: commit CHANGELOG hunk and push to origin

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W01

**Status:** DRAFT

**Scope:** P30-I23-W01

```eawf-wave-body
criteria:
  - id: CR-01
    text: "git status --porcelain reports no pending CHANGELOG.md change and the committed [0.6.0] section carries a '### Fixed' block (the I21 fixed hunk), backing up phase-branch content that today lives only in the dirty working tree."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "git diff --quiet on CHANGELOG.md exits zero and grep finds a '### Fixed' block in the committed [0.6.0] section"
    response:
      observe: file_matches
      object: "CHANGELOG.md committed clean with a '### Fixed' block under the [0.6.0] section"
      locus: source
    gate_ids: [G-01, G-02]
  - id: CR-02
    text: "git ls-remote --heads origin feature/eawf-v0.6-p30 returns exactly the local HEAD SHA, proving the phase branch (all 632 P30 commits, today local-only) is pushed to origin with upstream set."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the origin phase-branch head SHA from git ls-remote equals git rev-parse HEAD under a bash test comparison exiting zero"
    response:
      observe: matches_pattern
      object: "origin phase-branch head SHA equals the local HEAD SHA"
      locus: cli_exit
    gate_ids: [G-02]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["git", "diff", "--quiet", "--", "CHANGELOG.md"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/lint/test_branch_backup.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
