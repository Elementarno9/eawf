<!-- eawf-template: spec-wave -->

# Close the jury OR-fold single-auditor bypass

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W07

**Status:** DRAFT

**Scope:** P30-I23-W07

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A verdict-always wave under merged cross_vendor_jury=True with ADVISORY authority routes through _produce_high_risk_verdict + _enforce_wave_verdict_gate (the src/eawf/runtime/daemon/methods/state.py:1547-1553 path) instead of the jury fallthrough, so the blocking single-auditor still fires."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "the test asserts an ADVISORY verdict-always wave routes to the single-auditor verdict gate, not the jury fallthrough"
    response:
      observe: exits
      object: "an ADVISORY verdict-always wave routes to the blocking single-auditor gate"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The same wave under BLOCKING authority (test-injected calibrated cohort) routes to the jury tier; the jury replaces the blocking single-auditor only when block_authority is BlockAuthority.BLOCKING."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "with an injected calibrated cohort granting BLOCKING authority the test asserts the wave routes to the jury tier"
    response:
      observe: exits
      object: "a BLOCKING-authority verdict-always wave routes to the jury tier"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/daemon", "-k", "close_gate and auditor or close_gate and jury", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
