<!-- eawf-template: spec-wave -->

# Run the waiver-free full-tree gauntlet with deterministic evidence

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W34

**Status:** DRAFT

**Scope:** P30-I23-W34

```eawf-wave-body
criteria:
  - id: CR-01
    text: "just test-all (justfile:24-26, the full CI mirror, not the fast just test tier that sets EAWF_SKIP_PERF=1) exits zero on the integrated HEAD with NO --waive; the coverage-gate parse and tools/snapshot_pairing_gate.py over the phase range also pass locally."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "just test-all exits zero on the integrated HEAD with no waiver, and the local coverage + snapshot-pairing gates pass, under exit zero"
    response:
      observe: exits
      object: "zero from just test-all plus the local snapshot-pairing and coverage gates on the integrated HEAD"
      locus: cli_exit
    gate_ids: [G-01, G-02, G-03]
  - id: CR-02
    text: ">= 1 evidence row with evidence_kind=deterministic, status=pass, and created_at >= the I22 open date appears in .ea/store/evidence.jsonl after this wave's close; the row is minted BY this close through the daemon gate, flipping the phase's teeth metric live for the first time since 2026-06-10."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: functional_suitability
    measurable_signal: "an evidence.jsonl row with evidence_kind=deterministic, status=pass, created_at >= 2026-07-02 is asserted present after this close, under exit zero"
    response:
      observe: exits
      object: "zero from the evidence-row checker asserting a deterministic-pass row dated within I22 exists in evidence.jsonl"
      locus: cli_exit
    gate_ids: [G-04]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "-n", "auto", "--ignore=tests/snapshots/tui", "--ignore=tests/perf/tui"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "-n0", "tests/snapshots/tui", "tests/perf/tui"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-03
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/lint/test_phase_ci_gates.py", "tests/unit/test_coverage_gate.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-04
    criterion_id: CR-02
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/lint/test_fresh_deterministic_evidence.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
