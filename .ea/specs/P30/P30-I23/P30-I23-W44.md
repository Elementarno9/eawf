<!-- eawf-template: spec-wave -->

# Document skill runtime options in argument hints and bodies

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W44

**Status:** DRAFT

**Scope:** P30-I23-W44

```eawf-wave-body
criteria:
- id: CR-01
  text: 'Every spec section-4 hint update lands in SKILL_REGISTRY: flow (:1577 gains --stop-after/--resume/--args-per-step/--caps/--max-repair-cycles), audit (:1461 gains --kind/--level/--enforce), prep (:1449), research (:1437), spike (:1563-1565), roadmap (:1517), ship (:1472 retains [--dry-run]), polish (:1494), review (:1483), agent-dispatch (:1611 gains --headless/--model, no --sandbox-profile), each matching its verified parse site (flow/__init__.py:27-37, audit.py:118-124).'
  kind: behavioral
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: maintainability
  measurable_signal: the skill-registry and skill-reconcile suites assert each section-4 argument-hint update lands with the correct options, under pytest exit zero
  response:
    observe: exits
    object: zero from the skill-registry and skill-reconcile suites asserting every section-4 argument-hint update
    locus: pytest
  gate_ids:
  - G-01
  - G-02
- id: CR-02
  text: 'A ## Options section in each touched body documents option semantics and defaults, including the _FLOW_BODY auto-accept model-executed note and the ship ## Options note that --dry-run is model-executed (the engine parses commit/push/pr at src/eawf/workflow/skills/ship.py:588-590).'
  kind: behavioral
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: maintainability
  measurable_signal: 'the skill-registry suite asserts each touched body carries a ## Options section documenting semantics, defaults, and the model-executed notes, under pytest exit zero'
  response:
    observe: exits
    object: 'zero from the skill-registry suite asserting the per-body ## Options sections and model-executed notes'
    locus: pytest
  gate_ids:
  - G-03
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/unit/test_skill_registry_p11.py
    - tests/unit/test_skill_reconcile.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-01
  kind: affordance_parity
  args:
    mode: home
  policy: block
  cadence: every-wave
- id: G-03
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/unit/test_skill_registry_p11.py
    - tests/unit/test_skill_reconcile.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
