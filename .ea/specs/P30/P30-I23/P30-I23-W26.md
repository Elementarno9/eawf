<!-- eawf-template: spec-wave -->

# Enforce a plan-time typed-criteria floor on wave authoring

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W26

**Status:** DRAFT

**Scope:** P30-I23-W26

```eawf-wave-body
criteria:
  - id: CR-01
    text: "plan_wave (src/eawf/workflow/lifecycle/wave.py:194) rejects a legacy-kind / untyped criterion set with a typed error naming the criteria floor; edit_wave_plan (wave.py:363) and the eawf roadmap revise --add-wave route (src/eawf/surfaces/cli/commands/roadmap.py:693) all land waves through this library-level floor."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the floor test drives plan_wave with a legacy-string criterion set and asserts a typed error naming the floor is raised, under pytest exit zero"
    response:
      observe: exits
      object: "zero from tests/workflow/lifecycle/test_wave_plan_floor.py asserting plan_wave rejects a legacy-kind criterion set with a floor-naming error"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The same plan_wave call succeeds when a typed criteria_floor_waiver (with a >= 20-char reason) is supplied, and the waiver lands as a typed record on the Wave so repair-burst authoring stays possible but visible."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the floor test asserts plan_wave with a criteria_floor_waiver succeeds and the persisted Wave carries the typed waiver record, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the floor suite asserting plan_wave with a criteria_floor_waiver succeeds and records the waiver on the wave"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-03
    text: "A criterion with evidence_kind=deterministic but no gate is rejected at authoring time by plan_wave / edit_wave_plan, closing the gateless-deterministic hole."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the floor test asserts a gateless evidence_kind=deterministic criterion is rejected at authoring, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the floor suite asserting a gateless deterministic criterion is rejected at authoring"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/workflow/lifecycle/test_wave_plan_floor.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
