<!-- eawf-template: spec-wave -->

# Ship the live legacy-to-typed criteria converter

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W24

**Status:** DRAFT

**Scope:** P30-I23-W24

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A new daemon mutation spec.convert_legacy (rule 4 canonical writer; spec.py has no convert verb today) converts a scope's legacy criterion rows to kind == 'converted' (CONVERTED_KIND at common.py:518) through the daemon transaction with the EAWF021 measurability lint applied, attaching falsifying gates where derivable and an honest evidence_kind otherwise. Reuses convert_legacy_criterion / backfill_legacy_criteria (common.py:580,703)."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the daemon convert suite asserts a fixture scope's legacy rows become kind=converted through the daemon transaction with the EAWF021 lint applied, under pytest exit zero"
    response:
      observe: exits
      object: "zero from test_spec_convert_legacy proving fixture legacy rows convert through the daemon with the measurability lint"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "The CLI proxy eawf spec convert-legacy --scope <P##|P##-I##|wave-id> [--dry-run] prints a per-row conversion report; --dry-run writes nothing while reporting the would-convert set."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the CLI convert suite asserts --dry-run writes no state while printing the would-convert set, under pytest exit zero"
    response:
      observe: exits
      object: "zero from test_spec_convert_legacy CLI proving --dry-run reports the would-convert set with no state write"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-03
    text: "The converter refuses a row it cannot make measurable, leaving it legacy with a named reason (no silent lossy conversion); tools/eawf021_measurable_criterion.py stays the fixture harness."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the convert suite asserts an unmeasurable row stays kind=legacy and carries a named refusal reason rather than a silent lossy conversion, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the convert suite proving an unmeasurable row is left legacy with a named refusal reason"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/daemon/test_spec_convert_legacy.py", "tests/cli/test_spec_convert_legacy.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
