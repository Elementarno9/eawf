<!-- eawf-template: spec-wave -->

# Require evidence refs in headless executor reports

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W49

**Status:** DRAFT

**Scope:** P30-I23-W49

```eawf-wave-body
criteria:
  - id: CR-01
    text: "_render_report_output (models.py:472-518) instructs one evidence_refs entry per success criterion (gate command plus exit code, file:line, or store URN); the 'Leave evidence_refs ... empty' sentence (:516-517) is removed and the schema evidence_refs: [] (:500) re-annotated (AgentReportCommonBody already accepts refs at agent_report.py:99, no model change). Same commit: _EXECUTOR_BODY gains its 'evidence_refs REQUIRED, one per criterion' DoD bullet, deferred out of W43."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the report_output suite asserts the schema requires one evidence_refs entry per criterion and _EXECUTOR_BODY carries the matching DoD bullet, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the report-output suite asserting the refs-required report schema and the matching executor DoD bullet land together"
      locus: pytest
    gate_ids: [G-01, G-02]
  - id: CR-02
    text: "A fourth rung in verify_close_readiness (src/eawf/workflow/verify/dispatch_close.py:92-141) refuses close-ready when a wave has >=1 typed criterion and evidence_refs is empty, gated behind the teeth-wave profile bit; boundary tests: a zero-criteria wave passes with empty refs, a criteria-bearing wave with refs passes, and a criteria-bearing wave without refs refuses with reason substring 'evidence_refs'."
    kind: behavioral
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: reliability
    measurable_signal: "the dispatch_close suite asserts the fourth rung passes zero-criteria and referenced waves and refuses an unreferenced criteria-bearing wave, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the dispatch_close suite asserting the evidence-refs close rung refuses an unreferenced criteria-bearing wave"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/", "tests/daemon/", "-q", "-k", "dispatch_close or report_output"]
      scope: touched
    policy: block
    cadence: every-wave
  - id: G-02
    criterion_id: CR-01
    kind: affordance_parity
    args:
      mode: home
    policy: block
    cadence: every-wave
```
