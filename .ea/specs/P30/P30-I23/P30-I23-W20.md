<!-- eawf-template: spec-wave -->

# Enforce verdict and deterministic gates on daemonless close

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W20

**Status:** DRAFT

**Scope:** P30-I23-W20

```eawf-wave-body
criteria:
- id: CR-01
  text: 'An EAWF_DAEMONLESS=1 close of a wave with a failing command_exit_zero gate exits non-zero with NO state write and NO waiver: the daemonless/in-process close runs the W06 pre-flight (deterministic gates + floor pack) and BLOCKS on a failing required gate, replacing the log-only close_advisory WARNINGs at lifecycle_wave.py:895-919 over the pure flip in wave.py:749.'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the daemonless-teeth suite asserts a failing-gate close exits non-zero and writes no state and stamps no waiver, under pytest exit zero
  response:
    observe: exits
    object: zero from test_daemonless_close_teeth proving a failing-gate daemonless close aborts with no state write
    locus: pytest
  gate_ids:
  - G-01
- id: CR-02
  text: The same close succeeds with an explicit waiver and stamps the waiver evidence row plus close_mechanism=daemonless-waiver, preserving the existing waiver door (--waive / --no-runtime, _mutation.py:464 daemonless-waiver stamping).
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the waiver suite asserts an explicit-waiver daemonless close succeeds with a waiver evidence row and close_mechanism=daemonless-waiver, under pytest exit zero
  response:
    observe: exits
    object: zero from the daemonless waiver suite proving the explicit-waiver path succeeds and stamps the waiver row
    locus: pytest
  gate_ids:
  - G-02
- id: CR-03
  text: 'A verdict-always wave is refused daemonless when no fresh auditor verdict is persisted: the synchronous verify_wave_verdict_gate (verdict.py:312) read-gate runs in-process and the refusal message says to close via the daemon or waive with a reason (the daemonless path cannot spawn the auditor).'
  kind: deterministic
  acceptance_style: binary
  evidence_kind: deterministic
  quality_dimension: functional_suitability
  measurable_signal: the daemonless-teeth suite asserts a verdict-always wave with no fresh persisted verdict is refused with a close-via-daemon-or-waive message, under pytest exit zero
  response:
    observe: exits
    object: zero from test_daemonless_close_teeth proving a verdict-always wave without a fresh verdict is refused daemonless
    locus: pytest
  gate_ids:
  - G-03
gates:
- id: G-01
  criterion_id: CR-01
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/cli/test_daemonless_close_teeth.py
    - tests/unit/cli/test_daemonless_close_waiver.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-02
  criterion_id: CR-02
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/cli/test_daemonless_close_teeth.py
    - tests/unit/cli/test_daemonless_close_waiver.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
- id: G-03
  criterion_id: CR-03
  kind: command_exit_zero
  args:
    argv:
    - uv
    - run
    - pytest
    - tests/cli/test_daemonless_close_teeth.py
    - tests/unit/cli/test_daemonless_close_waiver.py
    - -q
    scope: touched
  policy: block
  cadence: every-wave
```
