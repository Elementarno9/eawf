<!-- eawf-template: spec-wave -->

# Add rendered-prompt lint banning eawf commands in worktree sections

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W39

**Status:** DRAFT

**Scope:** P30-I23-W39

```eawf-wave-body
criteria:
  - id: CR-01
    text: "A new test in tests/unit/test_prompt_rule_clauses.py implements the two-scope lint: composer scope (executor-role fixture prompt, interactive and headless: the ## Workflow and ## Out of scope sections are free of 'uv run eawf wave close' and 'uv run eawf state') and registry scope (_EXECUTOR_BODY only; all other bodies allowlisted). A self-test feeds a deliberately-broken composer string and asserts the lint fails, proving it can reject a broken artifact."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the prompt-rule-clauses suite asserts eawf-command bans in worktree sections and fails on a deliberately-broken composer fixture, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the prompt-rule-clauses suite asserting the two-scope eawf-command lint and its refuse-broken-artifact self-test"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_prompt_rule_clauses.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
