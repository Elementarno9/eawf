<!-- eawf-template: spec-wave -->

# Harden the release-annotation lint and dry-run the W22 subject

**Spec URN:** urn:eawf:v1:spec:EAWF/P30/P30-I23/P30-I23-W32

**Status:** DRAFT

**Scope:** P30-I23-W32

```eawf-wave-body
criteria:
  - id: CR-01
    text: "The release-annotation lint (tools/commit_prefix_lint.py:134, check at :409-420) fires on ANY release= substring and fails unless it matches the standalone (release=vX.Y.Z) paren group with optional a/b/rc pre-release; the fused shape (audit=A-x, release=v0.6.0) is a hard reject naming the phase-release.yaml:46 workflow regex, while (audit=A-x) (release=v0.6.0) passes."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the extended lint test asserts the fused (audit=A-x, release=v0.6.0) is rejected with a workflow-regex message and (audit=A-x) (release=v0.6.0) is accepted, under pytest exit zero"
    response:
      observe: exits
      object: "zero from tests/unit/test_commit_prefix_lint.py asserting the fused release annotation is rejected and the standalone group accepted"
      locus: pytest
    gate_ids: [G-01]
  - id: CR-02
    text: "A dry-run test asserts the drafted W22 phase-close subject [P30] state: close iter + phase (audit=A-P30-I22-ship) (release=v0.6.0) matches the phase-release.yaml:46 extraction regex byte-for-byte and passes the lint, and that the fused BL-1 shape fails."
    kind: deterministic
    acceptance_style: binary
    evidence_kind: deterministic
    quality_dimension: maintainability
    measurable_signal: "the dry-run test asserts the drafted W22 subject matches the phase-release.yaml:46 extraction regex byte-for-byte and the fused shape fails, under pytest exit zero"
    response:
      observe: exits
      object: "zero from the lint suite asserting the drafted W22 subject matches the workflow extraction regex byte-for-byte"
      locus: pytest
    gate_ids: [G-01]
gates:
  - id: G-01
    criterion_id: CR-01
    kind: command_exit_zero
    args:
      argv: ["uv", "run", "pytest", "tests/unit/test_commit_prefix_lint.py", "-q"]
      scope: touched
    policy: block
    cadence: every-wave
```
