# P30-I17 iter-close audit (flow)

Fresh-context audit gating the close of iter P30-I17 ("Autopilot full-wire", 11 waves: W01-W10 planned + reactive W11), a BINDING pass that turns on built-but-idle autopilot machinery.

## Summary

Verdict: **minor** (pass with one low followup). The autopilot drive loop is now fully bound live; the audit's two real "binding incomplete" findings were fixed by reactive W11 before close.

Verified [1][2]:

- **Core binding is live** — background `fleet.drive` returns a handle and drains on a worker (W01); the arm spec (caps, hard_halt, kclean_k, scope) reaches DriveParams (W02); per-round frontier recompute (W04); watcher liveness probe + stall deadline (W05); `fleet.pause`/`halt`/`resume` registered with loop cooperation (W06); cockpit auto-reattach on mount (W07); needs_user fork + jury block-authority via the close-gate resolver + non-lane kill (W09); the W10 acceptance drives a budget-capped 2-of-3-wave drain and the tui_flow G-series extends to G20.
- **W11 closed the two dormant bindings the auditor caught** — the live `start_background_drive` now arms the bounded spawn ladder (DL-11) + repair ladder (DL-7) via `classify=_live_lane_error_classifier` + a real `LaneRepairHook` (was None on the live path, ladders fired only in tests); the live spawn threads `output_text=spawn_result.text` so the W08 stdout producer fires and the agent-watch tail is non-empty on a real run. `idle_contract_gate` gained two source-scan checks guarding both live bindings.
- **Idle-contract gate** — green ("no new contract ships idle"); all 18 registered audit-DSL kinds wired.
- **Tests** — `tests/daemon/` (634) + the fleet-live/flows/autopilot suites green; the only `-n auto` failures are the known shared-`.ea`-daemon config-validate flakes (pass in isolation). The W07-reattach stale autopilot fork-inbox test was fixed (reattach precedes resolve_fork).

Remaining followup (low, non-blocking): a future end-to-end integration test exercising the live grounded-repair re-verify (DL-7 `run_oracle` re-run) on a real multi-vendor host; W11 proves the binding is non-None and the built ladder fires, but the full grounded re-run is only reachable on a live multi-vendor spawn.

## References

| # | Reference |
|---|---|
| 1 | `src/eawf/runtime/daemon/methods/fleet.py`, `src/eawf/runtime/daemon/methods/agent.py`, `src/eawf/runtime/daemon/dispatch_runner.py` |
| 2 | `src/eawf/surfaces/tui/screens/overlays/arm.py`, `src/eawf/surfaces/tui/modes/autopilot.py`, `tools/idle_contract_gate.py`, `tests/daemon/test_fleet_autopilot_acceptance.py` |

## Provenance

Auditor: fresh-context `eawf:auditor` subagent (verdict pass-with-followups); the two real findings (W03 ladders + W08 output dormant on the live path) were remediated by reactive wave W11 and re-verified (idle-gate + tests green). Target: iter `P30-I17` on `feature/eawf-v0.6-p30`.

## Scrub

No machine paths, hostnames, credentials, or PII. References are repo-relative.
