# A62-P30-i05 — flow-audit (EUCAP harness EU instrumentation)

## Summary

Verdict: **minor**. Iter P30-I05 closed the EUCAP contract set (capture, attribute, enforce runtime EU) after three reactive remediation waves. The fresh-context audit passed 13/13 wave criteria (185 targeted tests green) with one substantive finding, W01(a): `_capture_runtime_baseline` was a permanent `return None` stub, so no production path stamped `Wave.runtime_baseline` and the close-time EU delta could never fire on a live wave — the enforce half (W08 zero-runtime gate) shipped live while the capture half stayed idle. W14 wired the real session-keyed sidecar read, closing that idle-contract gap. The polish sweep flagged one YAGNI nit (a dead `tokens_per_eu` parameter), fixed in W15. The full-tree `just test` gauntlet then surfaced nine more close-gate tests still red under W08's gate — fallout the scoped W08/W13 fixes missed — repaired in W16. Final tree: 16/16 waves closed, the full gauntlet at 10669 passed with only the 7 known config-validate shared-daemon contention flakes red (all 31 pass single-process), `pre-commit run --all-files` green including the idle-contract gate.

## Reactive fixes folded into the iter

- **W13** — re-pinned three `tests/daemon` gate fixtures (verdict, spec-jury, cross-vendor-jury) that W08's zero-runtime close gate had broken; pinned to `524b2750`.
- **W14** — wired claim-time baseline capture: `_capture_runtime_baseline(session_id)` resolves the session-keyed statusline cache path, reads the runtime-counter sidecar, and stamps a real `RuntimeBaseline`, returning `None` only on a genuine miss. Replaced the monkeypatch-only capture test with a real-sidecar read test plus a missing-sidecar test. Closes the W01(a) idle-contract finding. Pinned to `8546eeff`.
- **W15** — dropped the dead `tokens_per_eu` keyword param and its unreachable `<= 0.0` guard from `_tokens_to_eu`; the sole caller never overrode it and the eu_basis choice happens at the call site. Pinned to HEAD.
- **W16** — added `runtime_baseline`/`runtime_latest` fixtures to nine more close-gate tests across five `tests/unit/daemon` suites and one `tests/workflow/verify` seam that the full-tree gauntlet caught still red under W08. Pinned to HEAD.

All four reactive waves carry the runtime-zero close waiver (`--no-runtime`): the outer Claude Code harness session is not instrumented for these meta-waves, so no sidecar exists for their eawf claim sessions — the same EU=0-root the W10 spike already quantified.

## Checks

- `uv run pytest` over the EUCAP targeted suites (runtime baseline / counters / capture hook / sidecar / wave-close actuals / cli wave-close / state methods): 185 passed.
- `just test` full-tree gauntlet: 10669 passed, 33 skipped, 1 xfailed; 7 config-validate failures are the known shared-`.ea`-daemon contention flakes (31 pass single-process via `-p no:xdist`).
- `uv run mypy` over the changed lifecycle + telemetry modules: clean.
- `uv run pre-commit run --all-files`: all hooks passed, including `idle-contract-gate` (confirms the runtime close gate stays bound and the W14 wiring did not re-introduce an idle contract).

## References

- State and specs: `.ea/state.json`, `.ea/specs/P30/P30-I05/`.
- Capture wiring: `src/eawf/workflow/lifecycle/wave.py`, `src/eawf/runtime/runtime_counter_sidecar.py`, `src/eawf/runtime/runtimes/claude/statusline.py`.
- Enforce gate: `src/eawf/runtime/daemon/methods/state.py` (runtime.capture RPC + zero-runtime close gate), `tools/idle_contract_gate.py`.
- W10 wall-clock fallback spike: `.ea/artifacts/research/2026-06-10-P30-I05-W10-wall-clock-fallback.md`.

## Provenance

Produced by the `/flow` autonomous drive on 2026-06-10. A fresh-context auditor verified all 13 wave criteria and returned pass-with-followups on the W01(a) baseline stub; the operator chose to remediate before close, so W14 was added through `roadmap revise`, implemented, verified, and closed through the daemon. The polish sweep (W15) and the full-tree gauntlet remediation (W16) followed, then the consolidated gauntlet + pre-commit confirmed green.

## Scrub

- status: clean
- No absolute local paths, hostnames, credentials, or PII in this artifact. References are repo-relative.
