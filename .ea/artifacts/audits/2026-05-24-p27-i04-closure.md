# P27-I04 closure audit

## Summary

- P27-I04 (TUI richer views iter) delivered 31 waves: the braille/render-mode bar foundation, flush-right roadmap bar + fixed-width wave counter, the hybrid wave-row size/burn metric, the grouped status pane (LIFECYCLE/EFFORT/GATES/DISPATCH bands + EU/day velocity + ETA), the workspace per-repo and user-portfolio DataTables with Enter-zoom, the toast emitter + `ui.toasts` config key, the expand-curated config pane, and a closing W31 shared-markup helper. A fresh-context auditor verified every headline deliverable against its declared `success` criteria with file:line code evidence; all are met. Overall verdict **pass**: all criteria met, TUI + snapshot suites green, no followups [1].
- Audited HEAD: `aa1b2d9` on `feature/eawf-v0.3-p27` (W31 — shared markup helper, the last wave). Verification chain: one fresh-context auditor over the I04 deliverable surface in `src/eawf/tui/**` + the W01 daemon accrual point, plus the TUI suites on the integrated branch [1].
- **W01-W02 foundation — pass.** `dispatch_cost -> tokens_consumed` daemon accrual via `state.mutate.wave_accrue_tokens` (W01); braille bar renderer `render_bar_braille` + `RenderMode` literal + ASCII fallback, with the live toggle (`probe_braille_coverage` / `resolve_render_mode` / `watch_render_mode`) honoring `FONT_NO_BRAILLE` + `ui.glyphs` (W02) [2].
- **Roadmap surface (W03/W11/W18/W27/W30) — pass.** Flush-right bar pin `_pin_bar_right` + `_SCROLLBAR_GUTTER`, fixed 7-char wave-counter field, the hybrid `_wave_burn_bar` lighting the `effort_bucket` size bar when no live token-budget and auto-upgrading to token-burn, and the W30 fixed-width 2-char bucket label that aligns the glyph runs (regression-tested) [3].
- **Status pane (W04/W05/W14/W26) — pass.** Grouped LIFECYCLE/EFFORT/GATES/DISPATCH bands, `build_velocity_eu_per_day`, the `_eta_line` projection, and the DISPATCH NOW/NEXT/WAIT slice with band-label accent tinting [4].
- **Workspace + user scopes (W06/W07/W15/W19/W24/W25/W28/W29) — pass.** `WorkspaceTable` + `PortfolioTable` DataTables with `RowZoomed`; `RepoZoomMixin._enter_zoom` awaits `_clear_zoom_mount` (DuplicateIds race fix) + `on_screen_suspend` reset (cached-screen leak fix); the `z` zoom binding fully removed; `synthesize_user_state` from the global registry; breadcrumb keyed by explicit screen `scope` [5].
- **Toasts + config (W08/W09/W12/W13/W16/W17) — pass.** `ToastEmitter` with a pure `diff` + `ui.toasts` (off/important/all) gate; the config modal surfaces every `CONFIG_REGISTRY` leaf in alphabetical tabs incl. the curated `ui.glyphs` / `ui.dashboard_panes` / `estimation.display.*` / `telemetry.*` / `daemon.*` rows; the multichoice control extracted to `multichoice_checklist.py` under the LOC cap [6].
- **W31 shared markup helper — pass.** `eawf.tui.widgets.markup` (`escape_markup` backslash-escapes `[`, `style_labeled_line` tints the leading `label:` token) consumed by both `git_pane.py` and `status_pane.py`; 13 unit tests added; `repo_screen` + `roadmap_scrolled_long_iter` goldens refreshed in the same commit [7].
- Cross-cutting: rule-25 clean (no design-decision provenance in source); no TODO/FIXME/HACK or debug prints introduced across `src/eawf/tui/`; all 31 waves CLOSED in `state.json`.

## Followup triage

None. All deliverables present, cited, and test-backed; no gaps or regressions found.

## References

[1] `.ea/state.json` — `state.audits["A38-P27-i04"]` + `state.waves["P27-I04-*"]` outcome strings + the landed commit chain on `feature/eawf-v0.3-p27`; audited HEAD `aa1b2d9`; TUI + snapshot suites 888 passed (`pytest tests/tui/ tests/snapshots/tui/ -q`)
[2] `src/eawf/daemon/dispatch_runner.py` (`wave_accrue_tokens`); `src/eawf/tui/widgets/eu_bar.py` (`render_bar_braille`, `RenderMode`, ASCII fallback); `src/eawf/tui/app.py` (`probe_braille_coverage`, `resolve_render_mode`, `watch_render_mode`)
[3] `src/eawf/tui/widgets/roadmap_tree.py` (`_pin_bar_right`, `_wave_burn_bar`); `src/eawf/tui/widgets/eu_bar.py` (`render_completion_bar`, `render_size_bar`, fixed-width counter + bucket fields)
[4] `src/eawf/tui/widgets/status_pane.py` (`_SECTION_HEADERS`, `build_velocity_eu_per_day`, `_eta_line`, `build_dispatch_slice`, `_DISPATCH_BANDS`)
[5] `src/eawf/tui/widgets/workspace_table.py`; `src/eawf/tui/scopes/_zoom.py`; `src/eawf/tui/scopes/user.py`; `src/eawf/tui/widgets/header.py`
[6] `src/eawf/tui/toast_emitter.py`; `src/eawf/config/registry.py` (`ui.toasts` + curated leaves); `src/eawf/tui/screens/overlays/config_modal.py`; `src/eawf/tui/widgets/multichoice_checklist.py`
[7] commit `aa1b2d9 [P27-I04-W31]` + `src/eawf/tui/widgets/markup.py`, `src/eawf/tui/widgets/git_pane.py`, `src/eawf/tui/widgets/status_pane.py`, `tests/tui/test_widgets_markup.py`

## Provenance

- audit_id: A38-P27-i04
- audit_kind: evaluation
- scope_id: P27-I04
- verdict: pass (all 31 waves meet declared criteria with code evidence; TUI + snapshot suites green; no followups)
- created_at: 2026-05-24
- author: claude-opus-4-7 (session flow-p27i05)
- supersedes: none

## Scrub

- status: clean
- references: repo-relative paths + commit SHAs only
- local paths: none in body
- real emails: none
- abstract placeholder names: not applicable
