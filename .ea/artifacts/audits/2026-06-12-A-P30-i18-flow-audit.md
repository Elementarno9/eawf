# P30-I18 iter-close audit (flow)

Fresh-context audit gating the close of iter P30-I18 ("Research campaign live", 12 waves: W01-W09 planned + reactive W10-W12), a BINDING pass turning on the built-but-idle research campaign control plane.

## Summary

Verdict: **minor** (pass with all flagged followups resolved before close). The research campaign control plane is bound live end-to-end; every gap the audit + operator review surfaced was fixed by reactive waves W10-W12.

Verified [1][2]:

- **Binding is live across W01-W09** — round_runner spawns each StagedDispatch + parses typed `ResearcherReportBody` (W01); `research.add_question` + claim reconcile through the canonical writer (W02); `research.run` + per-round/checkpoint persistence + followup/snapshot RPCs (W03); operator-channel steer/broadcast/override + ChannelFold per D-2/D-3 (W04); Round projection + CampaignProgressState fold into `eawf status` (W05); EviBound synthesis -> promote_draft gating (W06); CLI question/status verbs + docs (W07); FA8 board live-wire, "not yet wired" literal retired (W08); a 2-round + mid-run-steer in-process acceptance (W09).
- **Audit followups RESOLVED by W10** — the auditor (verdict pass-with-followups) flagged two dormant bindings: `state.claims` populated only as a throwaway shadow, and `prune_round_carryover` with no production caller. W10 folded the round-end claim reconcile into real `state.claims` via the canonical writer and wired the L1 prune into the run loop; it also fixed three real close regressions (the new `research_round`/`operator_input` StoreKinds now register in the kinds registry + enum pin + autogen regen) and added idle-gate source-scans guarding both bindings.
- **Operator live-review fixes (W11, W12)** — W11 fixed a critical doctor-mode TUI crash (`OSError: Errno 9`): W29's process-global fd-0 redirect corrupted Textual's live stdin reader; replaced with per-subprocess `stdin=DEVNULL` at the four probe call sites. W12 vertically centered the ASCII-art seal across the autopilot/feed/agent_watch/evidence empty heroes (the empty-body `height:1fr` had top-pinned it) and the populated claims/unresolved board goldens were regenerated for the live-wired RUN band.
- **Idle-contract gate** — green; the two new campaign-run bindings (claim fold, prune) are guarded.
- **Tests** — the research/board/campaign/doctor suites pass; the only `-n auto` failures are the known shared-`.ea`-daemon config-validate flakes.

No remaining blocking followups. Low followup carried forward (P31): L2-L5 pruning reducers are reserved per the catalog.

## References

| # | Reference |
|---|---|
| 1 | `src/eawf/kernel/spec/campaign_driver.py`, `src/eawf/runtime/daemon/methods/research.py`, `src/eawf/kernel/store/kinds/` |
| 2 | `src/eawf/surfaces/tui/modes/research_board.py`, `src/eawf/surfaces/tui/widgets/empty_state.py`, `src/eawf/observability/doctor/checks.py`, `tools/idle_contract_gate.py` |

## Provenance

Auditor: fresh-context `eawf:auditor` subagent (verdict pass-with-followups); the two binding followups were remediated by reactive W10, and the operator's live-review findings (doctor crash, seal vertical alignment) by W11/W12, all re-verified green. Target: iter `P30-I18` on `feature/eawf-v0.6-p30`.

## Scrub

No machine paths, hostnames, credentials, or PII. References are repo-relative.
