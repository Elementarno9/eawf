# A-P30-I24 — iter-close evaluation: research-board redesign + evidence/watch UX

## Summary

Iter P30-I24 delivered the operator-requested research-board redesign plus the evidence/watch UX fixes surfaced by a pass-2 live review of the fixed (W24-W28) v0.6 TUI. Eleven waves closed: W01 tree-interaction spine (arrow-select, scroll-into-view, first-paint hanging indent), W02 campaign tree (pickable collapsible campaigns, status-before-title questions), W03 polymorphic detail pane (campaign stats / question answer / claim summary; in-tree peek retired), W04 evidence zoom modal (full-screen claim reader), W05 evidence-mode clarity (verdict-sigil legend, failed-wave drill row, researcher detail), W06 watch roster (list key + picker decouple), W07 interaction-liveness gate (asserts selection moved, not just repaint), plus four ship-prep polish waves W08 (swap the tofu gate sigil U+2394 -> U+25C8), W09 (deterministic help-overlay snapshot), W10 (broad-font-stack review SVG export), W11 (empty-fixture research-board Enter liveness exemption).

Verdict: **pass**. The redesign is spec-complete against the operator's locked design (campaign tree + context-sensitive right pane + full-screen evidence modal, self-teaching flow), verified green and rendered visually.

## Evidence

- **Full merged-state suite green.** `uv run pytest tests/tui/ tests/snapshots/tui/` on the merged branch: 2959 passed after the one merged-state regression (research-board Enter liveness) was root-caused and fixed in W11. Each wave additionally passed its own scoped Pilot/unit tests and `pre-commit run --all-files` in-worktree.
- **W03 (L-effort, high-risk) independently audited.** A fresh-context auditor verified all three W03 criteria in-code (campaign stats render, question-scoped answer with an absent-unrelated-claim assertion, in-tree peek fully removed) with no W01/W02 regression; verdict pass, seeded to the auditor-report store per the verdict-always gate.
- **Visual verification.** The redesigned board was rendered live against the smoke fixture (Pilot export -> resvg PNG in JetBrainsMono Nerd Font): campaign-focus stats, question-focus scoped answer, the full-screen evidence modal (body + numbered sources + conflicts + provenance), and the evidence-mode verdict legend + role column all render correctly. The one tofu glyph found in review (U+2394) was fixed in W08 and re-verified via resvg.
- **Gate hygiene.** sigil-totality, snapshot-pairing, and the W07/W28 interaction-liveness gates are green; goldens repinned render-fresh in paired `test:` commits throughout.

## References

| N | Reference |
|---|-----------|
| 1 | Pass-2 root-cause + locked design brief: `.ea/local/research/2026-07-03-tui-ux-review-pass2.md` |
| 2 | Per-wave close outcomes + commit SHAs on `feature/eawf-v0.6-p30` (`git log --grep '[P30-I24-'`) |
| 3 | W03 independent auditor verdict: `.ea/store/auditor_report.jsonl` (scope P30-I24-W03) |

## Provenance

Compiled by the 2026-07-04 /flow session at eawf HEAD on `feature/eawf-v0.6-p30`, from the merged-state full-suite run, the W03 independent audit, and the live rendered-image review of the redesigned board.

## Scrub

Repo-relative paths only; the smoke-fixture path referenced in the source review is session-local. No PII, no absolute local paths.
