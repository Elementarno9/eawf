# A52-P29-i09 — flow-audit (UI/UX review follow-ups)

## Summary

Verdict: **minor** (pass-with-followups). Iter P29-I09 delivered all eight UI/UX review-follow-up waves against their success criteria, plus two reactive waves (W09 autogen-tree repair, W10 polish), with one non-blocking out-of-scope followup (backlog B094).

A fresh-context auditor verified each criterion refute-first against the HEAD working tree and the per-wave pinned commits, paying special attention to the universal-quantifier criteria ("every", "all ten surfaces", "no control routes through a stub") where partial delivery hides. The parent then ran the full pytest suite (9725 passed, 1 environment-only zsh-completion failure, 1 autogen-drift failure repaired in W09).

This iter closes the v0.5.0 phase (P29): I09 is the phase-co-closing iter, so its close bundles the phase close.

## Delivered

The eight UI/UX review follow-ups from the I08 live review (A51), plus two reactive waves:

- W01 — `modal_depth` counts only `counts_toward_depth` modals (brief viewer exempt via `counts_toward_depth=False`) and `MAX_MODAL_DEPTH` is raised to 6, so a brief plus five reference drills never trips the cap toast; `ReferenceModal` and other overlays still count.
- W02 — entity dedup consolidated at the `push_modal` chokepoint via `DetailModal.dedupe_key` (top-only, no toast); the W25 per-path pre-check folded in; `/find` carries `entity_id`, so re-choosing the modal-top entity is a no-op on the row-drill, `/find`, and choose-in-modal paths.
- W03 — `CANONICAL_HINT_ACTIONS` pins one action per shared footer token (arrow=`select`, `Enter`=`open`, `Esc`=`back`, `w/r/u`=`scope`, `F5`=`refresh`, `c`=`config`, ...); `render_hint_label` raises on a drifted shared-token action; all ten footer surfaces unified.
- W04 — a frozen `HINT_KEY_PRIORITY` plus an idempotent `order_hints` chokepoint in `Footer.set_hints` injects `c config` + `F5 refresh` and orders every surface identically; an enumeration test pins the canon order and shared-key positions across all ten surfaces.
- W05 — `agent.pause` / `agent.resume` persist `State.dispatch_paused` through the canonical writer; `claim_wave` blocks the next dispatch when paused (regardless of `out_of_order`); the autopilot space key toggles a real paused/resumed verdict; schema migrated 1.5 -> 1.6 (additive `dispatch_paused`) with a registered `v1_5_to_v1_6` step.
- W06 — the not-yet-wired `_issue_unwired` stub mechanism is removed from autopilot and agent-watch; skip is a real cheap local frontier-advance; arm is honestly labeled deferred (no doomed round-trip); kill/halt keep the real `agent.kill` placeholder honestly labeled on both surfaces.
- W07 — `eawf research campaign new` stages via `stage_campaign` and persists a `ResearchCampaignPayload` through the new daemon `research.create_campaign` RPC (shared `persist_campaign` helper, daemon-proxy-then-direct-append fallback); the row surfaces as a campaign node in the TUI research board.
- W08 — `HelpScreen` auto-derives a per-mode action-key section from each mode's own `BINDINGS` (inherited chrome stays in the Global section) plus an `alt+left` / `alt+right` reference-nav section; a coverage test asserts every own binding has a help row.
- W09 (reactive) — regenerated `docs/reference/autogen/schema.md` + `state.schema.json` via `generate_all` to match the schema 1.6 model, so the autogen drift gate passes (W05 had regenerated only the bundled `src/` schema copy).
- W10 (reactive, polish) — corrected the footer surface-count comment (eleven -> ten) and confirmed via a full-diff sweep that the five executors' work is already consistent on log format, error phrasing, `Raises:` blocks, naming, and ASCII.

## Anti-drift verdict

Each criterion is genuinely met (Tier-1: the deliverable exists and works), verified refute-first against the source, not the commit messages. The universal-quantifier criteria were confirmed exhaustively:

- W03/W04 cover all ten footer surfaces (three scope screens plus seven mode screens, all `ScopeScreen` subclasses routing through the single `render_hint_label` / `order_hints` chokepoints), pinned by enumeration tests.
- W06's `_issue_unwired` / `_call_unwired` / `_NOT_WIRED_TEMPLATE` stubs are gone from both autopilot and agent-watch (grep returns zero matches); skip/arm/kill behave as the criterion demands.
- W08's coverage test, driven off `MODE_REGISTRY`, asserts every per-mode own binding has a help row.

Where the recorded `file_scopes` were thinner than the criterion (W03's "all ten surfaces" listed only three files; W05's gate spanned `wave.py` + migrations beyond its three listed files; W07's registration needed `server.py`), the parent widened the wave `file_scopes` to match the real contract before dispatch, per the brief-fidelity anti-drift discipline, rather than letting the criterion silently thin.

## Followups (non-blocking)

- B094 (P3) — `src/eawf/surfaces/tui/modes/research_board.py` retains the same not-yet-wired stub set (`_issue_unwired` / `_call_unwired` / `_NOT_WIRED_TEMPLATE`) for its `r follow-up` and `s snapshot` verbs. This is OUT of W06's scope (autopilot + agent-watch only) and predates I09; it is a candidate for a future "make research-board controls honest" wave applying the W06 pattern.
- The footer surface-count comment drift the auditor noted was fixed in W10.

## Honest-negative / known-failure integrity

The audit pass was read-only; it mutated no state. The one remaining suite failure (`tests/cli/test_completion_runtime.py::test_installed_zsh_script_completes_in_live_zsh`) is environment-only: the test runs the globally-installed `eawf` binary (`shutil.which("eawf")`), which is stale and predates zsh-completion support, while the current branch code emits a valid zsh script. CI installs from the branch, so the gate passes there; this matches the A49/A51 precedent classifying it as a sandbox limitation, not a regression. The autogen-tree drift that surfaced in the full-suite run was a real gap (W05 regenerated only the bundled `src/` schema copy, not the second committed `docs/reference/autogen/` copy) and was repaired in W09. The CI-only coverage + snapshot-pairing gates fire on the phase PR at ship.

## References

- Originating review: `.ea/artifacts/audits/2026-06-03-A51-P29-i08-flow-audit.md` (the I08 live UI/UX review that recorded the I09 follow-up waves)
- Wave commits: `git log --grep '[P29-I09-W'` on `feature/eawf-v0.5-p29`
- Schema migration: `src/eawf/kernel/migrations/v1_5_to_v1_6.py`
- Followup: backlog B094

## Provenance

Produced by the `/flow P29-I09` audit stage on 2026-06-04. A fresh-context auditor ran the criteria-vs-code and anti-drift lenses over the I09 diff and the per-wave pinned commits, then ran the named verifying suites green; the parent ran the full pytest suite. The two reactive waves (W09 autogen repair, W10 polish) were appended to I09 per the append-don't-open-a-second-iter discipline. No state was mutated by the read-only audit; this report records the verdict that gates the I09 close and the P29 phase close.

## Scrub

- status: clean

No absolute local paths, hostnames, credentials, or PII. All references are repo-relative paths or Eawf ids.
