# P27-I03 closure audit

## Summary

- P27-I03 (hygiene + build-out iter) delivered 26 waves via full-auto DAG execution: W01-W23 functional + W27 (`iter plan` verb, staged pre-iter) + two in-flight reactive fix-waves (W24, W25). W18 and W20 were re-scoped mid-flight (operator-approved) from single-file stubs to their true cross-codebase footprints. A fresh-context auditor verified every wave against its declared `success_criteria`; all 26 are met with code evidence. Overall verdict **minor**: all criteria met, full gauntlet green (pre-commit 14 hooks, `mypy src/` 472 files, `pytest tests/ -q` 5941 passed / 27 skipped / 1 xfail), two non-blocking cosmetic followups deferred [1].
- Audited HEAD: `5a25fda` on `feature/eawf-v0.3-p27` (W17 land — the last functional wave). Verification chain: one fresh-context auditor over all 26 waves' diffs + declared criteria, plus the post-DAG gauntlet on the integrated branch [1].
- **W18 — remove 8 deprecated `CliError` subclass aliases — pass.** The legacy aliases (`NotFound`/`InvalidInput`/`ValidationFailed`/`LockConflict`/`InstrumentMissing`/`UserDeclined`/`IntegrityViolation`/`HookBlocked`) are removed and ~132 raise sites + 31 except/isinstance + 38 imports across 118 files migrated to the canonical five-bucket class + `kind=` tag; `build_envelope` folds `err.kind` into `data.kind`, so exit codes + envelope shape are byte-unchanged; zero alias class references remain in `src/` + `tests/` [2].
- **W17 — reduce grade-D functions — pass.** All 15 grade-D functions decomposed into single-responsibility helpers; `radon cc -n D src/eawf` returns empty; behaviour preserved (suite count unchanged) [3].
- **W20 — rename `eawf.tui_v2` to `eawf.tui` — pass.** 78 `git mv` renames + import-token sweep including the load-bearing `spec/heuristics.py` `UI_SCOPE_PREFIXES` literal and the coverage-gate key; zero `tui_v2` references remain outside `.ea/` history [4].
- **W21 — enforce SandboxPolicy at dispatch — pass.** The deny-list is unioned (wave + global scope) and subtracted from the dispatch `allowed_tools` projection, so a wave with a denied tool cannot dispatch it [5].
- **Profile / render cluster (W01-W08) — all met.** Structured `RenderBlock` triad + XOR validator (W01); opt-in `quality.yaml` (W02) + `agent_driven` (W03) profiles; cognitive-complexity gates EAWF011 + ruff C901 with EAWF010 demoted to an advisory coarse alarm (W04); prose-rule dedup into the python profile + daemon-authority consolidation into rule 4 (W05); ml/quant/apps/infra stub rule sets (W06); bounded-title consumers + entity-description surface (W07); entity-title naming rule + `lint_entity_title` backstop (W08) [6].
- **Dispatch / daemon cluster (W09-W14) — all met.** `run_dispatch` production caller + live-daemon E2E (W09); typed `agent_end` reports validated by `check_agent_report_invariants` (W10); wave to IN_PROGRESS at implementation-start, dispatched + inline (W11); six model-only code-quality skills (W12); the four god-method `action()`s decomposed onto a shared `SkillAction` base in `skills/engine.py` (W13); typed `SubagentSpec` library + role registry rendering to all three kept runtimes (W14) [7].
- **Remaining build-out + hygiene (W15, W16, W19, W22, W23) — all met.** `/roadmap apply` prefills the PENDING wave DAG before an AUQ gate (W15); skill Decision-surfaces normalized (W16); state-write helpers lifted from the CLI into `eawf.state.io` behind a `StateValidationError` boundary (W19); publish-not-block mutmut campaign wired with a documented >=70% target (W22); contract tests for runtime resolution + daemon JSON-RPC, pinning the OpenCode-hooks gap via xfail (W23) [8].
- **Reactive fix-waves — both met.** W24 added `plan` to `EXPECTED_ITER_VERBS` (fallout from the W27 `iter plan` verb); W25 made the path-leak scan skip documented angle-bracket / ellipsis home-directory placeholders, symmetric with the existing email-scan placeholder skip, unblocking W05 without weakening real-leak detection [9].
- Cross-cutting: rule-25 clean (no design-decision provenance in source); new ingestion models ride `extra="forbid"`; the behaviour-preserving refactors (W13/W17/W19/W20) leave the suite count unchanged.

## Followup triage

Two non-blocking followups are deferred; neither gates the I03 close.

- **DEFER — W18 docstring alias-name prose.** ~40 `:class:` / `Raises:` docstring references still name removed alias classes (e.g. `NotFound`) as prose. Confirmed non-blocking: zero occurrences in mkdocs pages, and `eawf doc verify --strict` runs manual-stage only (not in the gauntlet). Queue a docs-polish sweep to rephrase to the bucket + `kind=` form [2].
- **DEFER — W22 mutmut score-pending.** The mutmut import-trampoline corrupts pydantic-core compiled validators, so the campaign stats phase aborts; the CI job is `continue-on-error` and the publish step tolerates a missing score, so CI never reds. Wiring + the documented >=70% target are in place; the score populates after runner tuning [8].

## References

[1] `.ea/state.json` — `state.audits["A37-P27-i03"]` + `state.waves["P27-I03-*"]` outcome strings + the landed commit chain on `feature/eawf-v0.3-p27`; audited HEAD `5a25fda`; full gauntlet 5941 passed / 27 skipped / 1 xfail (`pytest tests/ -q`), `mypy src/` clean on 472 files, pre-commit 14 hooks green
[2] commit `34d6b44 [P27-I03-W18]` + `src/eawf/cli/errors.py` (alias defs removed; `build_envelope` `kind=` fold); `.pre-commit-config.yaml` (`eawf-doc-verify-strict` is manual-stage)
[3] commit `5a25fda [P27-I03-W17]`; `radon cc -n D src/eawf` empty
[4] commit `5303e53 [P27-I03-W20]` + `src/eawf/tui/**`, `src/eawf/spec/heuristics.py` (`UI_SCOPE_PREFIXES`), `pyproject.toml` (`tool.eawf.coverage.gates.tui`)
[5] commit `a435ee1 [P27-I03-W21]` + `src/eawf/dispatch/renderer.py`, `src/eawf/sandbox/policy.py`
[6] commits `cffe9e8 [W01]`, `ebad948 [W02]`, `b82efd9 [W03]`, `3a3472c [W04]`, `9d0a420 [W05]`, `bf3a9bc [W06]`, `5532500 [W07]`, `ca753c1 [W08]`
[7] commits `010fa6c [W09]`, `cfa59a5 [W10]`, `992c059 [W11]`, `097c174 [W12]`, `a84c8ac [W13]`, `72bbb78 [W14]`
[8] commits `4572ec2 [W15]`, `31ae879 [W16]`, `0e269bb [W19]`, `48f054d [W22]`, `da80910 [W23]` + `docs/reference/mutation-testing.md`
[9] commits `94dcd86 [W24]`, `9009216 [W25]` + `src/eawf/cli/commands/hook.py`

## Provenance

- audit_id: A37-P27-i03
- audit_kind: evaluation
- scope_id: P27-I03
- verdict: minor (all 26 waves meet declared criteria with code evidence; full gauntlet green; two non-blocking cosmetic followups deferred)
- created_at: 2026-05-23
- author: claude-opus-4-7 (session eawf-flow-p27-i03)
- supersedes: none

## Scrub

- status: clean
- references: repo-relative paths + commit SHAs only
- local paths: none in body
- real emails: none
- abstract placeholder names: not applicable
