# A44 — P29-I01 flow-audit (spawn-foundation iter)

## Summary

Fresh-context audit of iter P29-I01 (the spawn-free foundation iter of v0.5.0), covering all 29 landed waves (W01-W26 planned + W27-W29 reactive) on branch `feature/eawf-v0.5-p29` against base `main`.

Verdict: **minor** (pass-with-followups). 51 success-criteria MET, 3 PARTIAL, 0 UNMET, 8 deferred-by-design across ~56 sub-criteria. No blockers, no cross-wave regressions. Full suite green (7766 passed, 27 skipped, 1 xfailed; the 14 `test_mutation_proxy.py` failures are an `EAWF_DAEMONLESS=1` artifact and pass 15/15 normally).

The EviBound Keystone-B chain is coherent at every link and the routing landmine was avoided: W07 `resolve(ref)` routes on `CriterionEvidenceKind` (not `EvidenceKind`), W08 wires rung-1 (`compile_gate -> _run_deterministic_gate -> run_checks`, passed == returncode 0) into promotion and un-idles `IntentBrief.evidence_refs`, W09 adds the refute-first in-process rung-2 NLI (pluggable, no torch dep, documented <0.7 escalation contingency), and W10 adds the rung-4 attested render plus the closed verdict ladder (renamed to `EviBoundVerdict` by polish wave W29 to resolve a rule-17 collision with `agent_report.CriterionVerdict`).

Naming conventions hold across the changed set (canonical `agent_role` / `effort_bucket` / `scope_id` / `evidence_kind`; bare log keys; lowercase no-trailing-period error phrasing; Google `Raises:` blocks). No design-decision references in source comments (rule 25 / eawf012 clean). No abandoned-branch (`poc/llm-assist`) provenance leaked into W21. Schema + autogen goldens regenerated by W28 so all drift gates pass.

The 3 PARTIAL criteria are the W02 pre-claim spike's honest negative (it disconfirmed the presumed `dispatch_cost.session_id` 1:1 mapping — the spike did its job) and the W08 / W22 deliverables whose live wiring is deferred by design to later iters. All 8 deferred-by-design items are genuine foundation-iter scope boundaries with documented contingencies, not silent gaps.

## Followups

Carried to later iters (none block I01 close):

- W02: per-session EU attribution remains unsolved — `dispatch_cost` rows key on `wave_id`/envelope-id, not `session_id`. When the metering writer is wired (W22 deferral, ~I03) confirm the session-cost path does not assume the 1:1 the spike refuted. The W02 success-criterion text asserts a confirmation the code correctly refutes; reword on the next scope revision.
- W19: the shared `Urgency` ladder has two carriers (pause via W17, `OpenQuestion` via W19) but no `AttentionFeed` sorter that ranks them on the comparable scale — build it in the iter that surfaces the operator attention queue. `UserQuestion` body carries no `urgency` field (it lives on the pause envelope); note for any consumer that expects `UserQuestion.urgency`.
- Polish (non-blocking, deferred): the pure-reducer spec siblings split dataclass-vs-Pydantic inconsistently — `RoundLoopResult` embeds the `SaturationReport` dataclass via `arbitrary_types_allowed=True`, loosening nested validation; align the campaign-reducer family on one choice. The rung-3 jury reducer's `JuryAggregateOutcome` will need a `-> EviBoundVerdict` mapping when TRUST-7 wires the live jury.
- Deferred-by-design (live wiring in I02+/I03): W08 EvidenceRecord persistence + CLI brief-frontmatter parse; W22 daemon dispatch-runner emit wiring (billing stays $0 until wired); W24 daemon dispatch-runner re-ask wiring; W20 codex pricing rate (flagged placeholder pending operator confirmation); W09 real-model NLI spike; W04 ~5000 scale ceiling is an advisory, un-calibrated figure.

## References

- Branch diff: `git diff main..feature/eawf-v0.5-p29` (29 wave deliverables + bookkeeping).
- EviBound chain: `src/eawf/workflow/evidence/{resolve,evibound,rung2,rung4}.py`.
- Research control-plane: `src/eawf/kernel/spec/{saturation,research_campaign,operator_input,round_loop,pruning}.py`; `src/eawf/kernel/state/{models,enums}.py` (Claim/OpenQuestion/Iter.trigger/Urgency).
- Spawn/metering/cancel: `src/eawf/runtime/runtimes/{adapter,claude/adapter,metering,cancel}.py`; pricing `src/eawf/observability/telemetry/pricing.py`; routing `src/eawf/workflow/dispatch/routing.py`.

## Provenance

Produced by the `/flow full P29-I01` pipeline (audit stage) on 2026-06-01: a fresh-context auditor subagent re-read the diff against the declared `success_criteria` per the verify-before-claim ladder, followed by a repo-wide polisher consistency sweep (which surfaced and fixed the W29 rename).

## Scrub

No absolute local paths, hostnames, credentials, or PII in this artifact. References are repo-relative.
