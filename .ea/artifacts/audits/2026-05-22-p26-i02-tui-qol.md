# P26-I02 TUI QoL + deferred-feature pull-in audit

## Summary

- P26-I02 added nine waves (W01-W09) layering operator-requested TUI quality-of-life onto the v0.3 surfaces. This fresh-context pass confirms all nine implement their declared success criteria against the source tree. Overall verdict **minor**: every wave's primary contract holds, the full suite is green (4056 passed, serial collection order at the audited HEAD), and the two follow-ups are non-blocking criterion-wording drifts [1].
- Audited HEAD: `f4f074a`; per-wave evidence cites each wave's commit on `feature/eawf-v0.3-p26` [1].
- **W01 — config-pane Enter-edit redesign — pass.** Focus-zone model removed; `↑/↓` move the field cursor (clamped), `←/→` switch tabs; `Enter` is the sole mutator (bool toggle / choice forward-cycle a-b-c-a / scalar inline in-row edit, popup only on newline-or-overflow); `ConfigKey.multiline` added on the `extra="forbid"` model; Space + arrow value-cycling removed; help.py + footer hint updated; goldens regenerated [2].
- **W02 — /theme swap + var migration + persist — pass.** `/theme dark|light|cb|auto` swaps `app.theme` via `register_theme`; semantic vars migrated out of `theme.tcss` global scope into per-theme `Theme.variables` (dark = Wong unchanged, cb = distinct CB-safe, light = textual-light, auto→dark); unknown name → warning notify; `ui.theme` persisted via the layered writer + added to `CONFIG_REGISTRY` (ui/choice) + `LEAF_KEY_REGISTRY` (writable g/w/r/env) [3].
- **W03 — /find /filter /sort + ellipsis — pass.** `/find` fuzzy-ranks waves+backlog by id+title (reuses `fuzzy_score`) and opens the existing DetailModal; `/filter backlog`→`apply_filter`, `/sort`→`cycle_sort`; backlog title truncation is width-aware + reactive on resize, replacing the fixed `_TITLE_MAX_WIDTH=48` [4].
- **W04 — footer weekly-burn — pass.** Footer renders `compute_weekly_burn` with a graceful `— no data` empty-state when `weekly_eu_target` is unset or actuals empty; closes backlog B050 [5].
- **W05 — DetailModal prettify + bars — pass.** `resolve_detail` gains iter + phase resolvers; `h/d/m/e/dp` tabs via `TabbedContent` (`Tab`/`Shift+Tab` cycle, arrows keep native scroll); completion bar (closed÷total) on iter/phase, effort_bucket size bar on wave, EU/token rows as em-dash empty-state; reusable `render_completion_bar`/`render_size_bar`/`render_eu_bar_plain`/`EMPTY_STATE` exported from `eu_bar.py`; box enlarged to 80%/120/85% [6].
- **W06 — pane bars + tree ellipsis — pass.** Inline completion bars on RoadmapTree iter/phase rows (wave rows bar-free) + a StatusPane `progress:` line, reusing the W05 helpers; tree row titles ellipsize width-aware with an `on_resize` rebuild [7].
- **W07 — NeedsUserModal wiring — pass.** New `eawf skill resume <pause-urn> --choice <label>` verb (unknown urn → not-found, bad choice → invalid-input); the TUI auto-opens the shipped NeedsUserModal for the active scope's open pause and the pick resolves it (failure → error toast). Greenfield: pauses persist as daemon-appended event-store rows (no pause MutationKind exists yet) and detection rides the StateBinding refresh (no daemon push bus yet) [8].
- **W08 — close B014 — pass.** B014 (multi-pane TUI) recorded satisfied by the 4-pane RepoScreen + `eawf roadmap show`; the proposed memory pane dropped per the I02 scope decision; bookkeeping-only [9].
- **W09 — config env-knob hygiene — pass.** `EAWF_BLITZ_DEPTH` + `EAWF_BLITZ_DEPTH_COUNTER` added to `_RESERVED_ENV_VARS` so the config env-layer skips the blitz recursion knobs; a regression test asserts no phantom `blitz_depth_counter` key leaks. Fixes a pre-existing, xdist-masked leak surfaced by the I02 serial full-suite run [10].
- Cross-cutting: rule-25 clean (no design-decision refs in the I02 source diff); the one new ingestion field (`ConfigKey.multiline`) lives on an `extra="forbid"` model; error-path tests present (inline out-of-range, unknown-urn/bad-choice, phantom-key, eu_bar zero-total/unknown-bucket) [1].

## Followup triage

Two follow-ups surfaced; both non-blocking criterion-vs-implementation wording drifts (the implementations are improvements over the criterion text).

- **DEFER (no fix) — W06 criterion phrasing.** The criterion names the `render_bar_plain` path, but the shipped impl uses `render_completion_bar` (the EU-suffix `render_bar_plain` path was deliberately removed); the completion bar is the correct populated signal. Close-out note only [7].
- **BACKLOG — W07 real push subscription.** The criterion says "subscribes needs_user_pause push"; no daemon push bus exists yet, so detection rides the StateBinding refresh. Functional outcome (auto-open for the active scope) is met and tested; wire a true push subscription when the daemon push bus lands [8].

## References

[1] `.ea/state.json` — `state.waves["P26-I02-W##"]` outcome strings + commit chain on `feature/eawf-v0.3-p26`; audited HEAD `f4f074a`; full suite 4056 passed (serial)
[2] commit `c038576 [P26-I02-W01] feat: config-pane Enter-edit redesign` + `src/eawf/tui_v2/screens/overlays/{config_modal,edit_field}.py` + `src/eawf/config/registry.py` + `src/eawf/tui_v2/screens/help.py`
[3] commit `1270a4f [P26-I02-W02] feat: /theme swap + per-theme var migration + ui.theme persist` + `src/eawf/tui_v2/{theme.py,theme.tcss,app.py}` + `src/eawf/tui_v2/palette/verbs.py` + `src/eawf/config/registry.py`
[4] commit `a3267c7 [P26-I02-W03] feat: wire /find + /filter + /sort verbs + width-aware backlog title ellipsis` + `src/eawf/tui_v2/palette/verbs.py` + `src/eawf/tui_v2/widgets/backlog_table.py`
[5] commit `ad9de25 [P26-I02-W04] feat: footer weekly-burn line with no-data empty-state (closes B050)` + `src/eawf/tui_v2/widgets/footer.py` + `src/eawf/estimation/metrics.py`
[6] commit `f643c5e [P26-I02-W05] feat: DetailModal iter/phase resolvers + tabs + completion/size bars + larger box` + `src/eawf/tui_v2/screens/overlays/detail.py` + `src/eawf/tui_v2/widgets/eu_bar.py`
[7] commit `677ce5d [P26-I02-W06] feat: completion bars in RoadmapTree + StatusPane + width-aware tree row ellipsis` + `src/eawf/tui_v2/widgets/{roadmap_tree,status_pane}.py`
[8] commit `d2202a6 [P26-I02-W07] feat: wire NeedsUserModal — skill resume verb + pause auto-open + pick` + `src/eawf/cli/commands/skill.py` + `src/eawf/skills/needs_user.py` + `src/eawf/tui_v2/{app.py,screens/overlays/needs_user.py}`
[9] commit `8eb29bb [P26-I02-W08] state: close B014 as satisfied + close wave W08` + `.ea/state.json`
[10] commit `72011b8 [P26-I02-W09] fix: reserve blitz recursion env knobs from config env-layer` + `src/eawf/config/layered.py` + `tests/unit/test_config_layered.py`

## Provenance

- audit_id: A34-P26-tui-qol
- audit_kind: review
- scope_id: P26
- verdict: minor (all nine W01-W09 waves implement their core criteria; full suite green at 4056 passed serial; two non-blocking criterion-wording follow-ups, one deferred + one backlogged)
- created_at: 2026-05-22
- author: claude-opus-4-7 (session eawf-flow-p26-i02)
- supersedes: none

## Scrub

- status: clean
- references: repo-relative paths + commit SHAs only
- local paths: none in body
- real emails: none
- abstract placeholder names: not applicable
