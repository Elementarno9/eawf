# A47 — P29-I04 flow-audit (live-spawn + cross-vendor jury iter)

## Summary

Fresh-context audit of iter P29-I04 ("Live spawn, cross-vendor jury, EviBound rung-3, AUQ + TUI") on branch `feature/eawf-v0.5-p29`, covering all 14 delivered waves (W01-W14) against their declared `success_criteria`, dispatched as four concern-clustered fresh-context `auditor` subagents (no parent conversation) reading committed code vs criteria with adversarial probing and confirm/refute of the executor's registered followups.

Verdict: **minor** (pass-with-followups) after same-iter remediation. The audit confirmed the claude dispatch lane is genuinely live + jailed + tested end-to-end, but surfaced one real safety gap and several idle-machinery findings of major severity that were then remediated inside the iter via the WIDE remediation wave W15 (six slices). Residual followups are all P2/P3 and tracked.

### What the audit found (W01-W14)

- **claude spawn spine (W01-W04)** — PASS. Live wave-executor spawn behind the I03 floor + AgentSession registration (un-idling the dispatch report emit); deny-list threaded to the claude argv; bounded retry + tiered failure notice over the CLI failure taxonomy. Two confirmed pre-existing followups: B080 (opencode child env-scrubbed but NOT FS-jailed; opencode lane unregistered in the shared `jail.py`/`env_scrub.py`), B081 (codex/opencode have no per-call CLI tool-deny flag).
- **cross-vendor adapters (W05-W06)** — PASS-with-followups. `CodexAdapter`/`OpenCodeAdapter` `spawn_session` fork their CLIs + parse real metered/priced `SpawnResult`s; well-tested at unit level. Findings: the live cross-vendor spawn was unreachable on the production dispatch path (routing emitted claude-only; the jury that binds the adapters had no `src/` caller), and vendor-model pricing missed (codex/opencode model ids fell through `lookup_pricing` to `priced=False, cost=0`).
- **verdict + jury + EviBound + AUQ (W07-W10)** — the central finding. W07 (Keystone A, single fresh-context auditor verdict producer + close gate) is genuinely WIRED + enforcing. But W08's 3-vendor disjoint-family jury (B082), W09's EviBound rung-3 + live research rounds, and W10's cross-runtime AUQ conversion were BUILT + unit-tested but IDLE — zero production callers; the enforcing close gate ran only the single W07 auditor. Additionally W07's `memory=False` fresh-context contract was computed + logged but never threaded to the spawn (B084) — a dead override (fresh context still held structurally via fresh session + diff-only prompt).
- **TUI modes (W11-W14)** — PASS. research_board / agent-watch / autopilot registered in the Textual MODES chassis on digits 7/8/9, reachable, golden regenerated; the EAWF010 LOC exclusion is scoped to the single oversized `surfaces/tui/app.py` (B083, extract-split owed).

### What W15 remediated (same iter, WIDE)

The operator elected a WIDE remediation wave to un-idle the cross-vendor verdict stack and make the iter's "cross-vendor jury" deliverable honest. Six slices landed, each committed + tested:

1. **opencode safety floor (B080)** — registered the opencode auth lane in the shared `jail.py` + `env_scrub.py`; the opencode child is now FS-jailed (no unjailed degrade) + delegates to the shared env-scrub (dropping the local duplication); promoted `extra_path_dir` to the shared builder.
2. **per-vendor model + pricing (B086/B087)** — `model_for_runtime` resolves the capability tier off the routing decision and maps it to each vendor's OWN model id (codex -> OpenAI id, opencode -> `provider/model`, claude byte-identical); `lookup_pricing` resolves codex+opencode model ids so juror `SpawnResult`s price honestly.
3. **jury at the close gate (B082)** — `_enforce_wave_close_gate` (async, awaited in `mutate` before apply) convenes `convene_cross_vendor_jury` over claude+codex+opencode for the `always` (high-risk) subset behind an opt-in `verify.cross_vendor_jury` flag (default off; degrades to the single W07 auditor when off or when fewer than quorum juror CLIs resolve on the host). A minority-veto FAIL / split / sub-quorum NEEDS_USER blocks close. Seven tests over the real convener + reducer (spawns stubbed).
4. **EviBound rung-2->3 chain + per-level live-round dispatch (B085, partial)** — `drive_text_claim_chain` chains a rung-2 ESCALATE into `escalate_to_rung3`; `drive_campaign` dispatches `run_live_rounds` at cockpit Level 3+. Both assemblers are tested, so `escalate_to_rung3` + `run_live_rounds` are no longer bare-idle. The deeper production-entry-point wiring is deferred (see Followups / B091).
5. **daemon-level cross-vendor spawn integ test (B088)** — drives codex AND opencode spawns through the REAL adapter parse + jail + metering path (faking only `create_subprocess_exec`), asserting per-vendor pricing + a `dispatch_cost` row; plus opencode FS-jail engagement.
6. **polish (B084/B090)** — removed the dead `memory=False` override + its orphaned helper/imports and reworded the docstrings to state the structural fresh-context guarantee accurately; converted the lone `agent_watch` `.format()` template to an f-string (rule 9).

## Followups

Carried forward (none block I04 close; I04 is non-final, so no ship/PR gate applies):

- **B091 (P2) — EviBound + campaign production entry points.** W15 added `drive_text_claim_chain` + `drive_campaign` so `escalate_to_rung3` + `run_live_rounds` have tested callers, but those assemblers are themselves not yet reached from a production entry point: the brief-promotion gate `check_brief_promotable` runs rung-1 deterministic only (routing textual-entailment refs through rung-2->3 needs an async `EntailmentScorer` + production `BallotFn` + a ref-modality discriminator), and the campaign-execution layer has no production driver at all. This is the deeper half of the original W09 idle finding; it depends on subsystems beyond W15's scope.
- **B081 (P2) — per-call CLI tool-deny flag.** codex/opencode have no `--disallowedTools`-equivalent; the FS-jail floor is the confinement. Maps to an argv flag once the upstream CLIs ship one.
- **B089 (P3) — codex `model_reasoning_effort` `-c` key probe.** The exact key rests on a docstring + fixture comment; record a live-probe spike brief or add a probe test.

## References

- Plan + criteria: `eawf wave show P29-I04-W<NN> --dispatch-prompt`; v0.5 detailed plan `.ea/local/research/2026-05-31-p29-detailed-plan.md`.
- Branch diff: `git log --grep '[P29-I04' 3392f2ad..HEAD` (14 wave deliverables + W15 remediation + bookkeeping).
- W15 opencode floor: `src/eawf/runtime/sandbox/jail.py`, `src/eawf/runtime/sandbox/env_scrub.py`, `src/eawf/runtime/runtimes/opencode/adapter.py`.
- W15 model + pricing: `src/eawf/workflow/dispatch/routing.py` (`model_for_runtime`), `src/eawf/observability/telemetry/pricing.py`.
- W15 jury at close: `src/eawf/runtime/daemon/methods/state.py` (`_enforce_wave_close_gate`), `src/eawf/observability/eval/cross_vendor_jury.py`; flag `src/eawf/platform/profiles/models.py` (`VerifyBlock.cross_vendor_jury`).
- W15 rung-2->3 + live rounds: `src/eawf/workflow/evidence/chain.py`, `src/eawf/kernel/spec/campaign_driver.py`.
- W15 integ test: `tests/daemon/test_cross_vendor_spawn_integ.py`; jury gate test `tests/daemon/test_wave_cross_vendor_jury_gate.py`.
- Polish: `src/eawf/workflow/dispatch/verdict.py` (B084), `src/eawf/surfaces/tui/modes/agent_watch.py` (B090).

## Provenance

Produced by the `/flow audit + polish P29-I04` pipeline on 2026-06-02. The audit stage dispatched four concern-clustered fresh-context `auditor` subagents (no parent conversation) that re-read the committed P29-I04 diff against each wave's declared `success_criteria` per the verify-before-claim ladder, citing source `file:line` evidence per criterion and confirming/refuting the executor's registered followups (B080-B083). The operator dispositioned the findings via `AskUserQuestion` (WIDE remediation wave W15; fix B084/B090 in polish). W15 slice 1 + slices 3 and the polish were authored by the operator-driver in the main worktree; slices 2-3 and 4-5 were delivered by serial fresh-context `executor` subagents, each verified (diff re-read + full suite) before commit. Verdict folds the post-remediation state: the blocking findings (B080 safety gap, B082/B086/B087 idle/unreachable cross-vendor stack, B084 dead override) are resolved + tested; B085 is assembler-closed with B091 carrying the deeper production wiring.

## Scrub

- status: clean

No absolute local paths, hostnames, credentials, or PII. References are repo-relative or eawf-internal ids.
