# A53-P29-i10 — flow-audit (Fidelity Spine substrate)

## Summary

Verdict: **minor** (pass-with-followups). Iter P29-I10 delivered all nine Fidelity-Spine substrate waves against their success criteria, including the high-risk live `state.json` schema migration (1.6 -> 1.7). A fresh-context auditor verified each wave refute-first against the HEAD working tree and the per-wave pinned commits; the parent ran the full pytest suite (5671 passed, 1 duckdb-optional skip) and `uv run mypy src/` (clean, 622 source files). Two non-blocking MINOR follow-ups, both spec-prose items that no criterion pins.

P29-I10 is NOT the phase-co-closing iter (I11 + I12 follow), so this close is iter-only; phase P29 stays ACTIVE.

## Delivered

The nine substrate waves (spec FS ids from `.ea/local/research/2026-06-04-fidelity-spine-impl-spec.md`):

- W01 (FS01) — `OracleTier`/`ObserveVerb`/`ProofLocus`/`ResponseClause` added to `kernel/spec/common.py`; `QualityDimension` relocated from `wave.py` and widened to the 10-member ISO-25010:2023 set (OPERABILITY retained for back-compat); `CriterionSpec` gains required `quality_dimension`+`measurable_signal` and optional `response`+`oracle_tier` with a `_judged_requires_reason` validator.
- W02 (FS04) — one `schema_validate` `CheckKind` member + impl (`kinds/schema_validate.py`), registered; malformed args degrade to `status="fail"` not raise; `compile_gate` generic passthrough handles it unchanged.
- W03 (FS07) — guarded lifecycle FSM tables `WAVE/PHASE/ITER_TRANSITIONS` + `GuardName`/`GuardContext`/`validate_transition` in `lifecycle/spec.py`; scattered guards refactored to consult the table. Independently confirmed behaviour-preserving (deps not bypassed by out_of_order, sibling bypassed, pause unconditional; error order preserved).
- W04 (FS09) — key-press affordance probe (`record_keypress_transcript` via real `pilot.press`, reading `app.screen.active_bindings`) + `affordance_parity` `CheckKind`; a footer key with no resolving binding classifies UNRESOLVED — the dead-`c`-hotkey detection the incident demanded.
- W05 (FS02) — `assign_oracle_tier` total verb->tier function + `_VERB_TIER` (matches spec §3 verbatim) + `_tier_for_gate_kind` raising stub.
- W06 (FS03) — `Wave.success_criteria: list[str] -> list[CriterionSpec]`; `schema_version` 1.6 -> 1.7 with the `v1_6_to_v1_7` grandfather-backfill migration; readiness `_load_criterion_specs` un-idled; the plan/producer path retyped (with a `grandfather_criterion` helper for operator-string criteria). The LIVE `state.json` was migrated 1.6 -> 1.7 (1376 legacy criteria grandfathered, schema_version verified 1.7, criteria now typed objects).
- W07 (FS15) — `VisualContract` + `DeterminismRecipe` types in new `spec/visual.py` (`VisualSurface` closed Literal; reuses CriterionSpec/GateSpec/WaveBehavior), re-exported via the spec package.
- W08 (FS08) — `transition_coverage` `CheckKind` + a self-instrumenting Hypothesis `RuleBasedStateMachine` whose covered edge set is compared to the FSM table edges (deterministic across seeds).
- W09 (FS10) — TUI affordance-resolves matrix over all 8 `MODE_REGISTRY` modes x footer-advertised keys (93 pairs, all resolve); a synthetic dead-key error-path proves the matrix is non-vacuous.

## Anti-drift verdict

Each criterion is genuinely met (Tier-1: the deliverable exists and works), verified refute-first against the source, not the commit messages. The keystone risks were checked exhaustively: the FS07 "pure refactor" was confirmed behaviour-preserving against the pre-refactor guards; the FS03 migration grandfather formula was confirmed to handle the live data (max legacy criterion 392 chars < the 500 cap; short strings get the fixed measurable_signal); the FS09 probe routes through the real key->Binding layer, not the `run_action` shortcut that masked the original dead-`c` bug. The ratifying Decision `D-FIDELITY-SPINE` (scope P29) landed as W01's deliverable. Every deliverable commit is wave-form `[P29-I10-W##] <type>:`; no source file carries a design-decision provenance comment.

The I10 waves' own success criteria were grandfathered to typed CriterionSpec by the W06 migration (they were staged as `list[str]` before the retype); the enforcement that scores typed criteria (FS06 close gate + FS13 verifier-on) lands in I11.

## Followups (non-blocking, MINOR)

- MINOR — FS03 spec prose named both readiness loaders, but `_load_gate_specs` (`src/eawf/workflow/verify/readiness.py:98`) still returns `[]`. Defensible: GateSpec rows are not persisted in `Wave.success_criteria` (they need a separate markdown body parser), and no FS03 criterion (CR-1..CR-4) pins gate-spec loading. Tracked as backlog B095 (gate-spec body parser + refresh the stale "v0.4.0" docstring).
- MINOR — FS03's named golden-regen step over `tests/golden/state` was a no-op because no golden fixture carries populated typed criteria (the only `success_criteria` golden bytes are `[]` or rendered text). The G7 migration-safety step was therefore not exercised end-to-end on the state golden surface; the obligation reappears if a future fixture gains populated criteria. Tracked as backlog B096.

## Honest-negative / known-failure integrity

The audit pass was read-only; it mutated no state. The full suite ran 5671 passed / 1 skipped (the skip is the optional `duckdb` metrics-store dependency, absent locally). `tests/e2e` and `tests/eval` were excluded from the gate run (they need special infra); the CI-only coverage + snapshot-pairing gates fire on the phase PR at ship, not at iter close. The packaging-test failure one executor observed was a worktree-only stale build-artifact leftover and is absent in the parent tree (full suite green confirms).

## References

- Implementation spec: `.ea/local/research/2026-06-04-fidelity-spine-impl-spec.md` (FS01-FS17; I10 = FS01/02/03/04/07/08/09/10/15)
- Companion doctrine: `.ea/local/research/2026-06-04-spec-as-layered-oracle.md`
- Ratifying decision: `D-FIDELITY-SPINE` (scope P29)
- Wave commits: `git log --grep '[P29-I10-W'` on `feature/eawf-v0.5-p29`
- Schema migration: `src/eawf/kernel/migrations/v1_6_to_v1_7.py`
- Followups: backlog B095, B096

## Provenance

Produced by the `/flow P29-I10` audit stage on 2026-06-06. A fresh-context auditor (no access to the implementing conversation) ran the criteria-vs-code and anti-drift lenses over the `98c692b4..HEAD` diff and the per-wave pinned commits, spot-ran mypy + the new test files green, and verified the live 1.6 -> 1.7 migration on `.ea/state.json`. The parent ran the full `tests/unit tests/workflow tests/property tests/snapshots` suite (5671 passed). No state was mutated by the read-only audit; this report records the verdict that gates the I10 iter close. Phase P29 stays ACTIVE (I11 + I12 follow).

## Scrub

- status: clean

No absolute local paths, hostnames, credentials, or PII. All references are repo-relative paths or Eawf ids.
