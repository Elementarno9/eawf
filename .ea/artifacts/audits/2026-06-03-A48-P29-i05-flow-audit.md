# A48-P29-i05 — flow-audit (trust pipeline + TUI brief-parity)

## Summary

Verdict: **minor**. Iter P29-I05 delivered all 14 waves (10 planned + 4 ship-stage operator-review repairs) against their success criteria with the integrated suite green (1592 passed, 0 failed; pre-commit clean across the whole tree). The audit applied four lenses — criteria-vs-code, provenance-fidelity, honest-negative integrity, and scope/test discipline — plus a live operator acceptance pass.

The iter had two clusters. The **trust cluster** (W01–W05) built the reputation/jury pipeline: a `VerdictOutcome` outcome-loop, a Brier + EB-Beta + Wilson-LB scorer under a hard min-N honesty gate, a C→B→A `ReputationTier` ladder (distinct from the output-quality `TrustTier` literal), reliability-weighting over the cross-vendor jury, and a verdict-agreement cost A/B over the metering ledger. W01 and W04 are live; W02, W03, and W05 are **honest-negative** — they refuse to score / return `INSUFFICIENT` over today's empty per-wave verdict and `dispatch_cost` stores, verified genuinely empty rather than fabricated, and light up the moment live dispatch accrues rows (the idle-contract pattern, exemplar `observability/eval/self_eval.py`).

The **TUI brief-parity cluster** (W06–W10) remediated incident `2026-06-02-tui-brief-criteria-drift.md`: it renumbered `MODE_REGISTRY` to the ratified D-TUI-MODES digit map (1 home / 2 autopilot / 3 research / 4 trust / 5 doctor / 6 evidence / 7 feed / 8 watch, Config stub retired), landed the **D-TUI-MODES Decision row** and promoted the `tui-chassis` brief (closing the incident's root-cause #3 — no ratifying decision), wired the autopilot intervention keys, rebuilt the research mode into the 3-pane orchestrator, fixed the five section-K defects, and made the breadcrumb full-location + clickable with a real runtime cell.

## Provenance-fidelity (incident mitigation #4)

No remaining brief-scope drift. All five remediation items from `2026-06-02-tui-brief-criteria-drift.md` (its W16/W17/W18/W19/W20 == this iter's W06/W07/W08/W09/W10) landed, the D-TUI-MODES decision is active in `state.json`, and the brief is promoted to `.ea/artifacts/research-2026-05-30-tui-chassis.md`. The feed and watch modes the as-built kept (digits 7/8) are the two useful net-new surfaces the incident itself blessed.

## Honest-negative integrity

Verified, not fabricated: zero `*_report.jsonl` rows, zero `dispatch_cost` rows on disk. `build_verdict_outcomes` returns `[]`; `compute_role_reliability` returns `INSUFFICIENT` with every score field `None`; `compute_cost_ab` returns `INSUFFICIENT_DATA` with no `$` figure; the tier is `None` for every role. Tests prove the scorers produce real numbers when fed synthetic cohorts, so these are gated-idle, not dead.

## Operator-review repairs (W11–W14)

Live acceptance testing surfaced five practical defects that the unit suite passed over; each was root-caused and repaired as a wave appended to this iter:

- **W11** — Doctor mode hung the TUI: `gather_doctor_health` ran the synchronous `git log --grep`-per-wave drift scan on the UI thread. Fixed by offloading to a Textual worker (`asyncio.to_thread`, exclusive group, placeholder-then-paint), mirroring `widgets/git_pane.py`.
- **W12** — Footer mode row: added the always-on mode accelerator row (2-row layout, active mode highlighted) and removed the stale `1-9 mode` hint + `digit 9` docstring on autopilot.
- **W13** — Clickable breadcrumb was a no-op: the `@click` markup used bare action names that Textual resolved against the `Header` `Static` (no such actions). Fixed by namespacing to `app.*` so they resolve against `EaApp`; added a guarded `action_switch_mode`.
- **W14** — Live roadmap rarely refreshed (restart required): push mode stopped the mtime-poll loop, so a stalled/silent push stream stranded bound state. Fixed with an always-on mtime-gated poll backstop alongside push.

## References

- Incident remediated: `.ea/artifacts/incidents/2026-06-02-tui-brief-criteria-drift.md`
- Ratified design: `.ea/artifacts/research/research-2026-05-30-tui-chassis.md` (D-TUI-MODES)
- Trust design: `.ea/local/research/trust/` (reputation-engine, cross-vendor-jury, verdict-producer)
- Wave commits: `git log --grep '[P29-I05-W'` on `feature/eawf-v0.5-p29`
- Prior iter audit: `.ea/artifacts/audits/2026-06-02-A47-P29-i04-flow-audit.md`

## Provenance

Produced by the `/flow full P29-I05` ship-stage audit on 2026-06-03. Lenses run over the `93d71458..HEAD` diff by a fresh-context auditor (criteria-vs-code, provenance-fidelity, honest-negative integrity, scope/test discipline), with the four operator-review repairs each test-verified and live-confirmed by the operator. The one audit finding (autopilot stale digit reference) was fixed in W12. No state was mutated by the read-only audit pass; this report records it.

## Scrub

- status: clean

No absolute local paths, hostnames, credentials, or PII. References are repo-relative or eawf-internal ids.
