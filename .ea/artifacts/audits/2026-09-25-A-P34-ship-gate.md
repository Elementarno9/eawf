# P34 ship-gate audit

## Summary

Phase P34 shipped as one pull request, #41, from `feature/eawf-v0.7-p34` at `e7cbb6c3`. That head carries forty-nine waves in eleven commits: nine themed commits that group the waves by subject and carry each wave's `Eawf-Wave` trailer, one commit that makes the tree type-check clean, and one state commit. The ship gate passes on that head, and the recorded verdict is **minor**, inherited from the evaluation audit of the phase's only iter [R1]: every high finding was repaired inside the phase, and every medium and low finding is filed with evidence.

The phase lands on `main` by fast-forward, per D66, so every wave pin that points into the branch stays valid after the merge. Nothing is tagged or published from P34. The `0.7.0.dev3` create, candidate, receipts and approval ride `P35-I01-W01`, because a checkpoint cannot reach CANDIDATE before a tag push from `main` produces its publication receipts; B163 files the root cause.

## What the gate checked

**Continuous integration on the pull request passes on every leg.** The Linux and macOS full-suite legs, the Windows named-pipe transport set, the console golden contract replay, the strict prose gate, the snapshot pairing gate, the real-bwrap jail launch, the runtime-deps install smoke, the ast-grep floor and the change classifier all pass on `e7cbb6c3` [R2]. The fresh-checkout double run and the green-tree record run only on pushes to `main` and were skipped here by design.

**Every pre-push hook passes on the pushed head.** That includes `mypy src/` over 1,088 source files, which the first push attempt failed with 163 errors from the alias shims an earlier wave left behind; a repair wave retargeted every import and removed the shims before the push landed.

**Local runs agree with CI.** The fast suite passed in an isolated runtime directory, as did the TUI snapshots, the performance budgets and the end-to-end tests. The only red was the phase-branch backup assertion, which passes once the branch is on the remote. The skill envelope evaluations fail identically on `main`, run in no CI job, and are filed as B177.

**The evidence record reconciles.** `eawf wave verify-commits` reports every closed wave reconciled with zero drift after each wave was repinned to the themed commit that carries its trailer.

## References

| Ref | Location | What it establishes |
|---|---|---|
| R1 | `.ea/artifacts/audits/2026-09-24-A-P34-I01-eval.md` | the evaluation verdict, the in-phase repairs and the filed findings |
| R2 | pull request #41 checks | the green continuous-integration legs on the shipped head |
| R3 | `.pre-commit-config.yaml` | the pre-push hooks, including the whole-tree type check |
| R4 | `tests/lint/test_branch_backup.py` | the backup assertion, red until the push |

## Provenance

Compiled 2026-09-25 at the close of P34 from the pull request's check results, the pre-push hook run on the pushed head, the local suite runs and the phase's evaluation audit.

## Scrub

- status: clean
- references: repo-relative
- credentials, PII, machine-specific paths: none
