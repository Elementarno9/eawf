# A46 — P29-I03 flow-audit (safety-floor iter)

## Summary

Fresh-context audit of iter P29-I03 ("Safety floor: env-scrub + egress proxy + FS jail + budget-HALT") on branch `feature/eawf-v0.5-p29`, covering all 5 waves (W01-W05; iter activate = 86a1443a) against their declared `success_criteria` and the verified threat model in `.ea/local/research/2026-05-30-safety-floor.md`, read-only code-vs-criteria with adversarial probing.

Verdict: **minor** (pass-with-followups). 5 criteria PASS + 1 cross-cutting row PASS, 0 UNMET, 0 refutations. The auditor attempted and failed to find a cred var surviving the scrub (case / substring / mid-string variants), a host sneaking past `classify_egress` (suffix / prefix / subdomain / null-byte / IPv6-mapped / decimal-IP / port-embed / ungated-pypi), a jail arg re-sessioning (only doc-negations of `--new-session`/`setsid` exist), and a `budget -> runtimes -> budget` circular import (zero `cancel` symbol under `runtime/budget/`). mypy clean (583 files); 375 floor tests pass.

W01 env-scrub closes threat-table C4: `build_child_env` starts from an empty dict (`env_scrub.py:141`), pins PATH to a fixed `/usr/bin:/bin:/usr/sbin:/sbin` floor (never the parent PATH), seeds only HOME/LANG/LC_*/TERM plus the spawning lane's auth family, and drops AWS_/GH_/GITHUB_/SSH_/KUBECONFIG/EAWF_ and every unknown var by construction (fail-closed); the live `ClaudeAdapter.spawn_session` `create_subprocess_exec` now passes `env=build_child_env(self.id)` (`adapter.py:429`).

W02 egress closes the network half of threat-scenario (b): `classify_egress` runs the canonicalization guard FIRST (null-byte / non-DNS-char rejected before any allowlist compare, defeating the Penligent SOCKS5 bypass), then metadata-deny with precedence over the allowlist and `gate_phase`, then an EXACT-hostname (frozenset membership, not `endswith`) lane allowlist, then default-deny; the UDS proxy returns the deny decision before opening any outbound connection (`egress_proxy.py:225` precedes `:227`), and Windows is an honest `egress_proxy_supported()` predicate + typed `EgressUnavailableOnWindowsError`, not an import-time block.

W04 jail closes the structural argv-policy-escape (threat-scenario c): `jail.py` returns an argv PREFIX (Linux `bwrap` with `--unshare-pid` + `--die-with-parent` together per bubblewrap #529; macOS `sandbox-exec -f <profile>`), confines writes to the validated cwd (read-only root + single rw cwd-bind / `(deny default)` + `(allow file-write* (subpath cwd))`), masks the 8 cross-tool credential dirs while carving out the agent's own credential (claude Keychain on macOS plus the on-disk Claude credentials file on Linux/Windows; codex `$CODEX_HOME/auth.json`), and emits NO `--new-session`/`setsid` so the daemon-set process group is preserved and `cancel_process_group` reaps the whole tree — the load-bearing invariant the W03 spike proved the reap depends on. The spawn seam prepends the jail with feature-detection (`jail_supported()` + `shutil.which(wrapper)`), falling back to unjailed + a loud warning when the tool is absent, keeping `env=` + `cwd` + `start_new_session=True` untouched.

W05 budget-HALT wires `classify_enforcement` HALT to the kill ladder daemon-side: `enforce_token_cap` lives in `runtime/daemon/budget_interlock.py` (not `runtime/budget/`, avoiding the `cancel <- budget.service` cycle), drives `cancel_with_grace` (which composes `terminate_with_grace` over the process group) only on a HALT with a positive pgid, None-guards the pgid for today's dark mutating dispatch (computes + loudly logs the over-cap but sends no signal), and is wired into `accrue_tokens_consumed` after the state portalock.

The floor correctly stays "read-only-assist-only-until-jail": the mutating wave-dispatch spawn remains dark by design (`daemon/methods/agent.py` pid=0, kill placeholder) — no live state-mutating spawn was turned on in this iter (that is I04). Conventions hold across the changed set: ASCII-only source, f-strings, full type hints, `from __future__ import annotations`, Google `Raises:` blocks; `EgressDecision`/`InterlockOutcome` are frozen slotted dataclasses (no new pydantic models).

## Followups

Carried forward (neither blocks I03 close; both recorded as backlog):

- **Egress decimal/octal-IP metadata form (cosmetic):** a decimal-encoded metadata IP (e.g. `2852039166` == 169.254.169.254) lands at `default-deny` rather than the `metadata-deny` reason tag. The outcome is still BLOCKED (safe), and the proxy only ever forwards the normalized `decision.host` to `open_connection` so there is no resolver-side re-expansion gap — the gap is reason-tag precision only.
- **Linux own-cred re-bind for a relocated `$CODEX_HOME` (I04 concern):** `_build_linux_argv` re-binds the own-cred read-only only when it exists at the resolved path; an operator who places `$CODEX_HOME` *under* one of the tmpfs-masked cred dirs would need an explicit re-bind. The default codex location is not masked, so the common path is covered; this surfaces only for the I04 live-codex-spawn wave, not the read-only claude floor shipped here.

## References

- Plan + criteria: `eawf wave show P29-I03-W<NN> --dispatch-prompt`; threat model `.ea/local/research/2026-05-30-safety-floor.md`.
- Branch diff: `git log --grep '[P29-I03' 40cb293a..HEAD` (5 wave deliverables + bookkeeping).
- W01 env-scrub: `src/eawf/runtime/sandbox/env_scrub.py`; wiring `src/eawf/runtime/runtimes/claude/adapter.py:429`.
- W02 egress: `src/eawf/runtime/sandbox/egress.py`, `src/eawf/runtime/sandbox/egress_proxy.py`.
- W03 spike verdict: `.ea/local/research/2026-06-02-s-jail-0-srt-double-fork.md`.
- W04 jail: `src/eawf/runtime/sandbox/jail.py`; spawn seam `src/eawf/runtime/runtimes/claude/adapter.py:67-120`.
- W05 budget-HALT: `src/eawf/runtime/daemon/budget_interlock.py`; wiring `src/eawf/runtime/daemon/dispatch_runner.py:486`.

## Provenance

Produced by the `/flow full P29-I03` pipeline (audit stage) on 2026-06-02: a fresh-context `eawf:auditor` subagent (no parent conversation) re-read the committed diff against each wave's declared `success_criteria` and the safety-floor threat table per the verify-before-claim ladder, citing source `file:line` evidence per criterion and adversarially probing each containment boundary. Waves W01/W02/W04/W05 were delivered by serial fresh-context executors in the main worktree (commits 34f7eaf, 9447061, d918337, 330ce87); W03 was a read-only researcher spike.

## Scrub

No absolute local paths, hostnames, credentials, or PII. References are repo-relative or eawf-internal ids.
