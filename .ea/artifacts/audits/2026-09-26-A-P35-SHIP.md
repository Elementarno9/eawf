# P35 ship-gate audit

## Summary

**Verdict: minor.** Phase P35 is ready to open its phase pull request, land on `main` by fast-forward and be released as `0.7.0.dev4` through `eawf release pipeline`. Nothing blocks the ship [1][7].

`eawf release preflight 0.7.0.dev4` has one red required row, `ancestry`, which can only turn green once HEAD is on `origin/main`. The other nine required rows pass, and so does the advisory `platform` row. The changelog section matches the phase's commits and the schema facts. No new backlog row is P0. The one P1, B188, gates the P36 live cutover rather than dev4. `eawf wave verify-commits` finds no drift across the closed P35 waves, and the phase diff is clean of secrets and personal data.

## Findings

| # | Severity | Location | Finding | Disposition |
|---|---|---|---|---|
| F1 | info | preflight `ancestry` row | fails `source_not_publishable` because HEAD is not yet an ancestor of `origin/main` | expected before the merge; green after the fast-forward |
| F2 | low | preflight `artifacts` row | before the merge it builds from `origin/main` and passes on the dev3 wheel and sdist, so it does not yet prove dev4 builds | the pipeline's `build_receipts` step dispatches the release workflow's dry run on the merge commit before any tag |
| F3 | low | `CHANGELOG.md` dev4 Limitations; B188 | dev4 ships the epoch-2 apply verb while B188 (apply reads the committed corpus and ignores uncommitted live state) is open, and the changelog did not say so [6][7] | fixed in P35-I05-W07 |
| F4 | low | `.github/workflows/phase-release.yaml` | the version check runs only when the landed head subject carries `(release=vX.Y.Z)`; with a fast-forward that is the phase-close commit [2] | the phase-close subject ends with `(release=v0.7.0.dev4)` |
| F5 | info | `git log origin/main..HEAD` | squashing the bookkeeping rewrites SHAs, so wave pins will read `pinned_mismatch` until repinned by trailer [5] | re-run `eawf wave verify-commits`, then `--repair` in a quiet window, after the squash |
| F6 | info | I05 wave commits | `Eawf-Wave` and `Co-Authored-By` sit in separate paragraphs, so `git log --format=%(trailers)` misses `Eawf-Wave`; eawf's own parser still finds it | merge the two blocks when squashing |
| F7 | info | B180, B185, B190 (P2) | conduct checks, current-model pricing rows and the console write path | carried to P36 |

## Checks

- `uv run eawf release preflight 0.7.0.dev4` exits 3 with `ready=False`. It is read-only: it sweeps through `sweep_for_tag`, and the probes write nothing [4]. Required rows version_consistency, changelog, tree_cleanliness, membership, migration, artifacts, dependencies and provider pass; the advisory platform row passes. The required `ancestry` row fails. The optional `perfect_realization` and `credentials` rows are unavailable. The profile is native_canary with fifteen gates.
- `uv run eawf release train show` exits 0 and lists eight rungs, with dev4 open (native_canary) and dev5 pending (product_canary).
- The CHANGELOG `[0.7.0.dev4]` section was checked against `git log origin/main..HEAD`: every feature commit maps to a bullet, the sections follow the dev1 to dev3 shape, and the Migration note is correct (`_DEFAULT_TARGET_VERSION = "1.20"`, no diff under `src/eawf/kernel/migrations/`).
- Backlog B178 to B191 are all open: ten P3, three P2 (B180, B185, B190), one P1 (B188), no P0.
- `uv run eawf wave verify-commits` exits 0 with 0 drift.
- A secrets scan of every added line of `git diff origin/main..HEAD` finds no home paths, IP addresses, hostnames or tokens; the only emails are `example.*` placeholders and a fixture co-author trailer.
- `uv run eawf config get vcs.conventions.release.cadence` returns `manual`, so phase close is not gated on the release preflight [8].
- `dist_tag_for_version("0.7.0.dev4")` returns `next` and `npm_version_for` returns `0.7.0-dev.4`; the tracked marketplace manifests pin no version [3].
- `importlib.metadata.version('eawf')` returns `0.7.0.dev4`.
- `uv run pytest` over the five test files that pin dev4 and dev5 exits 0 with 217 passed.
- The operator's pre-PR run of the CI-only gates on this branch: the coverage legs plus `tools/coverage_gate.py` pass at 90% total; `tools/snapshot_pairing_gate.py origin/main HEAD`, `tools/requirement_trace.py check`, `eawf hook validate-prose --strict --base origin/main`, `pytest tests/eval/test_skill_eval.py -m eval`, `mypy src/` (after P35-I05-W06), `deptry .` and `pre-commit run --all-files` each exit 0. The branch-backup check stays red until the push, and one TUI autopilot test flaked once and passed 2 of 2 alone and 75 of 75 across its file.

## References

[1] `CHANGELOG.md`

[2] `.github/workflows/phase-release.yaml`

[3] `.github/workflows/plugin-release.yaml`

[4] `src/eawf/surfaces/cli/commands/release_tag.py`

[5] `src/eawf/workflow/lifecycle/wave_trailer_repin.py`

[6] `src/eawf/kernel/migration/epoch2/apply.py`

[7] `.ea/state.json` (B178 to B191, D69, D70)

[8] `docs/rules/release-process.md`

## Provenance

A fresh-context, read-only audit of branch `feature/eawf-v0.7-p35` at `516e417f` against `origin/main` `026b33ab` (tag `v0.7.0.dev3`), compiled 2026-09-26. It made no edits and ran no mutating eawf commands, and did not re-run the full suite. The F3 fix landed afterwards as P35-I05-W07.

## Scrub

- status: clean
- references: repo-relative
- credentials, PII, machine-specific paths: none
