# A45 — P29-I02 flow-audit (surfaces-and-process iter)

## Summary

Fresh-context audit of iter P29-I02 ("Surfaces and process: CLI flex, preferences, TUI MODES chassis") on branch `feature/eawf-v0.5-p29`, covering all 30 core waves (W01-W30; iter activate = 533bd4c) against their declared `success_criteria`, read-only code-vs-criteria.

Verdict: **minor** (pass-with-followups). 29 criteria PASS, 1 pass-with-followup (W30), 0 UNMET. Both keystones pass — W16 (Textual MODES chassis: 6 modes home/trust/doctor/evidence/feed/config on digits 1-6, `switch_mode`-routed digit + `/<mode>` palette verbs, brand `Eä` outside-left of the breadcrumb, one-line mode-registry seam) and W29 (`Wave.claimed_at` + 1.2->1.3 migration backfilling from `wave_claimed` events; the live state is schema 1.3 with 103 waves carrying `claimed_at`, and all four elapsed-clock consumers — daemon publisher, stale detector, TUI burn-bar — anchor on `claimed_at` and render no clock when None, with no `opened_at` fallback path). No cross-wave regressions; no fabricated data.

All five anti-Goodhart / honesty criteria are not merely satisfied but structurally enforced: W20 Trust renders an honest-negative banner when data-starved (`is_data_starved` over all three signal sources, never a fabricated green score); W22 Evidence renders honest-empty until reports exist; W24 multi-repo + W25 cross-repo PR read only the explicit registry boundary with a trip-wire test that monkeypatches `os.walk` to raise; W25 binds no mutating verb.

Full suite: 8298 passed at the post-prep run; 3 cross-wave regressions surfaced and fixed (CLI import-budget — W29 had pulled `store.kinds`+`yaml` into the app-build path via the migration chain, relocated to run-time; the schema-version-set test now accepts 1.3; the help-overlay snapshot regenerated for the grown content). The single outstanding failure is the live-zsh subprocess completion test, which is environmental (the completion script-gen tests pass; the iter's new verbs add candidates, they cannot remove `wave`).

Naming + style conventions hold across the changed set (canonical `agent_role`/`effort_bucket`/`scope_id`; bare log keys; lowercase no-trailing-period errors; ASCII-only source). Schema + autogen goldens regenerated so the drift gates pass.

## Followups

Carried forward (none block I02 close):

- **W30 (declared, DRY):** `wave_budget_minutes` (`thresholds.py`) is the canonical wave-id -> pessimistic-minutes projection, but two consumers still re-derive it inline (`daemon/methods/state.py:_wave_elapsed_budget_minutes`, `widgets/roadmap_tree.py:_wave_time_budget_minutes`). Consolidate onto the shared helper. Non-blocking — the three agree by construction today.
- **W08 (declared, scope):** `eawf why` covers the explainable-kinds catalog (adds `hypothesis`) but excludes `incident`/`backlog` (not in `urn.py` `URN_KINDS`); extending the URN catalog is a scoped follow-up.
- **W14 (cosmetic, no action):** the plan-table SHA-derivation display for the W14 research-spike wave grep-matches an unrelated P29-I01 commit; the genuine deliverable is the verdict brief (present). Display-only noise.
- **Operator-review follow-up (post-audit, in flight as W31):** dogfooding surfaced four attention-feed refinements — user/portfolio scope must aggregate attention across registered repos (today reads the empty synthesized state); wave-derived attention must be scoped to the active phase/iter (historical failed/ready waves currently never clear); attention rows need a relative time-ago; and an explicit dismiss alongside the live auto-clear. W31 was appended to this ACTIVE iter and is verified independently of this 30-wave audit.

## References

- Plan + criteria: `eawf plan show --iter P29-I02`; per-wave `eawf wave show P29-I02-W<NN> --dispatch-prompt`.
- Branch diff: `git log --grep '[P29-I02' 533bd4c..HEAD` (30 wave deliverables + bookkeeping).
- Keystones: `src/eawf/surfaces/tui/modes/registry.py`, `src/eawf/surfaces/tui/widgets/header.py`, `src/eawf/surfaces/tui/app.py`; `src/eawf/kernel/state/models.py`, `src/eawf/kernel/migrations/v1_2_to_v1_3.py`, `src/eawf/workflow/lifecycle/wave.py`.
- Honesty enforcement: `src/eawf/surfaces/tui/modes/{trust,evidence}.py`, `src/eawf/surfaces/tui/widgets/registry_pane.py`, `src/eawf/surfaces/tui/screens/overlays/cross_repo_pr.py`.
- W14 spike verdict: `.ea/local/research/2026-06-01-w14-modes-chassis-verdict.md`.

## Provenance

Produced by the `/flow full P29-I02` pipeline (audit stage) on 2026-06-01: a fresh-context `eawf:auditor` subagent (no parent conversation) re-read the committed diff against each wave's declared `success_criteria` per the verify-before-claim ladder, citing source `file:line` evidence per criterion and applying the keystone/honesty fail rule. Waves were delivered by worktree-isolated executors, cherry-picked into the feature branch, and verified-landed before close.

## Scrub

No absolute local paths, hostnames, credentials, or PII. References are repo-relative or eawf-internal ids.
