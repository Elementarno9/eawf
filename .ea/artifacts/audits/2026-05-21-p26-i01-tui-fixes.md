# P26-I01 TUI manual-test fix sweep audit

## Summary

- P26-I01 appended eleven waves (W30-W40) after manual testing of the v0.3 surfaces; this fresh-context pass confirms all eleven implement their success criteria against the source tree. Overall verdict **minor**: every wave's primary contract holds, the full suite is green (4378 passed, 26 skipped at the audited HEAD), and the two audit follow-ups were themselves resolved in-phase by W39 + W40 [1].
- Audited HEAD: `ca56d55`; the per-wave evidence below cites each wave's commit on `feature/eawf-v0.3-p26` [1].
- **W30 — CLI lazy-import — pass.** Completion startup ~0.52s → ~0.24s; `import eawf.cli.app` pulls none of the heavy offenders (state.models / validate.strict / jinja2 / yaml); `tests/perf/cli/test_import_budget.py` gates it [2].
- **W31 — pane display — pass.** Roadmap/Backlog `overflow-x: hidden` + ellipsis; detail msgbox label:value alignment; roadmap pre-scroll to the active phase; Events timestamps normalized to one UTC format + width-aligned; lifecycle glyph colour tint [3].
- **W32 — keybindings — pass.** Raw `w`/`r`/`u` scope-switch (ctrl retained as documented aliases per help.py); refresh moved `r` → `F5`; dead `b`/`w`-waveboard bindings removed [4].
- **W33 — palette + audit overlay — pass.** Empty-input + Enter dismisses the palette; the audit overlay free-text id input is removed and the scope-derived audit id is surfaced [5].
- **W34 — ConfigModal — pass.** Registry-driven config window covering all CONFIG_REGISTRY tabs/keys (coverage test asserts the full registry); saves route through the layered-config writer and never touch `state.json` (test asserts a `.yaml` target); `c` + `/config` wired [6].
- **W35 — legacy tui/ removal — pass.** The legacy Rich `src/eawf/tui/` tree (9 modules) + 19 legacy tests are removed; `tui_v2` owns the non-TTY status frame + workspace `offline_render` via `src/eawf/tui_v2/offline.py`; source grep for `eawf.tui` is clean; deletion enumerated in the commit body [7].
- **W36 — counter scope + archive cascade — pass.** Status-pane wave counters scope to the active phase; `archive_phase` cascade-abandons non-terminal child waves/iters; `abandon_orphaned_waves` is a pure idempotent transform [8].
- **W37 — roadmap arrows — pass.** Plain `←`/`→` collapse/expand the roadmap tree (Textual Tree binds only shift+arrows by default); 8 Pilot tests [9].
- **W38 — ConfigModal nav — pass.** Focus-zone model: `↑`/`↓` traverse tab-bar↔fields, `←`/`→`+Space are focus-sensitive, the single-field runtime tab is navigable, with a visible zone-aware key helper [10].
- **W39 — polish — pass.** 23 orphaned `tests/golden/tui/` legacy goldens (zero live consumers post-W35) removed + enumerated; tui_v2 docstrings updated to reflect the legacy removal [11].
- **W40 — iter-activation guard — pass.** `claim_wave` auto-activates a PLANNED parent iter (+ stamps `current.iter_id`) after the gates pass, inlined to avoid the `activate_iter` `active_wave_ids` reset; closes the gap that let P26-I01 run 30 waves while PLANNED [12].
- A state backfill ran `abandon_orphaned_waves` through the canonical writer: the 16 `P21-I01-W01..W16` zombie waves + iter `P21-I01` are now ABANDONED, so global PENDING waves == 0 [13].

## Followup triage

Two follow-ups surfaced during the audit; both were resolved in-phase (not deferred).

- **RESOLVED IN-PHASE — orphaned legacy goldens.** The `tests/golden/tui/*` fixtures left dangling by the W35 legacy removal were deleted in W39 (deletion-rule compliant, enumerated) [11].
- **RESOLVED IN-PHASE — iter-activation gap.** The lifecycle gap that let waves run under a PLANNED iter is closed by W40's `claim_wave` auto-activation guard [12].
- **NOTE (trivial, no fix).** The W32 commit message says "ctrl dropped" but ctrl+w/r/u are intentionally retained as documented aliases (help.py); the prose is imprecise, the design is consistent [4].

## References

[1] `.ea/state.json` — `state.waves["P26-I01-W##"]` outcome strings + commit SHA chain on `feature/eawf-v0.3-p26`; audited HEAD `ca56d55`
[2] commit `40ff1ec [P26-W30] perf: lazy-import heavy deps in CLI command handlers` + `tests/perf/cli/test_import_budget.py`
[3] commit `38f81da [P26-W31] fix: TUI pane display` + `src/eawf/tui_v2/widgets/{roadmap_tree,backlog_table}.py` + `src/eawf/tui_v2/screens/overlays/{detail,events}.py`
[4] commit `b549adc [P26-W32] fix: TUI keybindings` + `src/eawf/tui_v2/app.py` + `src/eawf/tui_v2/screens/help.py`
[5] commit `ce6273e [P26-W33] fix: TUI palette empty-Enter dismiss + remove audit overlay free-text input` + `src/eawf/tui_v2/palette/command_palette.py`
[6] commit `914bd5a [P26-W34] feat: tui_v2 ConfigModal + EditFieldModal` + `src/eawf/tui_v2/screens/overlays/{config_modal,edit_field}.py`
[7] commit `a74dbf5 [P26-W35] refactor: remove legacy tui/ tree` + `src/eawf/tui_v2/offline.py`
[8] commit `8a8d847 [P26-W36] fix: scope status-pane wave counters to active phase + abandon-cascade` + `src/eawf/lifecycle/phase.py` + `src/eawf/state/migration.py`
[9] commit `ebdf9ad [P26-W37] fix: roadmap tree plain left/right collapse-expand` + `src/eawf/tui_v2/widgets/roadmap_tree.py`
[10] commit `ab16b01 [P26-W38] fix: ConfigModal focus-zone nav` + `src/eawf/tui_v2/screens/overlays/config_modal.py`
[11] commit `85a4bf4 [P26-W39] chore: polish — remove orphaned tui goldens + tui_v2 doc consistency`
[12] commit `ca56d55 [P26-W40] fix: auto-activate iter on first wave claim` + `src/eawf/lifecycle/wave.py`
[13] commit `a608c8e [P26] state: backfill P21 zombie waves to ABANDONED` + `src/eawf/state/migration.py`

## Provenance

- audit_id: A33-P26-tui-fixes
- audit_kind: review
- scope_id: urn:eawf:v1:repo:eawf
- verdict: minor (all eleven W30-W40 waves implement their core criteria; full suite green at 4378 passed; both audit follow-ups resolved in-phase by W39 + W40)
- created_at: 2026-05-21
- author: claude-opus-4-7 (session eawf-flow-p26)
- supersedes: none

## Scrub

- status: clean
- references: repo-relative paths + commit SHAs + Eawf URN only
- local paths: none in body
- real emails: none
- abstract placeholder names: not applicable
